fix(rpc): failed artwork upload no longer poisons the cache + in-flight upload dedup + orphaned-strings purge + template dialog input contrast
  - Device fix (user-reported: after a live customization the presence image stayed dead until a song switch or stop/restart): a failed external-assets upload used to fall back to the raw http URL, which `ArtworkCache` then CACHED — every later presence carried an unrenderable asset and the failure was never retried for that artwork (until a token change / connection recreation cleared the cache). Now a failed upload returns null: nothing is cached, and when no image resolved the phase-2 re-send is skipped (the phase-1 text-only presence is kept — upstream RM:386-389 semantics), so the next 5 s tick retries the upload on a cache miss and the image comes back without switching song
  - Root cause of the failures: duplicate concurrent uploads of the same artwork (the 5 s refresh tick and the advanced-settings re-sync both resolve the same URL; PW-7 keeps a same-image in-flight job running) → Discord external-assets 429s. `ArtworkCache` now dedups in-flight fetches per key: concurrent callers share ONE fetch (owner publishes the outcome to joiners and releases the slot on completion/cancellation/failure)
  - App: template input dialog (all 8 fields — name/state/details/pause/2 buttons) got palette-explicit `OutlinedTextField` colors — the default Material3 onSurface made the typed text + placeholder unreadable on the dark dialog background (`background1`): text = `palette.text`, placeholder = `palette.textSecondary`, cursor + focused border = `palette.accent`
  - Strings purge (user-committed as "fix string(s)"): 367 orphaned strings removed from `values/strings.xml` (verified 0 references in code/XML, no dynamic `getIdentifier`, manifest label is a Gradle placeholder) + the 3 lot leftovers (`txt_listen_to_ytmusic`, `txt_get_n_zik`, `discord_enable_browsing`) — all purged from the 49 `values-*` files (~17.5k lines total). Consistency audit: 0 excess strings in any locale; the 32 new lot strings are intentionally absent from locales pending the l10n sync; the 4 regional files (pt-rBR, sr-rCS, zh-rCN, zh-rTW) are partial overlays by design
  - Verification (`--rerun`): `:discordrpc:testDebugUnitTest` 57/57 green (3 new tests: shared in-flight fetch, cancelled-fetch slot release, failed-upload retry), `:ComposeN-Zik:testDebugUnitTest --tests "app.n_zik.android.extensions.discord.*"` green. Device re-test of the image fix pending
  - Note: `modules/discordrpc` is a git submodule — committed in the submodule first, then the pointer bumped in the main repo

feat(rpc): Discord RPC upstream parity lot (19 items) + advanced mode overhaul & browsing removal
  - Submodule `discordrpc`: gateway URL → v10 + resume URL used verbatim (item 2); heartbeat jitter ±5% computed once per session (item 3); generation guard — a superseded session's stale close is ignored, superseded heartbeat/watchdog jobs cancelled (item 1 + review); `sequence` only from `s > 0` (item 14); single `canResume` decode (item 15); presence update (op 3) + `clearPresence` carry `status`/`afk`/`since = 0` + activity type (item 6); INVALID_SESSION → session/seq reset + close 4000, reconnect on a fresh socket (item 18); `ArtworkCache` capped at 128 (insertion eviction, order-agnostic) + `clearArtworkCache()` exposed and wired to token change (item 10); `NonCancellable` removed from the external-assets upload — cancellable end to end (item 13); single public `DiscordRpc.APPLICATION_ID = 1379051016007454760` (item 11); `consumer-rules.pro` R8 keeps (item 16); 429-upgrade design kept + verification test (item 12)
  - 2-phase `setActivity`: the text-only presence lands immediately, the image job re-sends with `mp:` only if the `activityId` guard still passes; the in-flight upload is now cancelled only when the images actually change — a same-artwork 5 s refresh no longer kills a slow first upload (root cause of the intermittent artwork loss), a different-image supersede still cancels (2 new regression tests)
  - App: speed-aware timestamps (`start`/`end` divided by speed, `[1.50x]` details suffix, re-send on speed change via `onPlaybackParametersChanged`) (item 5); live refresh loop every ~5 s with network re-arm (item 8); 60 s pause → clear, 10 min idle → `closeDirect()` + connection re-created on the next event (item 7); persistent error — module `terminalCloseCode` StateFlow + app `DiscordRpcErrorState` (INVALID_TOKEN / RECONNECT_FAILED) + dismissible banner in Settings, cleared on recovery (item 9 + review); `fetchDiscordUser` reads `global_name` with `username` fallback (item 17); IDENTIFY audit: no non-conformance, payload untouched (item 19)
  - Advanced mode (item 6): new `DiscordAdvancedKeys.kt` (13 encrypted prefs — no legacy `EncryptedPreferences.kt` edit), `DiscordTemplateRenderer` + `DiscordActivityBuilder` (upstream template port, name fallback "N-Zik"), activity-type selector (applied always), pause-template line, 2 custom buttons, live preview card fed by the current playback (~100 ms)
  - User-directed renegotiation (2026-09-21, post-review): browsing presence removed entirely (PW-1); user-status selector removed — `status = online`, `afk = false`, `since = 0` on every presence update, activity-type selector kept (PW-2); "Show pause presence" switch, default ON, OFF short-circuits `sendPausedPresence` (PW-3); media artwork in the live preview with a neutral placeholder (PW-4); advanced submenu in/out animations (fadeIn/scaleIn 0.9, tween 1000 ms) (PW-5); live preview visible in BOTH advanced modes (PW-6); intermittent artwork loss fix (PW-7, see 2-phase above)
  - Review (3 layers, 30 findings): 15 patched (stale-4004 banner cleared on recovery, superseded heartbeat jobs, structurally cancellable upload, refresh-loop null-provider guard, log wording, advanced-key reset flow, fetcher-seam null semantics, new `ExternalAssetUploadTest` (2 tests, scripted Ktor engine), `clearArtworkCache` coVerify, advanced re-sync test, button-toggle icon), 14 deferred to `deferred-work.md`, 1 false (orphaned-socket race — closed by `close()`'s own launch, instance disposed)
  - `values/strings.xml` only: −6/+2 across the lot (status + browsing strings removed, pause-presence switch added)
  - Device fix (2026-09-21, user-directed after on-device diagnosis): a fresh session sends its first heartbeat IMMEDIATELY (no HELLO jitter) — the gateway only completes the identify (READY) once it has received the first heartbeat (device logs: READY 0.5–2.5 s after the first heartbeat in every run), so the jittered first heartbeat (up to `heartbeat_interval`, ~42 s) delayed READY, and with it the first presence update, by the same amount. The resume path keeps the upstream order (jittered heartbeat, then RESUME); the loop keeps its jittered interval. New handshake test pins it (op:1 at virtual time 0 with a 45 s interval); the loop-stability jitter test now checks the loop ticks
  - Verification (final, `--rerun`): `:discordrpc:testDebugUnitTest` 54/54 green, `:ComposeN-Zik:testDebugUnitTest --tests "app.n_zik.android.extensions.discord.*"` 35/35 green, `:ComposeN-Zik:assembleDebug` built by the maintainer. Device-verified and approved by the user: first-song presence in ~2–3 s (was up to ~40 s)
  - Note: `modules/discordrpc` is a git submodule — committed in the submodule first, then the pointer bumped in the main repo

fix(rpc): capture refused WebSocket upgrades so 429 is honored, harden reconnect state (submodule f1c2b44 / main 3dcfd7e392)
  - Ktor surfaces a refused WS upgrade as a bare exception (no status, no headers): the default `clientFactory` now installs an OkHttp interceptor that records non-101 responses (status + Retry-After) on a companion-registered `RefusalHolder` keyed by the engine config; `establishConnection()` consumes the capture — a 429 schedules a rate-limited reconnect (Retry-After, 60 s floor) instead of rethrowing, any other failure still propagates
  - `reconnectAttempts` is now an `AtomicInteger` (`connect()` runs on app threads while the reconnect loop runs on the background dispatcher); `connect()` after `close()` is a no-op (terminal gate — a released client would burn the budget and flip `reconnectAbandoned`, a false toast); `updatePresence()` aborts early when the gateway is closed/abandoned while waiting for the session
  - `GatewayWebSocketReconnectPolicyTest` gateways now run on a no-op `RecordingEngine` (no real OkHttp client created per test instance)
  - New `GatewayWebSocketUpgradeTest` (8 tests, scripted engine): 101 -> session delivery, 429 refusal with/without Retry-After (60 s floor), connect-after-close no-op, terminal 4004, 4000 reconnect + clean 1000 stays offline, budget reset on success, superseded/aborted presence handling — every test tears the session down (Close 1000) before it ends so `runTest` never drains a live heartbeat loop (that was an observed OOM: 14M+ frames piling up in the unbounded outgoing channel, fixed by the teardown, no heap bump needed)
  - App: `discord_rpc_reconnect_failed` reworded (what resumes status updates: next media change or manual reconnect); new `DiscordBrowsingSettingTest` test — the reconnect-abandonment watch fires the error toast (`mockkObject(Toaster)`, Main dispatcher swapped)
  - Verification (`--rerun`): `:discordrpc:testDebugUnitTest` 26/26 green, `:ComposeN-Zik:testDebugUnitTest --tests "app.n_zik.android.extensions.discord.*"` 9/9 green (7 + 2), `:ComposeN-Zik:assembleDebug` green
  - Note: `modules/discordrpc` is a git submodule — committed in the submodule first (f1c2b44), then the pointer was bumped in the main repo (3dcfd7e392)

fix(discord): port upstream RPC hardening (max reconnect attempts, stale activity guard, reconnect policy) + abandoned toast
  - `GatewayWebSocket` (submodule `discordrpc`) now abandons automatic reconnection after `MAX_RECONNECT_ATTEMPTS = 7` (`reconnectAbandoned: StateFlow<Boolean>`, counter reset on established connection and on manual `connect()`); previously it retried forever with the ×2 backoff capped at 60 s
  - Stale activity guard ported from upstream: `AtomicLong activityId` in `DiscordRpcConnection`, invalidated by `clearActivity()`/`close()`/`closeDirect()` — an in-flight `setActivity` (image resolution or the ≤30 s session wait) is cancelled instead of re-writing an older presence (checked before AND after the wait, via `updatePresence(staleCheck)`)
  - `GatewayWebSocket.close()` now closes the injected `HttpClient` after the session close (upstream `closeHttp()` equivalent — Ktor's `HttpClient(engine)` factory sets `manageEngine=false`, so the OkHttp engine hosting the socket was never released per connection cycle)
  - WS upgrade refused with 429 now honours the `Retry-After` header with a 60 s floor (pure helper `reconnectDelayFor`); reconnect backoff gains ±25 % jitter (pure helper `applyJitter`); terminal close codes centralised in `reconnectsOnClose` — 1000 (clean remote close, session reset, no reconnect), 4004 (invalid token, unchanged), 4014 (fatal, no reconnect)
  - `DiscordPresenceManager` watches `connection.reconnectAbandoned` per connection (2 creation sites, via a private helper) → warning log + `Toaster.e(R.string.discord_rpc_reconnect_failed)` on the UI dispatcher; new string in `values/strings.xml` (service toast: masked in background on API 30+, visible in foreground — accepted limitation, persistent settings banner deferred)
  - Test seams: `clientFactory` on `GatewayWebSocket`, `gatewayFactory` on `DiscordRpcConnection` (defaults unchanged, no production caller touched); no MockK in the submodule — pure fakes + `TestDispatcher`
  - New submodule tests: `GatewayWebSocketMaxAttemptsTest`, `DiscordRpcConnectionStaleGuardTest`, `GatewayWebSocketReconnectPolicyTest`, extended `GatewayWebSocketCloseTest` (HttpClient closure observed through an `AutoCloseable` plugin + recording `HttpClientEngine` fake with `@file:OptIn(InternalAPI)`)
  - App test-only change: `DiscordBrowsingSettingTest` helper stubs `reconnectAbandoned` on the relaxed mock (MockK cannot answer the new property)
  - Verification (`--rerun`): `:discordrpc:testDebugUnitTest` 18/18 green, `:ComposeN-Zik:assembleDebug` green, `:ComposeN-Zik:testDebugUnitTest --tests "app.n_zik.android.extensions.discord.*"` 8/8 green
  - `assets/notes/TODO.txt`: removed the "browsing key not being respected randomly" item (fixed in `94a5581dbf`)
  - Note: `modules/discordrpc` is a git submodule — its changes were committed in the submodule first, then the pointer was bumped in the main repo

fix(discord): browsing key randomly ignored — re-validate before the RPC write, never re-send a cleared presence
  - `GatewayWebSocket.clearPresence()` (submodule `discordrpc`) now nulls `lastPresence`, so `resendLastPresence()` (fired on READY/RESUMED after each reconnect) can no longer re-emit a presence the user already cleared — root cause of the "browsing" status coming back randomly (after BDD import / reconnects, fixed by a manual toggle off→on)
  - `DiscordPresenceManager.sendActivity` re-evaluates a `browsingPrecondition` right before the gateway write: an in-flight browsing write started while the setting was on is cancelled if it is disabled before the write lands (the gateway can wait up to 30 s for the session before writing)
  - `onPlayingStateChanged` updates `hasActiveMediaItem` before the token/network early-returns, so a media event seen while offline no longer leaves the flag stale for later route changes
  - Test seams added to `DiscordPresenceManager` (`connectionFactory`, nullable `tokenValidator`; defaults via the `defaultConnection` companion) — no production behavior change
  - New `DiscordBrowsingSettingTest` (6 tests): OFF never sends, ON + no media sends, ON + active media never sends, in-flight write cancelled on toggle-off, flag survives a network early-return, toggle handler clears on off and re-sends on on
  - Verification: `:ComposeN-Zik:assembleDebug` green, targeted Discord unit tests 8/8 green (6 new + 2 existing seam tests); full-suite run interrupted by the user at the compile-OK stage
  - Note: `modules/discordrpc` is a git submodule — its 1-line fix was committed in the submodule first, then the pointer was bumped in the main repo

refactor(legacy): #606 part 5 — dedup 19 duplicated uiRoundnessShape imports (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - The post-part-4 global re-audit (6 sub-agents) found 11 legacy files carrying 2-7 identical `import app.n_zik.android.uiRoundnessShape` lines (successive-merge artifacts) — 19 redundant lines removed, exactly one import kept per file (`Developer`, `Translator`, `SwipeableContent`, `HorizontalNavigationBar`, `Dialog`, `ArtistItem`, `PlaylistItem`, `SongItem`, `MiniPlayer`, `Modern`, kreate `ActionBar`)
  - Pure import hygiene, zero behavior change; verification: `:ComposeN-Zik:assembleDebug` green + repo-wide grep: 0 legacy file left with ≥2 identical `uiRoundnessShape` imports
  - Note: 7 app-package files with the same duplicate were discovered at commit time (`components/SongItem`, `FollowButton`, `BuildTransitionWarningDialog`, `CheckForUpdateDialog`, `MajorUpdateWarningDialog`, `NewUpdateAvailableDialog`, `UpdateScreen`) — out of the part-5 scope (legacy-only), tracked for a follow-up

fix(threading): #606 part 4 — last scope handler, legacy DB contexts, test literals, hygiene (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - Part 4 of the #606 audit: the post-part-3 global re-audit (6 sub-agents over app + legacy + extensions + modules + commonMain + tests) found 1 remaining production violation (3rd module scope without handler) + test-code literals + legacy sites without context on DB flows (legacy never received the "DB flows get `context = NzikDispatchers.DATA`" rule — part 2 was raw→lifecycle-aware migration only)
  - `GatewayWebSocket` scope (submodule discordrpc) now carries a `CoroutineExceptionHandler` (`Timber.tag("DiscordGateway")`) — the 3rd long-lived module scope guarded (part 3 handled `Invidious.scope` + `DiscordRpcConnection.httpScope`)
  - 30 legacy sites on DB-derived flows (13× `MyDownloadHelper.downloads`, Room DAO flows in `DownloadUtils` / `BookmarkStateManager` / `LikeStateManager` / playlist songs) now pass `context = NzikDispatchers.DATA`; the only legacy site left without context is `DownloadUtils.kt:145` (`downloader.progresses` = in-memory Media3 flow, documented)
  - 6 raw `Dispatchers.IO` literals in `MediaItemUtilsTest` → `NzikDispatchers.DATA` (single-source-of-truth)
  - Hygiene: `ChipItemColored` chip color now `remember`-stable per composition (was a `derivedStateOf` regenerating a random color on every invalidation); 4 duplicate `uiRoundnessShape` imports in `VideoItem.kt` deduplicated
  - Tests: new `GatewayWebSocketScopeHandlerTest` (handler logs + siblings survive; production wiring pinned on a real instance); `NzikDispatchersTest` (11 pins) + discordrpc seam pins (`Dispatchers.IO` standalone default) untouched by design
  - Verification: `:ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:test` (683 tests, 0 failure) + `:oldtube:test` (3/3) + `:discordrpc:test` (6/6) green; final greps: all 3 module scopes guarded, 0 raw `Dispatchers.IO` in the app test, legacy without context = the 1 documented memory flow
  - Note: `modules/discordrpc` is a git submodule — its 2 files were committed in the submodule first, then the pointer was bumped in the main repo

fix(threading): #606 part 3 — residual DB contexts, module scope handlers, dead imports (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - Part 3 of the #606 audit: the full post-part-2 re-audit (6 parallel sub-agents over app + legacy + extensions + modules + commonMain + tests) concluded 0 violations on the #606 perimeter; this change closes the 3 secondary residuals it flagged (the 4th — 2 duplicate imports from the part-2 migration — was already fixed in 93991d0dc9)
  - 43 app-package sites collecting DB-derived flows (Room tables, like/bookmark managers, playcounts, `MyDownloadHelper.downloads`) now pass `context = NzikDispatchers.DATA` to `collectAsStateWithLifecycle` — off-main collection as documented by `LikeStateManager`'s KDoc; the 6 `StateFlow` sites switch to the `Flow<T>` overload with `initialValue = <flow>.value` (no empty-state flash at startup)
  - The ~17 "simple state" sites (MutableState/UI-local: `MainActivity` routes, `UpdateScreen`, `ImageCacheFactory.storeVersion`, `Seek*`, ...) intentionally keep no context — part-1 precedent, no IO gain
  - `Invidious.scope` and `DiscordRpcConnection.httpScope` now carry a `CoroutineExceptionHandler` (previously an unhandled exception was unlogged and sibling coroutines risked cancellation): the innertube module uses its existing `InnertubeLogger` seam (no Timber in that module), discordrpc uses `Timber.tag("DiscordRpc")`
  - 4 dead `import androidx.compose.runtime.collectAsState` removed from legacy files (AlbumsItemMenu, VideoItem, StatsForNerds, OnlineSearchList)
  - Tests: `InvidiousScopeTest` +1 (failure logged by the scope handler, siblings survive), new `DiscordRpcHttpScopeHandlerTest` (same pattern, Timber tree planted in-test)
  - Verification: `:ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:test` (683 tests, 0 failure) + `:oldtube:test` + `:discordrpc:test` green; final greps: 0 DB site without context in the app package, 0 dead collectAsState import in legacy, both module scopes guarded
  - Note: `modules/discordrpc` is a git submodule — its 2 files were committed in the submodule first, then the pointer was bumped in the main repo

fix(downloads): enforce download quality in URL cache and always track it
  - Root cause 1: the download resolve path reused the player's `streamUrlCache` — a URL resolved at the *streaming* quality. For a track just played, the download silently reused the stream URL (wrong quality), skipped `processForDownload`, and `Format.downloadQuality` was left NULL → Stats for Nerds showed "Unknown" and the update-downloads non-compliant count was off
  - Root cause 2: even on a fresh download resolve, the `upsertSongFormat` call carrying `downloadQuality` was skipped by the `justInserted` guard when the streaming path had just inserted the same Format row
  - `StreamUrlCache` entries are now tagged with the resolved `InnerTubeXAudioQuality`: `put(...)` records it, `get(mediaId, quality)` only returns an entry resolved at the same quality (Auto maps to metered ? LOW : AUTO, so the comparison is on the *resolved* value, i.e. what the download actually used)
  - The player cache stays on the streaming path (tagged with the streaming quality); the download path now reads/writes its own dedicated `MyDownloadHelper.songUrlCache` (previously dead — never used), cleared by the existing download-quality preference listener so an old-quality URL is never reused after a quality change
  - New `audioQualityToInnerTubeX` helper (same mapping as `downloadQualityToInnerTubeX`) — the streaming `process`/resolve path uses it for its cache get/put; both mappings are pinned by tests so the cache quality comparison cannot drift
  - `processForDownload` now captures `trackedDownloadQuality` from its `downloadQualityFormat` parameter (a snapshot of the setting at resolution time) instead of re-reading `MyDownloadHelper.downloadQualityFormat` after network work
  - New `FormatTable.updateDownloadQuality` (plain `UPDATE ... WHERE songId`): after the fire-and-forget `upsertSongFormat`, the download path records the tracked quality even when the `justInserted` guard skipped the format upsert
  - Tests: `StreamUrlCacheTest` — 2 new cases (entry not returned for a different quality with generation unchanged, put at a different quality replaces the entry) + all existing cases adapted to the quality parameter; `DownloadQualityMappingTest` — 3 new cases pinning the streaming mapping
  - Verification: `:ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:test` green. Verified end-to-end on device: streaming High + download High on the same track → the download now resolves fresh at its own quality (`Download resolving for … (quality=HIGH)` instead of a silent cache hit) and Stats for Nerds shows the tracked quality instead of "Unknown"

fix(threading): route the legacy packages' state collection through NzikDispatchers (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - Part 2 of the #606 audit: the full post-remediation re-scan (legacy included, post-`299e2578d9`) found 56 residual `collectAsState` sites + 3 bare `runBlocking` in `app.it.fast4x.rimusic.*` / `app.kreate.android.*` — the `app.n_zik.android.*` package was already at 0 violations since part 1; the user explicitly lifted the legacy READ-ONLY constraint for this change
  - 48 `collectAsState(…, NzikDispatchers.DATA)` across 27 fast4x + 2 kreate files → `collectAsStateWithLifecycle(…, context = NzikDispatchers.DATA)`: off-main collection preserved, now lifecycle-aware (collection pauses when the UI is no longer STARTED)
  - 8 contextless `collectAsState()` (LandscapeBars discord route/tab, AccountsSettings syncStatus, playerUpdateTrigger in Player/MiniPlayer/Thumbnail, sleep timer in Player + kreate ActionBar) → `collectAsStateWithLifecycle(initialValue = …, context = NzikDispatchers.DATA)` (user decision: these also collect on DATA; StateFlow receivers use the `Flow<T>` overload since the `StateFlow` overload exposes no `context`); `Thumbnail`'s elvis fallback preserved
  - `utils/Player.kt` `excludeMediaItems`: 3 bare `runBlocking` (Room disliked songs/artists/albums queries) → `runBlocking(NzikDispatchers.DATA)` + justification comment — the public non-suspend API keeps its signature (11+ callers, mocked non-suspend in `PlayerQueueOffMainTest`); UI callers are already off-main via `withContext(NzikDispatchers.DATA)`, so the queries now run on the named dispatcher
  - Ruled WITHOUT change (documented): `AudioVolumeObserver.kt:29` `Handler(Looper.getMainLooper())` — `ContentObserver` requires a Handler on a Looper, the main looper is the documented choice in the code, and the dispatcher system does not cover Java callback APIs (same ruling as part 1's `MoreExecutors.directExecutor()`)
  - New test `ExcludeMediaItemsDislikedQueriesOffMainTest` (2): with the dislikes settings enabled, the 3 Room queries run on a thread different from the caller's; with the settings disabled, no query is issued at all
  - Verification: `:ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:test` green; grep `collectAsState(` in fast4x/kreate = 0 hit outside `collectAsStateWithLifecycle(`. Manual on-device checks suggested: legacy screens (stats, history, playlists, search), player with sleep timer, volume change (AudioVolumeObserver untouched)

feat(downloads): dedicated download quality selection + "update downloads" re-download batch
  - New `downloadQualityFormat` preference (Auto/High/Low, default Auto), independent of the streaming quality: enum `DownloadQualityFormat` (new, `app.n_zik/android/enums/`), setting UI in `DownloadQualitySettings.kt` (`DownloadQualityEntry`), wired in `NetworkSettings` under the streaming quality
  - Mapping to InnerTubeX `AudioQuality` is the same as streaming (High→HIGH, Low→LOW, Auto→ metered ? LOW : AUTO, `StreamResolver.downloadQualityToInnerTubeX`); InnerTubeX v0.6.0/v0.7.0 expose no MEDIUM tier (verified on the `AudioQuality` enum), so "Medium" was dropped from both `AudioQualityFormat` and `DownloadQualityFormat` — stored "Medium" values fall back to Auto through the existing `getEnum` catch, no migration code
  - Per-download tracking: new `Format.downloadQuality` column (Room migration 41→42, `From41To42Migration` + schema 42.json, default Auto), written by `StreamResolver.processForDownload` after a successful download; the player URL cache is invalidated on download completion so a streamed URL is never reused for a download
  - Live change without player restart: `MyDownloadHelper` observes the preference (`Download quality changed to ...`) and `PlayerServiceModern.onSharedPreferenceChanged` logs it under `NZik_Network` alongside the audio/image branches; `onMediaItemTransition` log now carries a `Download:` field
  - "Update downloads" button with pending-count badge: `MyDownloadHelper.countNonCompliantDownloads()` counts tracks whose stored quality differs from the current selection, `updateDownloads()` re-downloads them at the current quality through the existing `startBatchDownload`
  - Quality switcher and Stats for Nerds now expose the download quality (4th row); Stats for Nerds hides that row while the current track has no local download (`downloadCachedBytes == 0L`)
  - Crash fix: `UpdateDownloadsDialog.onConfirmAction` StackOverflow (recursive recomposition) — the dialog is dismissed before the batch starts
  - Race fix: `StreamResolver` captures the tracked download quality before the async DB write, so a fast quality switch is not lost by an in-flight download
  - Tests: `DownloadQualityMappingTest` (3), `FormatDownloadQualityTest` (6), `From41To42MigrationTest` (3), `DatabaseUpgrade40To41Test` extended to chain 40→41→42 (2); full suite 676 tests green
  - Verification: `:ComposeN-Zik:assembleDebug` green; end-to-end verified on device (High→Low switch triggers a re-download at LOW, URL cache invalidated). The full test suite was last run before the two final small fixes (download-quality change log branch, Stats for Nerds row hiding), which are covered by the on-device verification. InnerTubeX bumped v0.6.0→v0.7.0 in the same change (maintainer decision)

fix(threading): route every remaining scheduling site through NzikDispatchers (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - Full audit (4 parallel scans) found 62 residual sites outside the threading system; all remediated and re-audited clean (0 residual violations in `app.n_zik.android.*`, `extensions/`, `modules/`)
  - PoToken (`PoTokenWebView.kt`, `PoTokenGenerator.kt`, 8 sites): raw `Dispatchers.Main` → `NzikDispatchers.UI` (incl. `NonCancellable +` in `closeQuietly`); `scope = MainScope()` → `NzikDispatchers.fireAndForget(NzikDispatchers.UI)` so the WebView scope gets the Timber crash handler. Identity-preserving rename (`UI === Dispatchers.Main`, pinned by `NzikDispatchersTest`)
  - `runBlocking` kept only where a synchronous API forces it, now on the named dispatcher: `StreamResolver.kt` `DataSpec.process`/`processForDownload` (`runBlocking(Dispatchers.IO)` → `runBlocking(NzikDispatchers.DATA)`, ExoPlayer `ResolvingDataSource` sync-return contract) and `SessionMediaItemMapper.loadArtworkBytes` (bare `runBlocking` → `runBlocking(NzikDispatchers.DATA)`, so the Coil artwork I/O for Android Auto no longer blocks on the caller thread)
  - 48 `collectAsState(...)` across 20 screens/menus → `collectAsStateWithLifecycle(...)`, preserving the initial value and `context = NzikDispatchers.DATA` (the 3 contextless sites — `MainActivity` discord route + player update trigger, `SyncStatusDialog` — use the plain overload); net effect: flow collection pauses when the UI is no longer STARTED, collection stays off-main
  - `NetworkQualityHelper.isNetworkAvailableComposable`: `produceState { observeConnection().collect{} }` (collected forever on Main, missed by the initial `collectAsState` audit) → `collectAsStateWithLifecycle(initialValue = isNetworkConnected(context), context = NzikDispatchers.DATA)`
  - `modules/nextvisualizer` `VisualizerHelper`: removed dead `Handler`/`Runnable` lateinit fields, the commented `startDebug()` and the live `stopDebug()` (guaranteed crash — the `Handler` was never initialized)
  - Dead code: removed the unused legacy KMP Room database (commonMain `database/` — `MusicDatabase.kt` with its raw `setQueryCoroutineContext(Dispatchers.IO)` — + 14 entities, and `getMusicDatabase.kt`; nothing ever called `getRoomDatabase`/`getAndroidDatabaseBuilder`, the app uses `app.n_zik.android.core.database.Database`), plus 2 dead imports (`HomeDiscovery.kt` `collectAsState`, `PlayerServiceModern.kt` nonexistent legacy `collect`)
  - New test `SessionMediaItemMapperLoadArtworkOffMainTest` (Robolectric, `coEvery` on the suspend `ImageCacheFactory.loadBitmap`): asserts the artwork load runs on a thread different from the caller's while the bytes still land on the media item
  - `TODO.txt`: removed the "Optimization — new threading system" and "Miniplayer — swipe action" sections (both tasks done)
  - Verification: `:ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:test` green. Extension seams re-verified: Invidious/DiscordRpc `backgroundDispatcher` set to `NzikDispatchers.DATA` at startup (`MainApplication`), NextVisualizer driven through the `snapshotProvider` seam from `NzikDispatchers.VISUALIZER`. Manual on-device checks still to do: botguard PoToken flow, Android Auto session with artworks, like/bookmark menus, active visualizer

fix(player): keep the collapsed mini-player clear of side system bars without a rail
  - `miniPlayerSideInset` (`MiniPlayerTopAnchor.kt`) returned 0 without a navigation rail, so the card went under the side status bar / nav bar / display cutout. It now returns `safeInset - standardPadding` clamped at 0, so the card ends flush with the bar's edge, the same way as with the rail; the rail formula is unchanged
  - `MainActivity.kt`: comment updated to cover the side system bars in addition to the rail
  - `CardGeometryInsetsTest`: the no-rail case is replaced by three cases (a 48dp bar leaves 32dp, a 24dp bar leaves 8dp, a bar thinner than the 16dp margin adds nothing) plus no bar and no rail untouched (class now 8 tests, green)

fix(player): stop swipes and sideways slides acting during the opening (issue https://github.com/N-Zik-Group/N-Zik/issues/818) (issue https://github.com/N-Zik-Group/N-Zik/issues/816)
  - Holding the finger down after a swipe action left the mini-player displaced: once the action ran, the swipe gestures are cut (`SwipeActionLatch.isFired`, now an observable state, drives `SwipeToDismissBox.gesturesEnabled` in `MiniPlayer.kt`, legacy, minimal edit) and a `LaunchedEffect` calls `dismissState.reset()` so the interrupted drag settles the box back to its position
  - Slow diagonal open triggering the swipe action: the swipe's touch slop is doubled (`WithDoubledTouchSlop.kt`, new, same doubling as the song rows) so the vertical open gesture wins, and the swipe is off as soon as the sheet leaves its original position (`isMiniPlayerSwipeEnabled`: `progress == 0f` and no action just ran)
  - Cutting those horizontal gestures left nothing to absorb sideways movement, and the sheet's `detectVerticalDragGestures` grabbed it and opened or closed the player: `CustomBottomSheet.kt` now uses `detectMostlyVerticalDragGestures` (`DominantVerticalDrag.kt`, new) — a mostly horizontal movement is neither consumed nor followed
  - The "mostly vertical" check was one-shot, at the touch slop: stopping mid-opening (finger down) then sliding sideways still moved the sheet through the slide's vertical drift, and the release fling used the vertical velocity. `OngoingDragDirectionMonitor` rechecks the direction continuously, every touch slop of accumulated movement (tumbling window); once it turns horizontal the drag stops following and the sheet settles with a zero velocity on release (`onDragEnd(aborted)` in `CustomBottomSheet.kt`)
  - The player's skip swipes (`Player.kt`, legacy, minimal edit) are off until the sheet is completely open: `disablePlayerHorizontalSwipe` is now derived from the preference AND `isExpanded` (`isPlayerHorizontalSwipeEnabled`), covering the 6 gestures that read it
  - A 45-degree slide still counts as vertical, at the start of the gesture and mid-gesture
  - Tests (44 in 7 classes, all green): DominantVerticalDragTest (6), OngoingDragDirectionMonitorTest (5), MiniPlayerSwipeActionTest (8), SwipeActionLatchTest (6), CustomBottomSheetTranslationTest (8), CustomBottomSheetThresholdTest (6), PlayerSheetStateAnchorTest (5); `:ComposeN-Zik:assembleDebug` OK. Device-verified by the maintainer across the rounds; the mid-gesture recheck itself was not re-verified on a device

fix(player): the mini-player no longer disappears on rotation or after a UI change
  - Root cause 1, `PlayerSheetState.kt` (legacy, minimal edit): the sheet state is rebuilt from its last anchor whenever its bounds change (rotation, insets), but the anchor was only updated by `expand`/`collapse`/`dismiss`, never by `snapTo`, which is what start-up and media transitions use. The rebuilt sheet came back "dismissed" and `CustomBottomSheet` hid the mini-player. `snapTo` and `snapToAndWait` now remember the anchor matching the value (values between two anchors keep the last one)
  - Root cause 2, `MainActivity.kt`: about fifteen UI preferences (nav bar position and type, UI type, mini-player type, ...) call `recreate()`. The player service is not bound yet on the new instance, so the current media reads as absent and the sheet is dismissed, and nothing presented it again once the media came back (`isPlayerInitialized` is saved as true). The media effect is now also keyed on the sheet and calls the new `showMiniplayerIfDismissed()` (`MiniPlayerAutoExpand.kt`) when media is present
  - Tests: PlayerSheetStateAnchorTest (5: anchor remembered for collapsed / expanded / dismissed, kept between anchors, `snapToAndWait`), MiniPlayerRestoreTest (2: a dismissed sheet is brought back collapsed, a showing sheet is left untouched)

fix(player): honour the "top" mini-player position and adapt the layout to it
  - Root cause, `CustomBottomSheet.kt`: the sheet box always pushed itself to the bottom (`translationY = targetY * (1 - p)`), which cancelled the `TopCenter` alignment set in `MainActivity.kt`. The translation is now the pure `sheetTranslationY`, with a new `topPadding` provider (read at draw time) that anchors the mini-player at the top; the bottom behavior is unchanged
  - Placement, new `MiniPlayerTopAnchor.kt`: below the app header (64dp + status bar) with an 8dp gap, and below the top nav bar when it is at the top too (`miniPlayerTopInset`). It leaves with the header on scroll-hide (`miniPlayerTopPaddingPx`), fully above the screen once the header is gone
  - Gestures: mirrored when anchored at the top (`sheetDragDelta`, `sheetFlingVelocity`): swipe down opens the player, swipe up closes it
  - Side rails: with the nav bar on the left or right the collapsed mini-player narrows on that side (`collapsedStartInset` / `collapsedEndInset`, `miniPlayerSideInset`, `computeCardGeometry` now takes per-side insets) and its tap target no longer covers the rail; it still deploys to full width
  - Content: `Skeleton.kt` (legacy) reserves the room under a top mini-player (mini-player height + gaps), shrinking with the scroll-hide (`miniPlayerContentReservePx`) and eased over 250ms when the mini-player appears or leaves, so the list no longer pops
  - Nav bar at the top: it now leaves with the header instead of staying under the status bar (`AppNavigation.kt`, legacy: the content can go up by the bar's height; the scroll-hide travel in `MainActivity.kt` includes it). With a rail on a side the content stays clear of the status bar so the rail buttons remain reachable. `HorizontalNavigationBar.kt` (legacy): no rounded top corners for a top bar
  - Tests (33 in 6 classes): PlayerSheetStateAnchorTest and MiniPlayerRestoreTest above, CustomBottomSheetTranslationTest (8), CardGeometryInsetsTest (6), MiniPlayerTopAnchorTest (8), MiniPlayerContentReserveTest (4)
  - Verification: `:ComposeN-Zik:testDebugUnitTest --tests "app.n_zik.android.components.*"` green (the whole package, no skipped test). Checked on a device by the maintainer while iterating on the behaviors above. NOT re-run after the last edits: the full `testDebugUnitTest` suite and a final `assembleDebug`. NOT covered by a test (no Compose harness in the module): the real sheet rebuild on rotation, the `recreate()` flow, the scroll-hide travel of the nav bars, the eased content reserve. The height of the top nav bar (`TOP_NAV_BAR_HEIGHT`) is derived from `HorizontalNavigationBar`, not measured: it will drift if that bar changes

fix(player): mini-player swipe actions run once per gesture and act on the current song (issue https://github.com/N-Zik-Group/N-Zik/issues/818) (issue https://github.com/N-Zik-Group/N-Zik/issues/816)
  - #818, repeated action: once the finger reaches the end anchor, `AnchoredDraggableState` evaluates the veto of `confirmValueChange` again on every drag delta, and `MiniPlayer.kt` ran the action inside that callback. The new `SwipeActionLatch` lets the action run once per gesture and is rearmed when the swipe box is back at rest (`requireOffset()` back to 0)
  - #816, like on the wrong song: `rememberSwipeToDismissBoxState` keeps the `confirmValueChange` it got at the first composition, so the swipe used the state of that first composition. The right swipe now reads `binder.player.currentMediaItem` when the swipe happens instead of a composition value, and the sheet state, mini-player type, binder and context go through `rememberUpdatedState`
  - Return to position: the action changes the content (new title, like icon), which recomputes the swipe anchors, and the state could settle in a dismissed direction and stay displaced with its gestures off. A `LaunchedEffect` now calls `dismissState.reset()` as soon as `settledValue` is no longer `Settled`
  - Action choice moved to the pure `miniPlayerSwipeAction` (`MiniPlayerSwipeAction.kt`): right swipe likes on the Essential type and goes back on the others, left swipe skips to the next song. The haptic feedback now only fires when an action really runs
  - `MiniPlayer.kt` is a legacy file (minimal edit). `SwipeableContent.kt` (song items) is untouched: it keeps the same deprecated `confirmValueChange` scheme, without the repetition in practice since a song row is never swiped to its far end
  - Tests (9 in 2 classes): SwipeActionLatchTest (5: first call fires, later calls of a gesture do not, 200 calls run the action once, release rearms, release of an unused latch), MiniPlayerSwipeActionTest (4: right swipe on Essential and on the other types, left swipe, no action at rest)
  - Verification: both classes green, and the main sources compile through `:ComposeN-Zik:testDebugUnitTest`. Checked on a device by the maintainer. NOT re-run: the full `testDebugUnitTest` suite and a separate `assembleDebug`. NOT covered by a test (no Compose harness in the module): the drag behavior itself, the reset after the action, the like on the current song. Known detail: holding a swipe from the cover keeps the mini-player displaced until the finger is released; not addressed
