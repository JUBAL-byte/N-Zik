fix(player): auto-launch no longer leaves the app stuck with no nav bars when they were hidden by scroll
  - Root cause: the scroll-hide fling leaves `bottomBarOffset` at 240dp (and `topBarOffset` at -topBarHeight) applied to the bars AND to the player sheet container (MainActivity `.offset`), while re-showing the bars by fling is disabled while `showPlayer` is true - so an auto-expanded player (new track, "keep player minimized" OFF) rendered 240dp too low ("player à moitié") and the bars had no way back: force stop required
  - `MiniPlayerAutoExpand.kt`: `presentMiniplayerThenExpand` gains an optional `onPresent: (() -> Unit)? = null`, invoked synchronously before `snapTo(collapsedBound)`; default callers unchanged
  - `MainActivity.kt`: new local `restoreHiddenBars()` - silent no-op when both offsets are already 0; otherwise cancels the running offset job, sets `isBarsVisible = true`, animates both offsets back to 0 (tween 800ms FastOutSlowInEasing, user-tuned so the bars settle exactly when the auto-expansion starts) and logs a Timber.d trace; passed as `onPresent` at both auto-launch call sites (`launchedFromNotification` + `onMediaItemTransition`)
  - `MainActivity.kt`: scroll-hide guard (`onPreScroll`/`onPostFling`) now reads the sheet's real on-screen state (`isPlayerSheetExpanded`, synced from `playerSheetState.progress > 0.5f` via `snapshotFlow`/`distinctUntilChanged`) instead of the `showPlayer` flag, which stayed true after a back-collapsed player and permanently blocked re-hiding the bars while music plays
  - `MiniPlayerAutoExpandTest.kt`: new test `invokes onPresent exactly once, before the snapTo` (verifyOrder); the existing default-behavior test is untouched
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `MiniPlayerAutoExpandTest` 2/2 green; checked on a device by the maintainer (hidden bars + new track -> bars/mini player back smoothly, full player at correct position, no force stop; scroll re-hide works while the player is collapsed)
  - Spec: _bmad-output/problem-solution-2026-09-19.md (bmad-cis-problem-solving); code review skipped at the maintainer's request

fix(settings): failed backup import now offers a restart instead of leaving the database closed
  - New `GuardedImport.kt` helper `runGuardedImport`: runs the import block, and on failure checks `Database.isClosed` to show `RestartAppDialog`; `onFailure` is wrapped in `runCatching` so its own crash cannot skip the restart prompt
  - `ImportDatabase.kt`: the manual try/catch is replaced by `runGuardedImport`; behavior on success is unchanged
  - `ImportMigration.kt`: the import body (lines 51-151) is wrapped in `runGuardedImport`; previously there was no try/catch at all, so a failed import only logged to the coroutine scope
  - `GuardedImportTest.kt`: 6 unit tests (success, failure with open DB, failure with closed DB, ordering, cancellation rethrown, onFailure that throws)
  - Verification: `:ComposeN-Zik:testDebugUnitTest` GuardedImportTest 6/6, `:ComposeN-Zik:assembleDebug` OK. NOT run on a device: import a corrupted/truncated database backup
refactor(threading): last leftovers moved under NzikDispatchers - Goal G6 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `YouTubeLogin.kt` (legacy, explicit user derogation): the post-login `GlobalScope.launch` (implicit `Dispatchers.Default`, network `accountInfo()`) now runs on `NzikDispatchers.fireAndForget(NzikDispatchers.DATA)`; the dead `@OptIn(DelicateCoroutinesApi)` and its import are removed
  - Main-looper `Handler` timers converted to `fireAndForget(UI)` + `delay`: `RescueScreen` (the delayed `:rescue` process exit; `killProcess` now sits in a `finally` so it still runs if `finishAndRemoveTask()` throws, as the old Handler runnable did), `PoTokenWebView.close()` (teardown posted to Main from the JavaBridge thread), `AudioDeviceMenu` (the 1 s / 2.5 s Bluetooth refreshes and the 30 s poll, cancelled in `onDispose`)
  - `Handler(Looper.getMainLooper())` removed from `AudioDeviceMenu`, `AudioOutputManager` and `PlayerServiceModern`: `registerAudioDeviceCallback` / `registerAudioPlaybackCallback` take `null` for the main looper, same behavior
  - Left on purpose: `AudioVolumeObserver` Handler (legacy `ContentObserver(null)` is not the main looper), `PoTokenWebView` `MainScope()` (= `Main.immediate`, WebView is Main-affine, G4 exclusion), `commonMain` `MusicDatabase` (`getRoomDatabase` has no caller, a common module cannot see `NzikDispatchers`), the module defaults `Dispatchers.IO` of discordrpc and innertube (standalone seams), the G4 exclusions (StreamResolver `runBlocking`, PoToken `Dispatchers.Main`)
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `:ComposeN-Zik:testDebugUnitTest` 515 tests / 100 suites / 0 failures. No new test: Compose / Android-framework code without a JVM harness. NOT run on a device: `:rescue` process exit after "Restore settings", Bluetooth refresh and 30 s poll of the audio output menu, YouTube login, PoToken WebView teardown
  - Code review: Blind Hunter (one-shot route): 7 findings, 3 patches applied, 2 deferred entries in deferred-work.md, the rest rejected

fix(settings): importing a backup or changing the theme no longer sends the user back to Home or crashes on a closed database
  - Root cause 1: a theme/appearance preset, MaterialYou or a settings import writes preference keys that make `MainActivity` call `recreate()`. `startApp()` then re-read the original launch intent (`ACTION_MAIN`) into `shortcutIntentAction`, and the shortcut effect (`popBackStack(home)`) took it for a pending launcher shortcut, dropping the restored Settings screen
  - `HomeScreen.kt`: new `initialShortcutAction(action, isRestoredInstance)`. `MainActivity.onCreate` computes `isRestoredInstance = savedInstanceState != null` and passes it to `startApp(isRestoredInstance)`, so a recreated or restored activity never re-applies the launch intent. `onNewIntent` is unchanged: a shortcut tapped while the app is running still works
  - Root cause 2 (exposed by the fix above, the recreate used to hide it by landing on Home): `ImportDatabase` / `ImportMigration` call `Database.close()`; in "Both" mode the settings import then recreated the activity and Data settings queried the closed database in a `remember {}` on Main (`IllegalStateException: Database is closed`)
  - `Database.kt`: `isClosed` flag set by `close()` (no schema change). `ActivityRecreation.kt`: `shouldRecreateActivity(isDatabaseClosed)`. `MainActivity` skips `recreate()` when the database is closed, in the preference listener and in `onMonetColorsChanged`
  - Tests: InitialShortcutActionTest (3), ActivityRecreationTest (2)
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `testDebugUnitTest` on the `home` package + ActivityRecreationTest: 20 tests, 0 failures; checked on a device by the maintainer (backup import and theme change)
  - Code review: bmad-build oneshot, Blind Hunter only, run twice: 12 findings, 2 comment/log patches applied, 2 deferred entries in deferred-work.md (launch-intent URI replayed after a recreation; the process keeps running with a closed database after a failed import), the rest rejected

perf(threading): asynchronous, lossless FileLoggingTree - Goal M14 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - The legacy `FileLoggingTree` opened, wrote, flushed and closed the log file for EVERY line and checked/copied the file size, all on the thread that logs: disk I/O on Main whenever a log came from Main. Its `SimpleDateFormat` was shared between threads (not thread-safe) and a line logged just before a crash could be lost
  - New `app.n_zik.android.utils.logging.FileLoggingTree` (legacy file deleted, explicit user derogation): `log()` only queues an entry in an unbounded channel, so it never blocks and never drops a line (user requirement: nothing lost). A single consumer on `NzikDispatchers.DATA` (scope from `NzikDispatchers.fireAndForget`) formats and writes by batches, opening the file in append mode and closing it every batch (Rescue and the "debug log" setting delete it from outside), then applies the unchanged size rule (5 MB, keep the last 75 %). Same file, same line format, same DEBUG-and-above filter, still a `Timber.DebugTree`
  - A batch that cannot be written stays in memory and is retried in order; only a write failure that persists beyond 8 MiB waiting drops the oldest waiting lines. The writer never calls Timber (it would loop into itself). `close()` drains the queue and makes one last attempt on retained lines
  - Crash: `MainApplication` installs `flushThenDelegate(tree, 2000, CaptureCrash)` when the debug log is enabled, so the queued lines are written (wait bounded to 2 s) before `CaptureCrash` writes the crash log; `CaptureCrash` itself is unchanged
  - Tests: FileLoggingTreeTest (23: order, off the caller thread, non-blocking `log()` with a stuck consumer, priority filter, size rule, burst without loss, concurrent loggers, write failure then recovery, pending cap, flush ok/timeout, close, crash handler flush-before-delegate and timeout, file deleted between two batches)
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `:ComposeN-Zik:testDebugUnitTest` 515 tests / 100 suites / 0 failures. NOT run on a device: log export with the debug log on, a test crash
  - Code review: bmad-build 3-layer pass (Blind Hunter, Edge Case Hunter, Verification Gap): 15 findings, 4 patches applied (last write on close, close the queue if the consumer dies, crash handler extracted and tested, file-deleted test), 4 deferred entries in deferred-work.md (partial write on disk full duplicates lines, retry only on the next log call, non-atomic size trim, fragile timing tests), the rest rejected. Still possible: a hard process kill or native crash can lose the lines still queued; the 5 MB rule still drops the oldest lines

fix(lyrics): the "Theme" lyrics color now uses the theme accent, like the visualizer
  - Root cause: the visualizer's Theme color is always the theme accent (`NextVisualizer.kt`), but the lyrics used `lyricsThemeColor(palette)` = `palette.text` (white on dark themes) and only fell back to the accent for achromatic themes, so the lyrics stayed white on any colored theme, in every mode, with or without the cover. An earlier attempt (legacy `PureBlackColorPalette.text` special case removed, achromatic case added) did not address it
  - CoverPaletteExtractor.kt: `lyricsThemeColor(palette, onAccentBackground = false)` returns `palette.accent` in every case, the single source for the karaoke, synced and unsynced views; the now-unused `themePaletteIsAchromatic` is removed
  - Exception: when the lyrics background is itself painted with the accent (`showBackgroundLyrics` with the cover shown) the text keeps the theme text so it stays readable; `LyricsTextPainter` gets `textOnAccentBackground` (default false) and `SyncedLyricsView` / `KaraokeLyricsView` pass `showBackgroundLyrics && showlyricsthumbnail`
  - CoverPaletteExtractorAchromaticTest: the old test that froze the theme text for colored themes is replaced by three (accent for achromatic themes, accent and not text for colored themes in dark and light, text on an accent background)
  - Verification: `:ComposeN-Zik:test` 487 tests / 0 failures and `assembleDebug` OK; checked in the app by the maintainer. No code review pass (skipped at the maintainer's request)
  - Committed together with the uncommitted cover-tone work of the previous session (achromatic covers pick their tone from the cover lightness, same files)

chore(cleanup): legacy residue orphaned by the Goal G3 deletions (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - Removed (explicit user derogation, pure deletion): `getVersionCode()` in Utils.kt (its only caller was `CheckAvailableNewVersion`, removed in G3), the `loadedDataKey` constant in Preferences.kt (the key is no longer read or written anywhere), the `LocalUriHandler` import in Dialog.kt (the only import made unused by removing `NewVersionDialog`), and the 4 update-dialog strings of `values/strings.xml` (`actions_you_can_do`, `open_the_github_releases_web_page_and_download_latest_version`, `download_latest_version_from_github_you_will_find_the_file_in_the_notification_area_and_you_can_install_by_clicking_on_it`, `f_droid_users_can_wait_for_the_update_info`); the `values-*` files are left to Crowdin
  - Kept on purpose: drawables `globe` and `download` (used in 10+ files), `app_update_dialog_version` and `update_available` (used by the current updater), `getLongVersionCode()` (no caller, but not caused by G3), the other unused imports of Dialog.kt and Utils.kt (already unused before G3). `RiMusicUpdatedVersionCode.ver` is referenced nowhere, nothing to do
  - Verification: grep finds no remaining reference to the removed symbols (main, test, xml). NOT run for this change: `assembleDebug` and the unit suite (last full run before it: 486 tests, 0 failures). No code review pass (skipped at the maintainer's request)

refactor(threading): fire-and-forget scope hardening & dispatcher consolidation - Goal G5 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - New `NzikDispatchers.fireAndForget()` helper (SupervisorJob + Timber exception handler) in 2 overloads: `fireAndForget(dispatcher)` and `fireAndForget(context)` (a caller-provided Job stays the cancellation root; a SupervisorJob is only added when the context carries none)
  - 105 bare `CoroutineScope(...)` sites in androidMain converted (62 files) - every site keeps its exact dispatcher; scopes that were never cancelled stay never-cancelled, including the 3 screen-level field scopes (`GoToLink`, `ExportCacheDialog`, `QuickPicksRepository`)
  - 3 `Handler(mainLooper)` sites converted to coroutines: `MainActivity` startup toasts (`lifecycleScope` + `delay`, cancellation on destroy accepted by the frozen spec), `VoiceSearchUtils` (recognizer callbacks already run on Main - the 10 `.post` calls became direct invocations), `InvincibleService` 30 s keep-alive tick (`while(isActive){tick(); delay(30_000)}` with a `runCatching` guard, SCREEN_ON cancel+relaunch, `stop()`/`onDestroy` cancel the tick only so the scope stays reusable like the old Handler); the 5 framework-mandatory Handler sites kept (documented)
  - Dead code removed: `NetworkClientFactory.validateStreamUrl()`, `AudioUtils.audioFadeOut`/`audioFadeIn`, the 15 listed dead imports plus ~60 pre-existing dead imports in touched files
  - discordrpc fork (explicit human derogation "Tout (y compris discordrpc)"): new `DiscordRpc.backgroundDispatcher` seam (`@Volatile var`, standalone default `Dispatchers.IO`), `GatewayWebSocket` (was `Dispatchers.Default`) and `DiscordRpcConnection.httpScope` (was `Dispatchers.IO`) routed through it, the app wires `NzikDispatchers.DATA` once in `MainApplication.onCreate` (Invidious pattern); committed inside the `modules/discordrpc` submodule first, then the parent gitlink
  - Tests: `NzikDispatchersFireAndForgetTest` (6), `InvincibleServiceTest` (4 - Robolectric + virtual time + `mockkStatic(ServiceCompat)`), `VoiceSearchUtilsTest` (10 - Robolectric + MockK, synchronous-delivery assertions), `DiscordRpcSeamTest` (2 - app-side mirror so `:ComposeN-Zik:test` covers the seam) + module `DiscordRpcBackgroundDispatcherTest` (2)
  - Quality: 3 review layers (24 findings: 10 patched and re-verified, 3 deferred in deferred-work.md, 9 rejected) + full diff re-read by the lead
  - Verification: `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (484 app tests / 0 failures, 3 module tests / 0 failures); acceptance greps at 0 (bare `CoroutineScope(`, `validateStreamUrl`, dead imports, `Handler`/`Looper` in the converted files)

fix(colors): near-achromatic covers render a neutral gray following the cover instead of a brown/blue/pink tint
  - Root cause (CIS session _bmad-output/problem-solution-2026-09-19.md, probe-verified): on nearly achromatic bitmaps the androidx.palette vibrant role fails, so every `get*Color(fallback)` in the M3E extraction returns the legacy dynamic accent (a mid-tone L≈0.5 carrying the cover's faint hue), and the dark-theme boost of `m3eSaturate` (+0.35 saturation when S ≥ 0.1) amplifies that faint hue into the reported light brown/blue/pink tints on every cover-derived surface (repro: The Weeknd - "Angels"; the "not on all" part of the report = the S ≥ 0.1 gate)
  - `CoverPaletteExtractor.kt`: new `ACHROMATIC_CHANNEL_DELTA_THRESHOLD = 0.10f`, `channelDelta(rgb)` (normalized max-min R/G/B channel spread) and the pure guard `M3ECoverColors.m3eNeutralizeIfAchromatic(swatchPalette, fallbackArgb)` applied in `extractM3ECoverColors`: when the max swatch channel spread is below the threshold, all 7 swatches are returned as neutral grays (S=0, each swatch's own lightness kept; a swatch that fell back to the legacy accent takes the dominant's lightness). Channel spread is used instead of HSV saturation, which is lightness-dependent (probe: off-white rgb(235,233,228) reads S≈0.148 but Δ≈0.03 - an S < 0.1 gate would have missed exactly the reported "light brown on white covers" case; user decision 2026-09-19); covers with Δ ≥ 0.10 are extracted bit-identically to before (strict M3E parity)
  - `Player.kt` (legacy, minimal in-place patch): `computePlayerDynamicPalette` runs its 7 raw swatches through the same shared `m3eNeutralizeIfAchromatic` and builds the local vibrant-based dynamic palette from the neutralized vibrant swatch, so the player background, animated gradients, `ColorPalette` stripes and mesh stay consistent with lyrics/visualizer/mini-player/theme (this pipeline duplicates the extraction and does not call `extractM3ECoverColors`)
  - Spec renegotiated (spec-cover-color-m3e-morphing-alignment.md): the frozen `NEUTRAL_COVER` case moves from "vibrant swatch used (M3E behavior)" to "neutral gray following the cover"; the deviation from "strictly identical to M3E" is documented in the spec Intent/Always/Never/acceptance (M3E was undefined on achromatic, so there was no M3E behavior to preserve)
  - New test `CoverPaletteExtractorAchromaticTest` (Robolectric, 6: channelDelta lightness-independence + threshold margins; achromatic neutralization with cover-following lightness on the probe's fixture colors; per-swatch lightness kept on a two-region bitmap; colored/pastel bit-identical parity; guard no-op above the threshold; player-vs-extractor achromatic parity)
  - Verification: `:ComposeN-Zik:assembleDebug` OK; on-device check by the user OK; full color test suite green. Robolectric-only artifact understood and worked around: the androidx.palette K-means quantizer returns an empty palette (0 swatches, `dominantSwatch` null) for specific near-gray solids under Robolectric (probe: 7 of 13 warm grays around rgb(168,161,152), deterministic, wide irregular dead zone; it also slightly shifts the other fixture colors) - no such behavior on real devices, and the null is already handled by the callers' pre-alignment fallback (`coverSwatchesLoaded` -> `background1/2`). The test fixture was swapped from rgb(168,161,152) to the Robolectric-stable rgb(168,159,152) (delta still below the threshold after quantization)

fix(colors): align every cover-derived color to the M3EMorphingCover extraction (vibrant-based)
  - New shared extractor `CoverPaletteExtractor.kt` (`app.n_zik.android.components.player`): `extractM3ECoverColors` (full `Palette`, no `maximumColorCount`/filter, the 7 raw swatches with the same fallback accent as the player), `m3eSaturate`/`m3eDarkenBy` (pure copies of `Player.saturate()`/`Color.darkenBy()`), `m3eDynamicColorPaletteOf` (vibrant swatch HSL -> the existing capped `dynamicColorPaletteOf(hsl, isDark)`), plus named, tested helpers `m3eCoverBackgroundColor` and `m3eCoverForegroundArgb`
  - `Player.kt`: `computePlayerDynamicPalette` now builds the local dynamic palette from the vibrant swatch's HSL instead of the dominant swatch's, so the 4 animated-gradient bases, the player "cover" controls and the `ColorPalette` stripes pick up the cover's vibrant hue; the flat "Match song cover" background uses `m3eCoverBackgroundColor(vibrant)` - the exact expression of the M3EMorphingCover shapes' V parameter (human-verified: the shapes are only drawn with V/DV/M)
  - `Player.kt`: new `coverSwatchesLoaded` flag - until the swatches load, and after an artwork failure (the `catch` path), `CoverColor`/`CoverColorGradient` fall back to `dynamicColorPalette.background1/2` (the pre-alignment behavior) instead of rendering black from the zero-initialized swatches
  - `LyricsScreen.kt` ("cover" lyrics color) and `NextVisualizer.kt` ("cover" visualizer option): `m3eCoverForegroundArgb(vibrant)` instead of the dominant-based `palette.accent`
  - `MiniPlayer.kt` (`computeMiniPlayerPalette`) and `MainActivity.kt` (`setDynamicPalette`, app-wide dynamic theme): vibrant-based `m3eDynamicColorPaletteOf`; the dead 8-color/hue-filtered local `palette` in `setDynamicPalette` and its unused `Palette` import removed
  - Review pass (3 independent layers, 14 findings): 8 patched - the fallback flag above, the player's swatch fallback accent realigned to the extractor so both paths return identical swatches, 2 `Palette.generate()` per player track instead of 3, the vibrant-not-dominant hue source pinned by a new two-region bitmap fixture (solid-bitmap tests could not distinguish the hue sources), KDoc/comment fixes including a false "Palette default filter" claim; 4 deferred in deferred-work.md, 2 rejected
  - Tests: `CoverPaletteExtractorTest` (11, incl. the two-region dominant!=vibrant fixtures and the flat-background contract), `PlayerDynamicPaletteOffMainTest` (2, extractor parity), `MiniPlayerPaletteOffMainTest` (2) - 15/15 green; `assembleDebug` OK. NOT run on a device: player/mini-player "cover" backgrounds, animated gradients, lyrics/visualizer "cover" color, app-wide theme on track change
  - Known open (reported by the user, to be investigated in a dedicated problem-solving workflow): gray/white covers can yield a faint brown/blue/pink tint - likely the +0.35 saturation boost amplifying a tiny hue cast of the vibrant swatch; intrinsic to the "identical to M3E" rendering frozen in the spec

refactor(threading): raw dispatchers replaced by NzikDispatchers across androidMain - Goal G4 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - 135 files under ComposeN-Zik/src/androidMain/kotlin/ migrated symbol by symbol: `Dispatchers.IO` -> `NzikDispatchers.DATA`, `Dispatchers.Main` -> `NzikDispatchers.UI`, `Dispatchers.Default` -> `NzikDispatchers.MEDIA` (qualified `kotlinx.coroutines.*` forms included); raw imports removed when left unused
  - Human-approved decision (round-1 review loopback): keep the `Default -> MEDIA` mapping; the 3 Default sites (`forcePlayAtIndex` in `utils/Player.kt:136`, `LocalPlaylistSongs.kt:1089`, `NextVisualizer.kt:180`) now run on the shared 2-thread `nzik-media` pool instead of the isolated `Dispatchers.Default` pool
  - New test `ForcePlayAtIndexDispatchTest.kt` (3: body pinned to the `nzik-media-*` pool, playback start on Main via `Dispatchers.setMain`, body off the calling thread) pins the behavioral change; `NzikDispatchers.kt` KDoc now lists the MEDIA tenants
  - Approved exclusions kept as-is: 2 `runBlocking(Dispatchers.IO)` in `StreamResolver.kt` (ExoPlayer synchronous `ResolvingDataSource` API), 7 `Dispatchers.Main` sites in `PoTokenGenerator.kt`/`PoTokenWebView.kt` (WebView Main affinity), `SessionMediaItemMapper.kt` `runBlocking{}`, 3 `runBlocking{}` in legacy `utils/Player.kt` (G2), `GoToLink.kt` KDoc mention
  - Quality: 2 review rounds (3 independent layers each) + independent read-only audit - 4 minor findings auto-fixed (test KDoc wording, dead commented line in `HomeDiscovery.kt`, misleading no-op dispatcher comment in `LocalPlaylistSongs.kt`), 1 pre-existing family deferred to deferred-work.md (fire-and-forget unstructured `CoroutineScope` bodies) + audit leftovers (14 pre-existing dead imports, 11 pre-existing `Handler(mainLooper)` sites, dead `NetworkClientFactory.validateStreamUrl`)
  - Verification: `assembleDebug` + `testDebugUnitTest` BUILD SUCCESSFUL (443 tests / 0 failures), acceptance greps at 0 (remaining raw usages = approved exclusions only), full diff re-read line by line in both rounds
perf(threading): remaining Main-thread blockers in legacy screens - Goal G2 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - 10 legacy `Player.addNext/enqueue(list, context)` sites (kreate `ArtistDetails` x2, `AlbumDetails` x2, `LocalPlaylistSongs` x2, `ArtistLocalSongs` x2, `Podcast`, `PlaylistSongList`) ran `excludeMediaItems` (3 `runBlocking` DB reads) and `Song.asMediaItem` (synchronous bitmap decode) on Main in the click. The `getSongs().toList()` snapshot stays on Main, the filter + mapping now run in `withContext(NzikDispatchers.DATA)`, and the final `player.*` mutation stays on Main with `context = null` (no double filtering); the selection reset (`itemSelector.isActive = false`) is still synchronous at click time
  - `Dialog.kt` (song re-match): the `Innertube.searchPage` network call in a `runBlocking(Dispatchers.IO)` inside the `LaunchedEffect` is now `fetchSongMatchResults` (internal suspend fun extracted in place - AD-5 respected, no new legacy file); the search runs on `NzikDispatchers.DATA`, `songsList` / `startSearch` are written on Main; the `runBlocking` import is removed
  - `DataSettings.kt` (3 sites): cache clearing (`SimpleCache.removeResource` per key, `waveforms` `deleteRecursively`) and history deletion run in `withContext(NzikDispatchers.DATA)`; the dialog closes immediately at confirm, `refreshSignal.tryEmit` / `cacheCleanedCounter++` / `Toaster.done()` follow on Main
  - `OtherSettings.kt`: the blacklist `writeText` (add/remove) runs on `NzikDispatchers.DATA` with the list snapshotted at click time; state updates stay on Main
  - Code review pass (bmad-code-review, 4 layers, 16 findings): 1 decision + 1 patch resolved the same day - the player is now re-read from `binder` after the DATA block (null-guarded) at the 10 queue sites, closing the window where the playback service is destroyed during the filter (released ExoPlayer -> uncaught `IllegalStateException` crash in the unstructured coroutine); the re-match search is pinned by a new JVM test (`SongMatchingDialogSearchOffMainTest`, `legacyoffmain` pattern: mocked `Innertube.searchPage`, `Dispatchers.setMain` Main stand-in, thread assertion). 2 items deferred in deferred-work.md (pre-existing double search in `SongMatchingDialog`, orphan `getMediaItems()` in kreate `ArtistDetails`), 12 rejected
  - Ledger (deferred-work.md) fixed: 2 false positives removed from G2 (`Queue.kt:488`, `LocalPlaylistSongs.kt:1448` - single-item `enqueue(mediaItem, context)` reads prefs only, no DB)
  - Explicit user legacy derogation (2026-09-19) limited to the spec Code Map; only new legacy symbol is `NzikDispatchers` (AD-5: no `addNextOffMain`/`enqueueOffMain` in legacy)
  - New test: `SongMatchingDialogSearchOffMainTest` (3: items on success, empty list on failure, search off the simulated Main). The 10 queue sites share the contract already pinned by `PlayerQueueOffMainTest`; pinning the inline composable copies would need a Compose harness absent from the module - limitation documented in the spec
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `:ComposeN-Zik:testDebugUnitTest` 442 tests / 0 failures, `grep runBlocking` on `Dialog.kt` = 0. NOT run on a device: play-next / enqueue from album / artist / local playlist / podcast / online playlist with and without a selection, the Settings > Data cache clears, the Settings > Other blacklist round trip, the re-match dialog search
fix(lyrics): LrcLib pick also available in Unsynced mode, unusable tracks filtered, no more crash on a null track duration (issue https://github.com/N-Zik-Group/N-Zik/issues/765)
  - LyricsSettingsMenu.kt: "Pick from LrcLib.net" is now offered in Unsynced mode as well as Synced (both menu styles)
  - LyricsTrackSelector.kt: `pickedLyrics(mediaId, track, lyricsType)` stores the pick under the type of the mode it was made in (Synced: the synced text; Unsynced: the plain text, else the synced text without its timestamps), flagged `isEdited`. The track list only offers tracks usable in that mode (`canPick`), so a plain-only track no longer writes an Unsynced row that Synced mode never displays, and a track with no text no longer overwrites the stored lyrics with an empty row
  - LyricsFetcher.kt / LyricsScreen.kt: new `fetchAgainRowType` - "Fetch lyrics again" in Auto with nothing displayed no longer writes a row of type "Auto"
  - Track.kt (extensions/lrclib): `duration` threw `NumberFormatException: For input string: "null"` when LrcLib returns `"duration": null` (seen for "Attention" by Charlie Puth). It killed the app when the pick list was drawn and could also break the automatic best-match lookup. A null or unreadable duration now counts as 0
  - Checked in the app on an emulator (Android 17): entry and list in Unsynced mode; a pick stored with `isEdited = 1` and kept after a song change with the lyrics view open; "Fetch lyrics again" resets it; a Synced pick stored on the track that used to crash. Not checked: the content of the filtered list in Synced mode, and an upgrade from a real v39 database
  - New tests: LyricsTrackSelectorTest (5), FetchAgainRowTypeTest (3), TrackDurationTest (4)
  - Full unit suite: ComposeN-Zik 439 tests, 0 failures; `assembleDebug` OK

fix(rescue): second code review pass - busy database message, settings backup warning, reliable process exit, checkpoint tests (issue https://github.com/N-Zik-Group/N-Zik/issues/811)
  - Database export: when the WAL cannot be merged because another connection (the main app process) holds the database, `checkpointWal` now throws `RescueFiles.DatabaseBusyException` and the screen shows `rescue_error_database_busy` (close the app, Force stop) instead of a raw English exception message. The export still fails explicitly rather than copying `data.db` alone, which after a crash is a database with no table (reproduced)
  - "Restore settings": the `:rescue` process is now ended by a `Handler` on the main looper (it was `delay` + kill in the composition scope, cancelled when the user left the screen or the activity was recreated, which left stale in-memory SharedPreferences), and `exitPending` blocks every other write during the wait, so a reset or an import cannot commit the stale map over the restored files. The confirmation text says the Rescue Center will close
  - "Reset settings" run twice overwrote the only backup with already-cleared settings: the confirmation now warns when a settings backup exists (`rescue_confirm_reset_settings_replace_backup`), same rule as the database reset
  - `importSettings`: a CSV holding only credential rows with the keystore unavailable now returns `SettingsOutcome(encryptedSkipped = true)` (the warning) instead of a generic error, and the `commit()` results are checked, so a failed write is no longer reported as a success
  - `importDatabase` deletes the temporary copy `data.db.import` and its `-wal`/`-shm` before the copy and in the `finally` (validating a WAL-mode file makes SQLite create them); `deleteWithSideFiles` added
  - KDoc of `moveDatabaseFiles`, `swapDatabaseFiles` and `replaceDatabaseFile` reworded to the real guarantee: renames happen one after the other, a failure part-way can leave a half-moved set (no rollback, tracked in deferred-work.md)
  - Accessibility: a disabled action card exposes its reason as `stateDescription` (TalkBack), not only through the toast shown on tap
  - Tests: new `RescueFilesCheckpointTest` (Robolectric, native SQLite): the WAL is folded into `data.db` so a bare copy holds every row, a database in use is refused with `DatabaseBusyException`, a corrupt database is never deleted or modified. Checked by mutation: without `keepCorruptDatabase` the corrupt-database test fails (Android's default handler deletes the file), and with `OPEN_READONLY` the WAL and busy tests fail with `SQLiteDiskIOException`. `ShortcutIconSyncTest` now covers `loadPrefs` (cap, wrong-typed values) and `resolveActiveShortcutIds(context)` with a MockK `SharedPreferences`; the tautological parity test is replaced
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `:ComposeN-Zik:testDebugUnitTest` 435 tests / 88 suites / 0 failures (the total includes tests of unrelated work in progress). NOT run on a device: the `:rescue` process ending after "Restore settings", the Material3 dialog buttons. Android lint could not run: the working tree did not compile at that moment
  - Code review: second bmad-code-review pass on `143a095231` (4 layers): 1 decision (export when the WAL cannot be merged: option a, keep the explicit failure), 8 patches, 2 deferred in deferred-work.md (rollback of multi-file moves and swaps on an I/O failure, Context-bound actions without tests), 14 rejected (including the orphan translation keys, which Crowdin cleans)
perf(threading): last Main-thread blockers and a thread leak in new code, dead code removed - Goal G3 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - DeleteSongDialog.kt: deleting a local song ran a MediaStore query, `File.delete` and `contentResolver.delete` on Main inside the confirm click. The file part now runs on `NzikDispatchers.DATA` (`deleteSongFiles`, no-op for non-local songs; body extracted as `deleteLocalSongFile`) and the "deleted" Toast follows it. The DB transaction and `onDismiss()` are unchanged
  - PlayerServiceModern.kt: `Executors.newScheduledThreadPool(1)` created in `onCreate` was never shut down (no reference kept), so the periodic queue save survived `onDestroy` and each service re-creation added another pool. Replaced by `runPeriodically(30_000)` (new `utils/coroutines/PeriodicRunner.kt`: first run immediately, then a fixed delay, an exception in the action is logged and does not stop the loop) launched in the service `coroutineScope`, which `onDestroy` already cancels
  - MainActivity.kt: removed `preferences.edit(commit = true) { putBoolean(loadedDataKey, false) }` from `startApp()`, a synchronous disk write on Main at every start. Nothing reads that key (the `loadedData` of QuickPicks is an in-memory state). The `commit()` of the crash-log path is kept on purpose: the process is killed by `exitProcess(0)` 5 s later
  - Dead code removed. New code: `maybeRestoreFromDiskPlayerQueue` / `maybeSaveToDiskPlayerQueue` in PlayerServiceModern (0 callers) and their imports. Legacy (explicit user derogation, pure deletion only): `CheckAvailableNewVersion` (Utils.kt), `NewVersionDialog` (Dialog.kt), `models/PersistentQueue.kt`, the whole `extensions/webpotoken/` package (5 files, replaced by `core/youtube/security/potoken`) and `res/raw/po_token.html`
  - Accepted, not changed: `NextVisualizer.kt:173-174` initial value (`APP_ICON_BITMAP` + `getCircledBitmap`) is still computed on Main; the icon is already decoded on Main through `bitmapCover`, and a non-circular placeholder would flash a square on the first frame
  - New tests: DeleteSongFilesTest (2: local song deleted off the caller thread, non-local song never touches files), PeriodicRunnerTest (3: immediate first run then one per interval, cancellation stops it, a failing action does not kill the loop)
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `:ComposeN-Zik:testDebugUnitTest` 430 tests / 0 failures. NOT run on a device: deleting a local song, queue restore after the service is killed, no leftover `pool-*` thread
  - Code review: bmad-build 3-layer pass (Blind Hunter, Edge Case Hunter, Verification Gap): 26 findings, 0 patches, 4 deferred entries in deferred-work.md (final queue save on `onDestroy` likely lost and persistence toggle read once - pre-existing; legacy leftovers orphaned by the deletion such as `getVersionCode()`, `loadedDataKey`, update-dialog strings in ~50 locales; no test on `onConfirm` and the service wiring; the `Shuffler` test leaks an exception that breaks later `runTest` tests), the rest rejected

fix(lyrics): keep a manually picked LrcLib track from being replaced by the automatic fetch (issue https://github.com/N-Zik-Group/N-Zik/issues/765)
  - LyricsTrackSelector.kt: the pick handler wrote a plain `Lyrics` row (no `isEdited`, no `lastFetchedAt`). With the refetch TTL a null stamp counts as expired and non-timed rows are hunted on revisits, so a deliberate pick could be replaced by the default match the user had just rejected - the #765 symptom for a manual choice. The row is now built by the new `pickedLyrics(mediaId, track)` and saved with `isEdited = true`
  - "Fetch lyrics again" still hands the song back to the automatic fetch: it rewrites the displayed row with `data = null` and `isEdited = false`. That is the picked row whenever both share the same type (the pick type comes from the track content, not from the display)
  - The "Pick from LrcLib" entry is only offered in Synced mode
  - Lyrics.kt: KDoc of `isEdited` now covers both a typed edit and a LrcLib pick
  - Deferred (deferred-work.md, all pre-existing): a track without synced lyrics writes an Unsynced row that Synced mode never displays; picking a track with no text overwrites the stored lyrics of that type with an empty row; "Fetch lyrics again" in Auto with nothing displayed writes a row of type "Auto"
  - New test: LyricsTrackSelectorTest (2: synced track -> Synced row, plain-only track -> Unsynced row, both flagged `isEdited`)
  - Full unit suite: ComposeN-Zik 419 tests, 0 failures; `assembleDebug` OK

fix(rescue): harden the database, settings and shortcut operations after code review (issue https://github.com/N-Zik-Group/N-Zik/issues/811)
  - Database export failed on a WAL database: `exportDatabase` opened `data.db` `OPEN_READONLY` then ran `wal_checkpoint(TRUNCATE)`, which a read-only connection cannot do (reproduced with SQLite: "disk I/O error"), and a bare copy of `data.db` next to a `-wal` left by a crash contains no table. It now opens read-write, checks the checkpoint busy flag and fails explicitly instead of exporting an incomplete file
  - A corrupt `data.db` could be deleted by the rescue itself: `SQLiteDatabase.openDatabase` without a `DatabaseErrorHandler` uses `DefaultDatabaseErrorHandler`, which deletes the file when SQLite reports corruption at open time (AOSP `SQLiteDatabase.open()` -> `onCorruption()`). The export checkpoint and the import validation now pass a no-op handler (`keepCorruptDatabase`)
  - `resetDatabase` / `restoreDatabase` no longer open SQLite nor copy: `moveDatabaseFiles` renames `data.db` + `-wal` + `-shm` (+ `-journal`) together, so a failed checkpoint cannot lose recent data, stale side files of the previous backup are removed, and a failed delete is an error. `swapDatabaseFiles` parks the live database first and rolls back if bringing the backup in fails (before, a failed copy could leave a truncated `data.db` and the `finally` deleted the only good copy)
  - `importDatabase` copies to `data.db.import` next to `data.db`, validates it, then `replaceDatabaseFile` removes the stale `-wal`/`-shm` and renames it into place, so a failure while copying can never leave a truncated database
  - Settings: `backupSettingsFiles` / `swapSettingsFiles` (File-based) replace the copy-based swap. The XML files are swapped behind the SharedPreferences instances the `:rescue` process holds in memory (a later `commit()` would write the stale map over the restored file), so the screen ends the `:rescue` process 1.5 s after a successful "Restore settings"
  - `applySettingRows` (extracted from `importSettings`) routes credential keys to the encrypted editor and never writes them in clear when the keystore is unavailable; the import now fails when no setting could be imported. The failure log prints the exception class only: `NumberFormatException` quotes its input, so the previous "no longer logs the value" fix (`64b64320f9`) still leaked a credential value in debug builds
  - Shortcuts: `parseShortcutConfig` / `resolveActiveShortcutIds(order, enabled)` are the single source of truth for the launcher registration and `AppShortcutsSettingsDialog` (they had diverged). Whatever is stored (blank, unknown or duplicate ids, an order missing `rescue`), Rescue stays registered, ids are unique and at most 4 are active: the other shortcuts give way, never Rescue. `shortcutTargetClass` pins Rescue to `RescueActivity`; `registerAppShortcuts` logs when `setDynamicShortcuts` is rate-limited instead of reporting success; dead `SHORTCUT_IDS` and the unused `registerAppShortcuts` import in `MainActivity` removed
  - `RescueScreen`: the encrypted preferences are opened lazily on `Dispatchers.IO` (`async` LAZY) only when an action awaits them (Keystore work no longer runs in composition); dialog buttons are Material3 `Button`/`TextButton` through `RescueDialogButtons` (button role, 48 dp target); disabled cards are announced as disabled; a second reset warns that it replaces the previous backup (`rescue_confirm_reset_database_replace_backup`); "Delete backups" moved to the Danger Zone; the "main app running" message now says how to force stop the app
  - `RescueActivity`: the theme is resolved once in `remember` and wrapped in `runCatching` with a static Material3 fallback, so corrupt preferences (e.g. `getInt` on a value of another type) cannot crash the recovery screen; inline fully qualified names replaced by imports
  - strings.xml (values/ only): fixed misleading texts ("cannot be undone" for a settings reset that keeps a backup, "from cache" for `filesDir/rescue_backups`), `app_shortcuts_max_reached` takes the maximum as an argument, removed the unused `rescue_invalid_database` and `rescue_keystore_warning`
  - Tests: `RescueFilesTest` 31 (database move / swap / replace incl. side files, settings backup and swap, backups deletion, credential routing with MockK, unknown types), `ShortcutIconSyncTest` 22 (resolution invariants, dialog / registration parity, Rescue opens `RescueActivity`)
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `:ComposeN-Zik:testDebugUnitTest` 419 tests / 84 suites / 0 failures. NOT run on a device: database export on a crash-style WAL, behavior on a corrupt database, the `:rescue` process ending after "Restore settings", the new Material3 dialog buttons
  - Code review: bmad-code-review 4-layer pass (Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor): 2 decisions (backup slot policy, main-process guard message), 15 patches, 5 deferred in deferred-work.md (schema compatibility check on DB import, settings import type checks, `singleTask` without its own `taskAffinity`, shortcut-only entry point, no test on the Application.onCreate registration order), 16 rejected
fix(lyrics): review fixes for the refetch TTL - no needless BetterLyrics refetch of fresh word-timed rows, view-safe v40->41 migration (follow-up to `034a08e88d`)
  - From40To41Migration.kt: the `SortedSongPlaylistMap` view reads `SongPlaylistMap`. Since SQLite 3.26, `DROP TABLE` + `ALTER TABLE ... RENAME` fails with "error in view ... no such table" while the view points at the dropped table, and `fallbackToDestructiveMigration()` would then wipe the whole database. The view is now dropped before the six rebuilds and recreated afterwards with the exact definition of `schemas/.../41.json`
  - LyricsFetchWorker.kt (`handleEmission`): BetterLyrics ran whenever ANY of the three checks was needed and rewrote and re-stamped the Karaoke row, so a fresh word-timed row was re-fetched on every visit (and could be downgraded to plain lines) as soon as a Synced sibling was missing. It now only runs when `needKaraokeFetch` is true (Synced mode keeps it as the first source of its Synced check, where it never writes a Karaoke row); otherwise only the needed Synced check (LrcLib/KuGou chain) or the Unsynced-only chain (LrcLib -> YouTube) runs
  - Known limitation, left as is by maintainer decision: when BetterLyrics runs and succeeds, the needed Synced/Unsynced checks are still not executed in the same pass
  - Not verified on a device: Robolectric runs an older SQLite, so the original view failure could only be reproduced on stock SQLite 3.50.4; the migration must still be tried on Android 11+ before release
  - Tests: From40To41MigrationTest (view recreated with the exact definition and readable in position order; sets `legacy_alter_table=OFF`, a no-op on Robolectric's SQLite), LyricsFetchWorkerTest (fresh Karaoke row + missing Synced -> no BetterLyrics call and only a Synced write; fresh Karaoke/Synced + missing Unsynced -> only the Unsynced chain runs); both new worker tests fail without the fix
  - Full unit suite: ComposeN-Zik 398 tests, 0 failures; `assembleDebug` OK

perf(threading): remaining Main-thread blockers in new code — Goal G1 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - Found by an independent sweep of the code (not the specs) after Goals E/F: Goal E Lot 1 had only migrated 5 sites to `addNextOffMain`/`enqueueOffMain`, so 14 more new-code `Player.addNext/enqueue(list, context)` calls still ran the legacy `excludeMediaItems` filter (3 `runBlocking` DB reads) from click handlers on Main, usually after a `songs.map { it.asMediaItem }` (M10) that also ran on Main
  - H8: the 14 sites in `AlbumScreen`, `ArtistScreen`, `HomeSongsScreen`, `AlbumItemMenu`, `OnlineAlbumItemMenu`, `OnlinePlaylistItemMenu`, `LocalPlaylistItemMenu` now call `addNextOffMain`/`enqueueOffMain` with `asMediaItem` computed inside `withContext(NzikDispatchers.DATA)`. The 4 popup menus launch on `CoroutineScope(NzikDispatchers.UI)` rather than the popup's own scope (the menu hides and cancels it right after the click, which could silently drop the tap — same lesson as Lot 1); the 3 screens use their existing `rememberCoroutineScope()`
  - `CopyLogsDialog`: the log file `readText` ran on Main in the export button `onClick` and in the file-picker callback. The read is now a pure `readLogContent(logsDir, option)` run through `loadLogContent` on `NzikDispatchers.DATA`, with a test pinning that it does not run on the caller's thread
  - `AutoSessionCallback`: `onSetMediaItems` (`scope.future`) and `onPlaybackResumption` (`scope.launch`) ran on the Main-bound scope while mapping songs through `Song.asMediaItem` (synchronous bitmap decode); both now use `NzikDispatchers.DATA`. Checked that `onSetMediaItemsInternal` never touches `binder.player` before switching
  - **Fixed before review:** `ArtistScreen`/`AlbumScreen` passed `getSongs()` straight into the `DATA` mapping, but it can return the live `ItemSelector` (a non-synchronized mutable list), so ticking an item during the mapping could throw `ConcurrentModificationException`; a `.toList()` snapshot is now taken at click time
  - **Found and fixed during code review (3 layers, 26 raw findings, no intent_gap/bad_spec):** in `AlbumScreen`/`HomeSongsScreen` the selection reset (`itemSelector.isActive = false`) had drifted to after the async work, leaving the selection active during the add (a second tap could duplicate songs) and skipping the reset on failure; restored to run synchronously at click time. The log-read dispatch was not pinned by any test (moving it back to Main left everything green), fixed with an injectable-reader `loadLogContent` and a thread test. Repeated tracker-jargon comments trimmed to the WHY
  - Deferred (see deferred-work.md): pre-existing double log read / uncaught `IOException` in the export dialog and an unguarded `lateinit binder` in one Auto branch; no automated coverage for the 14 click sites or `AutoSessionCallback` (no Compose / MediaSession test harness in the module)
  - Out of scope, tracked separately: the equivalent legacy sites (12 `addNext/enqueue` calls, `Dialog.kt` `runBlocking` on Main, `DataSettings.kt` cache clearing) need an explicit legacy derogation (Goal G2); the `Song.asMediaItem` getter itself (~395 callers, synchronous decode) cannot be fixed locally
  - New test: `ReadLogContentTest` (8 tests: debug/crash/both, one log missing, absent file or directory, unknown option, dispatch off the caller thread)
  - Full unit suite: ComposeN-Zik 395 tests / 83 suites, 0 failures; `assembleDebug` OK

fix(rescue): credential keys, downloads index path, settings CSV round trip and stale cards (issue https://github.com/N-Zik-Group/N-Zik/issues/811)
  - Last.fm session was never exported/imported: `RescueFiles.LASTFM_KEYS` used the literal `"lastfmSession"` while the real constant `lastfmSessionKey` is `"lastfmSessionKey"`. The YouTube/Discord/Last.fm key lists are now built from the real constants (`ytCookieKey`, `lastfmSessionKey`, ...) so they cannot drift from `ExportSettingsDialog.buildCredentialEntries`
  - "Delete downloads" left the download index behind: `deleteDownloads` looked for `exoplayer_internal.db` in the cache/files dirs, but `StandaloneDatabaseProvider` creates it in the databases dir. It now deletes `context.getDatabasePath("exoplayer_internal.db")` plus its `-journal`/`-wal`/`-shm` files, so the index no longer claims songs are downloaded after their media is gone
  - Settings CSV import split the file with `readLines()` before parsing, which broke values containing line breaks that the exporter quotes. Export and import now use `kotlin-csv` (same library as the regular settings export/import) through `writeSettingsCsv` / `readSettingsCsv`; the hand-written `csvEscape` / `parseCsvLine` are removed
  - `exportSettings` / `importSettings` now return `Result<RescueFiles.SettingsOutcome>`; `RescueScreen.showSettingsResult` shows the new `rescue_encrypted_unavailable_warning` toast when the keystore could not be opened and credentials were skipped (previously only a Timber line, the UI reported plain success)
  - Security: the import failure log no longer prints the setting value (it can be a cookie/token), and `RescueActivity` plants the Timber `DebugTree` in debug builds only
  - `RescueScreen`: `statusMessage` was written but never read, so nothing recomposed after an async action and the Restore / Export logs cards could stay greyed out. Replaced by a `RescueFileState` (logs / DB backup / settings backup) read on `Dispatchers.IO` in a `LaunchedEffect` and refreshed after every action; also removed an unjustified `!!` and a duplicate import
  - `clearCache` and `deleteDownloads` now delegate to file-based cores (`clearCacheDirs`, `deleteDownloadFiles`) so they can be unit-tested on temp dirs; behavior of `clearCache` is unchanged
  - Behavior notes: the CSV is now written by `kotlin-csv` (CRLF, same as the regular export) and import reads it by header, so a settings file without its `Type,Key,Value` header row is no longer accepted; rows without a key are skipped
  - Tests: `RescueFilesTest` rewritten (21 tests): key lists compared with `buildCredentialEntries` for each group, CSV round trip with comma / quote / line break / empty value, rows without a key, `clearCacheDirs` keeps `exo_downloads` in every base dir, `deleteDownloadFiles` removes the index from the databases dir and keeps `data.db` and the streaming cache
  - Verification: `:ComposeN-Zik:assembleDebug` OK, `RescueFilesTest` 21/0 failures, `ShortcutIconSyncTest` 13/0 failures. The full suite and on-device checks were NOT run
  - Not fixed, suspected and to confirm on a device: `exportDatabase` opens the DB `OPEN_READONLY` then runs `wal_checkpoint(TRUNCATE)` (expected `SQLITE_READONLY` on a WAL database), and `SQLiteDatabase.openDatabase` without a `DatabaseErrorHandler` may delete a corrupted `data.db` on open
feat(rescue): add category headers and delete logs action (issue https://github.com/N-Zik-Group/N-Zik/issues/811)
  - Added category headers (Data & Backup, Maintenance, Danger Zone) to the Rescue Center menu to better organize the available actions.
  - Added a new "Delete logs" action with a trash icon to allow the user to easily clean up crash and debug logs.
  - Added a new "Delete downloads" action to clean up ExoPlayer downloads and its database.
  - Wired the new action in `RescueFiles.kt` with a `deleteLogs` method.
  - Updated GitHub issue templates (ug_report.yaml, ug_report_beta.yaml, ug_report_dev.yml) to advise users to use the Rescue Center for exporting logs and settings.
fix(database): resolve Room migration crash on startup due to unhandled default values
  - Root cause: Room migrations 26->40 added several columns across `Artist`, `Album`, `Song`, `Playlist`, `Format`, and `SongPlaylistMap` using `DEFAULT ...` in SQLite, but the Kotlin `@Entity` data classes omitted the corresponding `@ColumnInfo(defaultValue = "...")` annotations. This caused `IllegalStateException: Migration didn't properly handle` upon schema verification.
  - Fix: Added the missing `@ColumnInfo(defaultValue = "...")` to all affected properties in `Artist.kt`, `Album.kt`, `Song.kt`, `Playlist.kt`, `Format.kt`, and `SongPlaylistMap.kt` to match the exact defaults defined in the SQLite migration scripts (`"0"`, `"-1"`, `"NULL"`).

fix(lyrics): user-edited lyrics no longer overwritten by automatic re-fetch (issue https://github.com/N-Zik-Group/N-Zik/issues/765)
  - Root cause: `LyricsDecisionMaker.evaluateFetchNeeds` ignored the stored lyrics content, and the dedup globals `globalLast*AttemptMediaId` only remember the LAST mediaId — playing another track ~10 s and returning made the `LaunchedEffect` re-run the fetch, and `saveLyricsSafe` upserted the fetched row over the user's edit (same `songId`/`type` key) or created a higher-priority row shadowing it in Auto mode
  - New persistent marker: `Lyrics.isEdited: Boolean = false` (`@ColumnInfo(defaultValue = "0")`) + `From39To40Migration` (`ALTER TABLE Lyrics ADD COLUMN isEdited INTEGER NOT NULL DEFAULT 0`), `Database.kt` bumped 39 → 40, `40.json` schema generated — existing rows migrate to `isEdited = 0`, no data loss (the builder has `fallbackToDestructiveMigration()`, so a faulty migration would have wiped the DB)
  - `EditLyricsDialog.onSet` now saves through the new `internal fun EditLyricsDialog.editedLyrics(...)` factory, which always flags `isEdited = true`
  - `LyricsDecisionMaker.evaluateFetchNeeds`: a current row that is `isEdited` with non-empty `data` sets all three fetch needs to false — no network call, no upsert, in every mode (Auto/Karaoke/Synced/Unsynced)
  - `LyricsFetchWorker.saveLyricsSafe` now goes through `upsertUnlessEdited` (including the 5 s FK-retry path): re-reads the same-key row before writing and skips it when it is a non-empty user edit — closes the fetch/edit race (an edit saved while a fetch was in flight)
  - Semantics kept per the approved spec: an emptied edit (`""`/`null`) counts as "no lyrics" and re-enables the auto-fetch; the manual paths (refresh button, "Récupérer à nouveau", LrcLib pick, mode change) are untouched — "Récupérer à nouveau" rewrites the row `data = null` / `isEdited = false`, so a manual re-fetch still replaces the edit
  - Out of scope (tracked in deferred-work.md): the pre-existing unguarded writers `ExportCacheDialog.getLyricsText` and legacy `downloadSyncedLyrics`, the LrcLib selection not being flagged `isEdited`, and the `onSet` call-site test wiring (needs Compose test infrastructure)
  - Tests: `LyricsDecisionMakerTest` (edited row protected in every mode, Karaoke-as-Synced-fallback, un-edited baseline unchanged, emptied edit re-fetchable), `LyricsFetchWorkerTest` (no network + no write for a stored edit, in-flight edit not overwritten, emptied edit replaced), `LyricsTableTest` (`isEdited` round trip + manual reset), new `From39To40MigrationTest` (Robolectric on a raw v39-shaped SQLite DB: rows preserved with `isEdited = 0`, column attributes match what Room expects), new `EditLyricsDialogTest` (saved row is flagged)
  - Verification: `:ComposeN-Zik:test` 334 tests / 0 failures, `:ComposeN-Zik:assembleDebug` OK; the spec's manual checks (edit → play another track ~10 s → return; install over a real v39 base) were NOT run
  - Code review: bmad-build 3-layer pass (16 findings: 2 patched, 1 deferred, 13 rejected) then bmad-code-review 4-layer pass (15 findings: 2 patched — migration-test `type` assertion + JUnit 4 KDoc justification — 3 deferred, 10 rejected; a claimed "missing patch" was verified and refuted)

perf(animations): cheaper per-frame work in SeekBarWaved and ShimmerHost — Goal F, M11 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - **Investigation finding (corrects the original plan):** the audit's M11 lists 7 legacy per-frame animation/draw sites and ARCHITECTURE-SPINE.md (AD-4 tier 3) prescribed rewriting them "to get off Main". Reading all 7 (`PlayerState`, `ShimmerHost`, `SeekBarWaved`, `FluidGradient`, `AnimatedGradient` x2, `SliderPulsatingEffectModifier`, `BlurredCover`) showed they are Compose frame/draw callbacks (`withFrameNanos`, `drawWithContent`, `Canvas`, `rememberInfiniteTransition`) that must run on Main by platform design — there is nothing to move to a `NzikDispatchers` dispatcher, so M11 is not a threading bug like H10 was. Agreed with the user to do targeted CPU/idiom optimizations on the 2 files with real, reducible per-frame cost instead of a tier-3 rewrite/move; the other 5 were investigated and left untouched (negligible cost or already idiomatic)
  - `SeekBarWaved.kt`: `wavePath()` rebuilt the wave `Path` with a `sin()` + `lineTo()` per horizontal pixel, every animated frame, for as long as the "Wavy" seek bar is on screen. Sampling step is now a named `WAVE_PATH_STEP_PX = 3f` (was a hardcoded 1px) — ~3x fewer `sin()`/`lineTo()` calls per frame, no perceptible visual change (the wave is a smooth low-frequency sine). The x-coordinate loop was extracted into a pure `waveSampleXs()` helper because a Compose `Path` on the android target needs a real `android.graphics.Path` and can't be built in a plain JVM unit test
  - `ShimmerHost.kt`: hand-rolled `LaunchedEffect { while(true) { ...; delay(16) } }` (an approximate 60 FPS clock) replaced with `rememberInfiniteTransition().animateFloat(0f..1000f, tween(1600, LinearEasing))` — same 0..1000 range and ~1600ms cycle, but synced to Compose's real frame clock instead of a fixed delay, matching the pattern the rest of the M11 lot already uses
  - Dead code noticed but deliberately left alone (out of scope): `FluidGradientBox()` in `FluidGradient.kt` (no callers) and `wavePath2` in `SeekBarWaved.kt` (never called)
  - **Caught by orchestrator diff review before commit:** the implementation subagent created its test under `app/it/fast4x/rimusic/...` (legacy package), contradicting the spec's frozen "no new file under `app.it.fast4x.rimusic.*`" boundary. Moved to `app.n_zik.android.legacyoffmain.components.SeekBarWavedTest` (same convention as Goal E Lot 2's tests; `internal` is module-scoped so the symbols still resolve)
  - New test: `SeekBarWavedTest` (5 tests on `waveSampleXs`: segment count shrinks with a larger step, default step equals the named constant, first sample is the origin, full-width coverage within one step for several step sizes)
  - Visual check done by the user on their own device (Wavy seek bar + a shimmer loading screen): no regression seen. 3-layer code review skipped on the user's explicit instruction (2-file behavior-preserving patch, diff read in full, suite green)
  - Split off as a separate future lot: M14 (`FileLoggingTree` sync per-line file I/O, a genuine off-Main candidate) — logged in deferred-work.md
  - Full unit suite: ComposeN-Zik 323 tests / 77 suites, 0 failures; `assembleDebug` OK

refactor(threading): legacy in-place patches on H10/M12/M13 hotspots — Goal E, Lot 2 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - Unlike Lot 1 (caller-side only), these hotspots live entirely inside legacy screens (`app.it.fast4x.rimusic.*`) with no new-code call site to fix from outside — required an explicit user-granted derogation (AD-4 tier 2: minimal in-place `withContext` patch per site, no restructuring, at most one new-code import — `NzikDispatchers`, AD-5)
  - H10 (palette CPU work on Main): `MiniPlayer.kt`/`Player.kt` (ui/screens/player) each had their cover-swipe `LaunchedEffect` run `dynamicColorPaletteOf`/`Palette.from(bitmap).generate()` inline on whatever dispatcher the effect resumed on (Main). Extracted to `computeMiniPlayerPalette`/`computePlayerDynamicPalette` (module-`internal`, for testability only — no new public legacy API) and wrapped in `withContext(NzikDispatchers.MEDIA)`
  - M12 (network parsing on Main, 8 legacy screens): `MoodsPage.kt`/`NewAlbums.kt` (`Innertube.discoverPage()`), `MoodList.kt` (`Innertube.browseCategory`), `OnlineSearch.kt` (`Innertube.searchSuggestionsWithItems`), `HistoryList.kt` (`YtMusic.getHistory`), `ArtistScreenModern.kt` (`YtMusic.getArtistPage` + `Innertube.queue`, 2 independent wraps in the same `onSuccess`) all wrapped in `withContext(NzikDispatchers.DATA)`, minimal per-call-site patch, no extraction needed
  - M12+M13 combined (`VideoOrSongInfoScreen.kt`): `Innertube.songInfo`/`searchPage` (M12) and `Database.artistTable.findBySongId`/`findByName(...).first()` in a loop (M13) shared one sequential `LaunchedEffect(videoId)` body — extracted whole to `loadVideoOrSongInfo` (one entry/exit point, no control-flow restructuring) and dispatched via a single `withContext(NzikDispatchers.DATA)`
  - M13 (`PlaylistSongList.kt`): `playlistSongs.filter { Database.songTable.isLiked(...).first() }` (per-song Room read) wrapped whole in `withContext(NzikDispatchers.DATA)`
  - `GoToLink.kt` (M12, the riskiest site — an entire coroutine moving from Main to IO): extracted to `resolveGoToLink()`, launched on `NzikDispatchers.DATA` instead of `Dispatchers.Main`, with each of the 4 `navController.navigate(...)` calls plus `forcePlay` individually wrapped back in `withContext(NzikDispatchers.UI)` so navigation/playback still happen on Main despite the rest of the block running on IO
  - Code review (bmad-build, 3 layers — Blind Hunter/Edge Case Hunter/Verification Gap — 17 raw findings, 1 pass, no intent_gap/bad_spec): 5 patches routed — a KDoc off-by-one (comment said 7 state variables, listed 8), 2 missing test cases for `loadVideoOrSongInfo`'s catch-all/dedup paths, `GoToLink.kt` missing any off-main extraction+test despite being the highest-risk site, and a leftover `withContext(Dispatchers.Main)` inconsistent with the 4 `NzikDispatchers.UI` sites right above it. 3 findings deferred (pre-existing silent catches, pre-existing `CoroutineScope` not tied to composition lifecycle — both predate this diff, not introduced by it), 2 rejected (`NzikDispatchers.DATA` IO-pool contention already settled at Goal A, `loadVideoOrSongInfo` resetting to `null` on exception vs. keeping stale data — the UI already falls back gracefully, not a visible regression), 7 false (misread diff hunks, since disproven by direct file reads)
  - **Resumed after the implementation subagent hit an API rate limit mid-patch-application**: re-verified each of the 5 routed patches directly against the files on disk rather than re-delegating — 4 were already applied before the subagent died (KDoc fix, the 2 new test cases, `GoToLink.kt`'s extraction+test+dispatcher consistency fix); the 5th (indentation on `ArtistScreenModern.kt`'s new `withContext` block) turned out to be a non-issue on direct measurement — the new block nests consistently at 4-space increments, the visual mismatch the review flagged was pre-existing surrounding legacy code, out of scope for a tier-2 minimal patch
  - Full unit suite: ComposeN-Zik 318 tests / 76 suites, 0 failures; `assembleDebug` OK

fix(shortcuts): launcher shortcut icons stuck white on Pixel despite theme-adaptive design (_bmad-output/implementation-artifacts/spec-shortcuts-theme-icons-build-links.md)
  - Started as a bug report mid-review of a separate shortcuts feature (theme-adaptive icons + per-build-type links, built in a different session): on Pixel, shortcut icons showed white instead of the required "always black" — the "Option A" fix that session had written (push a same-id dynamic shortcut with a fixed black bitmap icon on the Google launcher) was marked "to verify on-device" and never actually tested
  - **Root-caused with real on-device/emulator verification (adb/dumpsys/logcat) at every step, not assumption:** (1) `homeLauncherPackage()` always returned `null` — Android 11+ package visibility requires a `<queries>` manifest entry to resolve another package's `ACTION_MAIN`/`CATEGORY_HOME` activity, which was missing; added it, confirmed the launcher (`com.google.android.apps.nexuslauncher`) now resolves. (2) Once resolved, "Option A" itself turned out to be platform-impossible: `ShortcutManager.setDynamicShortcuts`/`disableShortcuts` both threw `IllegalArgumentException: Manifest shortcut ID=... may not be manipulated via APIs` — a manifest (static XML) shortcut is fully immutable at the OS level, no mutation (add/update/disable) is permitted via the API once its id exists in the manifest, ruling out both "override by same id" and "disable static + add dynamic under a different id"
  - **Fix (user-approved redesign, "tout dynamique"):** removed the 5 static `shortcuts.xml` files (androidMain/debug/foss/dev/dev32) and the manifest's `android.app.shortcuts` meta-data entirely. `shortcuts/ShortcutIconSync.kt` renamed to `registerAppShortcuts(context)`, called from `MainActivity.onCreate`: registers all 4 shortcuts purely as dynamic shortcuts on every launch, with `Icon.createWithResource` (theme-adaptive, resolved by the launcher exactly like a manifest icon would be) on any launcher, or `Icon.createWithBitmap` (fixed black, via a new `tintedBitmap` helper) on the Google/Pixel launcher. Bonus: `Intent(context, MainActivity::class.java)` resolves the right package regardless of `applicationIdSuffix`, making the per-build-type `shortcuts.xml` overrides unnecessary too
  - Trade-off accepted by the user: shortcuts no longer exist until the app has been launched at least once after install (previously available immediately via the manifest, on every launcher, not just Pixel)
  - Verified on a Pixel_10_Pro_XL emulator: `dumpsys shortcut` shows all 4 shortcuts flagged dynamic with a real saved bitmap (`bitmapPath=...png`, no `iconRes`), matching log `"Google launcher: registered shortcuts with fixed black icons"`; black icons confirmed visually by the user on the emulator
  - `ShortcutResourcesTest.kt` (tested the now-deleted static `shortcuts.xml`) removed; `ShortcutIconSyncTest.kt` rewritten: launcher detection (unchanged), `shortcutSpec` id→resource/action mapping (plain JVM), `shortcutIcon`/`tintedBitmap` icon-type and size (Robolectric) — exact tint-color fidelity isn't verifiable in Robolectric here (its `ColorDrawable`/`ShapeDrawable` shadow doesn't apply `DrawableCompat.setTint` during `draw()`, confirmed by a failing assertion), so that's left to the real on-device verification above rather than asserted in the unit test
  - Briefly tried enabling `testOptions.unitTests.isIncludeAndroidResources` to load real resources in tests instead — reverted immediately, it regressed ~36 unrelated tests via a JVM security-provider conflict (`KeyStoreException`/`NoSuchAlgorithmException`)
  - **Follow-up, same session: shortcuts tapped while the app was already running (any of the 5 actions) did nothing — only a cold start worked.** Root-caused and fixed in 3 steps, each verified on-device (adb `am start -a <action>` while the app stayed open, screenshotted):
    1. `MainActivity.onNewIntent` updated `intentUriData` (deep links) but never called `setIntent(intent)` nor touched anything Compose-observable for the shortcut action — the composable kept reading the stale launch-time `intent.action` forever. Added a `private var shortcutIntentAction by mutableStateOf<String?>(null)` (same pattern as `intentUriData`), set from both `onCreate` and `onNewIntent` (which now also calls `setIntent(intent)`)
    2. First attempt regressed: resetting `shortcutIntentAction = null` synchronously inline in the composable (mirroring the old raw-`Intent.action = null` mutation) raced with `HomeScreen`'s `LaunchedEffect(openTabFromShortcut)` — both the -1→X and the immediate X→-1 recomposition could settle before that effect ever ran, silently dropping the navigation. Fixed by resetting from a `LaunchedEffect(shortcutIntentAction)` instead (matching how `intentUriData` already resets itself at the end of its own effect, a few lines below)
    3. Two more gaps found only by testing every one of the 5 shortcuts repeatedly, not just search once: (a) `HomeScreen`'s tab-shortcut handling (songs/albums/artists/library) only ever read `openTabFromShortcut` through a `remember`ed snapshot taken at first composition — live taps while the app stayed open never switched tabs (search alone had a dedicated live `LaunchedEffect`). Added an equivalent `LaunchedEffect(openTabFromShortcut)` for tabs, plus `shouldSwitchTabFromShortcut`/`tabIdForIndex` helpers. (b) `searchShortcutConsumed` (a `rememberSaveable` one-shot gate) latched permanently for the composable's whole lifetime instead of per-occurrence — search worked exactly once per app process, never again. Fixed by resetting that flag back to `false` as soon as the sentinel moves off `OPEN_SEARCH_SHORTCUT`, so each new live occurrence is treated as fresh while still guarding against the same occurrence re-firing across an unrelated recomposition. (c) None of the above helped if a *different* screen (e.g. search) was already on top of the back stack when a tab shortcut was tapped — `HomeScreen` isn't part of the composition while another destination is active, so its effects never ran regardless. Fixed with a `currentBackStackEntryAsState()`-driven effect in `MainActivity` that pops back to `NavRoutes.home.name` first and only clears `shortcutIntentAction` once the back stack has actually settled on home, giving `HomeScreen` a real chance to observe the value once it reappears
  - New test: `shouldSwitchTabFromShortcut` case added to `ResolveInitialTabIndexTest.kt`
  - Full unit suite: ComposeN-Zik 318 tests / 76 suites, 0 failures; `assembleDebug` OK

refactor(threading): caller-side wraps on H8/H9/M9/M10 hotspots — Goal E, Lot 1 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - New `utils/player/PlayerQueueOffMain.kt` (H8, paired with M10 wherever `Song.asMediaItem` feeds it): legacy `Player.addNext`/`Player.enqueue` filter their incoming list through `excludeMediaItems` whenever a non-null `Context` is passed, doing up to 3 `runBlocking` DB reads (disliked songs/artists/albums) — every `app.n_zik.android.*` call site used to call them directly on whatever thread the click handler ran on (Main, in every case here). New `addNextOffMain`/`enqueueOffMain` suspend extensions run that filtering on `NzikDispatchers.DATA`, then call the legacy function with `context = null` (skips its own filtering, already done) so the actual `player.*` (ExoPlayer) mutation happens exactly once, on whichever thread resumes the `withContext` — Main, for every current caller. `Player.kt` itself untouched (AD-4 tier 1: caller-side fix only)
  - 5 call sites migrated to the new helper: `HomeLibrary.kt`/`HomeArtist.kt`/`HomeAlbum.kt` (multi-select "Play next"/"Enqueue", `Dispatchers.IO` mapping renamed to `NzikDispatchers.DATA` at the same time) and `SongItemMenu.kt`/`VideoItemMenu.kt` (single-song "Play next"/"Enqueue", previously fully synchronous, no wrap at all)
  - `SongItemMenu.kt`/`PlayerItemMenu.kt`/`VideoItemMenu.kt`'s `onPlay` (M10 only — `Player.forcePlay` takes no `Context` and never filters, confirmed by reading `Player.kt`): `song.asMediaItem`'s bitmap decode now runs via `withContext(NzikDispatchers.DATA)` before `forcePlay` on Main
  - `ChangeCoverDialog.kt`/`ChangeArtistCoverDialog.kt`/`ChangeAlbumCoverDialog.kt` (H9): the `saveImageToInternalStorage` decode/scale/compress (plus the old-file delete, song dialog only) used to run synchronously inside the `ActivityResultLauncher` callback (Main). Extracted to an `internal fun saveCoverArt(...)` per dialog (testability only) and dispatched via `withContext(NzikDispatchers.DATA)` from a new `rememberCoroutineScope()`
  - `OnDeviceSongs.kt` (M9): `context.getLocalSongs(...)`'s MediaStore scan + file reads used to run inline in a `LaunchedEffect` (Main); added `.flowOn(NzikDispatchers.DATA)` right after the call so only the upstream query moves off Main, `distinctUntilChanged`/`onEach{ scrollToItem }`/`collect` stay on Main for the Compose state writes
  - Pure `Dispatchers.IO` → `NzikDispatchers.DATA` renames on the pre-existing like/dislike-toggle sites in the same 3 menu files (same underlying dispatcher, no behavior change)
  - **Correctif orchestrateur avant même la revue** : la première passe d'implémentation ne déportait que le filtrage H8 dans `SongItemMenu.kt`/`VideoItemMenu.kt` — `song.asMediaItem` (M10) restait calculé sur Main, dans le `coroutineScope.launch` lui-même, avant l'appel au helper. Détecté en relisant le diff complet indépendamment avant la revue (comparaison avec le pattern correct de `HomeLibrary.kt`), corrigé directement (4 sites, 2 fichiers)
  - **Found and fixed during code review (3 layers, 1 loop iteration, no intent_gap/bad_spec)**: (1) `SongItemMenu.kt`/`VideoItemMenu.kt`'s new `playNext`/`enqueue` coroutines launched on the popup menu's `rememberCoroutineScope()` — `MenuComponent.kt`'s `onShortClick(){ onClick(); menuState.hide() }` hides the menu right after the (now async) click returns, and while `Menu.kt`'s `AnimatedVisibility` keeps the popup alive through its exit animation rather than disposing instantly, the scope is still cancelled once that animation ends; if the async work outlasted it, the tap was silently dropped before `player.addNext/enqueue` ever ran. Fixed by launching on `CoroutineScope(NzikDispatchers.UI).launch {}` instead — independent of the popup's lifecycle, `NzikDispatchers.UI == Dispatchers.Main` so the final `player.*` call still resumes on Main. (2) The 3 cover dialogs' fire-and-forget save coroutine had no guard against two rapid picks completing out of order and overwriting `value`/the Coil cache with a stale result — fixed with a per-dialog `remember { mutableStateOf<Job?>(null) }` cancelling the previous save before launching a new one. (3) Only the song dialog's `saveCoverArt` had a dedicated test — added the same delegation-check test for the album/artist variants. (4) `PlayerQueueOffMain.kt`'s KDoc overstated a Main-thread guarantee the dispatcher-agnostic function doesn't itself provide — reworded
  - 1 item deferred: `LocalPlaylistSongs.kt:792` (legacy, never cited by the audit's H9) has the exact same synchronous `saveImageToInternalStorage` pattern, but fixing it would mean editing a legacy file (AD-4 tier 2), out of this tier-1-only lot — see deferred-work.md
  - 4 new test files (`PlayerQueueOffMainTest`, `ChangeCoverDialogSaveCoverArtOffMainTest` + 2 lighter delegation-only variants for the album/artist dialogs): mock-based verification that `addNextOffMain`/`enqueueOffMain` forward the filtered list with `context = null` and dispatch off the caller's thread, and that each dialog's `saveCoverArt` forwards its arguments unchanged (plus the song dialog's delete-old-file and thread-dispatch cases)
  - Full unit suite: ComposeN-Zik 288 tests / 69 suites, 0 failures; `assembleDebug` OK

refactor(threading): widget offloads on M4 hotspot — Goal D, Lot 4 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `widget/NZikWidgetManager.kt`/`PlaylistWidgetManager.kt` (M4): palette extraction (`dynamicColorPaletteOf`, expensive) and bitmap work (scaling, rounding, circling) used to run synchronously on whatever thread the caller used — `Dispatchers.Main` for the idle widget path (widget added/resized), raw `Dispatchers.IO` for the per-second playing-tick path. Both managers' `updateWidgets()`/`updateIdleWidget()` now wrap that work in `withContext(NzikDispatchers.MEDIA)`, so it always runs off Main regardless of the caller. `extractPalette` widened `private` → `internal` in both (testability only). `buildQuickPicks`'s `Dispatchers.IO` renamed to `NzikDispatchers.DATA` (same object, real DB/network I/O)
  - **Corrected an inaccurate assumption from the prior status doc**: the 3 `AppWidgetProvider`s (`MusicWidgetReceiver`, `TurntableWidgetReceiver`, `PlaylistWidgetReceiver`) do NOT run in a separate process (no `android:process` in `AndroidManifest.xml`) — normal JVM/Robolectric testability, no special limitation
  - `MusicWidgetReceiver.kt`/`TurntableWidgetReceiver.kt`/`PlaylistWidgetReceiver.kt`: `CoroutineScope(Dispatchers.Main)` renamed to `CoroutineScope(NzikDispatchers.UI)` (same object, pure rename)
  - **Found and fixed during code review, not before**: the first test pass (6 tests across 2 new files) only proved `extractPalette` behaves correctly under a `withContext(NzikDispatchers.MEDIA)` written by the test itself — none of them called the actual production `updateWidgets()`/`updateIdleWidget()`, so reverting those functions' own `withContext(MEDIA)` wrap would have left every test green. Fixed by adding one test per file that calls the real production entry point, using a `ContextWrapper` that records which thread calls `getSharedPreferences` (invoked synchronously inside `extractPalette`) to observe the actual dispatch. Verified via mutation testing: temporarily replacing the real `withContext(MEDIA)` with a no-op `run{}` made the new tests fail as expected, then reverted
  - `PlayerServiceModern.kt` deliberately left untouched (out of scope for this hotspot — its own dispatcher choice for the per-second widget-update tick wasn't part of the audit's M4 citation)
  - 2 new `*ExtractPaletteOffMainTest` files (8 tests total: result-equality for a solid bitmap and for `null`, thread-name assertion on `extractPalette` directly, and the real-entry-point dispatch assertion added in review)
  - Code review (bmad-build, 3 layers, 1 loop iteration): 2 `patch` findings (the decoy-test gap above, fixed); 4 findings deferred (`NzikDispatchers.MEDIA`'s fixed 2-thread pool now also hosts per-second widget-refresh work alongside existing lyrics/visualizer/botguard consumers with no isolation; idle-path widget updates moved from Main — naturally serialized — to MEDIA's 2 real threads, newly permitting idle-vs-idle concurrent updates to the same `appWidgetId`, plus a pre-existing non-volatile quick-picks cache race gaining a new component) — see deferred-work.md; 6 findings rejected as false/cosmetic (dependencies already present, diff-staging artifact, consistent with established test/visibility conventions from prior lots)

refactor(threading): DB/IO offloads on M2/M7/M8 hotspots — Goal D, Lot 3 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `playback/utils/Shuffler.kt` (M2): `play()` no longer blocks the caller via `runBlocking` for its 3 disliked-song/artist/album Room queries — the whole filtering+playback body now runs on an object-owned `scope.launch(backgroundDispatcher)` (`backgroundDispatcher` defaults to `NzikDispatchers.DATA`, a test seam). Since `play()` stays non-suspend (~8 of its ~13 callers invoke it from a plain `onClick`), it gained an `onComplete: () -> Unit = {}` parameter invoked once each exit path's real work is actually finished — never before, unlike a naive fire-and-forget rewrite would have produced
  - **Found and fixed during code review, not before**: the first implementation pass moved `play()`'s entire body into fire-and-forget without `onComplete`, so `play()` returned near-instantly instead of blocking like `runBlocking` used to. 5 of its callers (`ArtistDetails.kt` x2, `ArtistScreen.kt` x2, `HomeQuickPicks.kt`) depend on synchronous completion to clear a loading flag (`try { Shuffler.play(...) } finally { isXLoading = false }`) — with the naive rewrite, the loading spinner would have disappeared before the song actually started playing, a real observable-behavior regression against the fix's own acceptance criteria. Caught by 3-layer code review before merge; fixed by adding `onComplete` and updating exactly those 5 call sites (the other ~8 keep the default no-op). A second review pass then caught that the 3 disliked-content DB lookups inside `play()`'s background coroutine had no try/catch at all — with dislike filtering at its default-enabled setting, a transient DB error would have left `onComplete()` never firing and those same 5 loading flags stuck forever; fixed by wrapping the whole coroutine body in try/catch, routing failure through the same `onComplete`-on-UI path as every other exit
  - `positions()`: `Dispatchers.Default`/`Main` renamed to `NzikDispatchers.DATA`/`UI` (already correctly async, pure rename)
  - `updater/ui/UpdateScreen.kt` (M7a): the bundled changelog read (`R.raw.release_notes`, decode+full-text read) extracted to a top-level `readLocalReleaseNotes()`, moved from a synchronous `remember{}` block (main thread, during composition) to `LaunchedEffect` + `withContext(NzikDispatchers.DATA)`
  - `components/dialog/song/EditMetadataDialog.kt` (M7b): the picked cover-art image read (`ContentResolver.openInputStream(...).readBytes()`, up to several MB) extracted to `readCoverArtBytes()`, moved off the `ActivityResultLauncher` callback's Main thread via `coroutineScope.launch(NzikDispatchers.DATA)`, state assignment on `NzikDispatchers.UI`
  - `components/menu/player/AudioDeviceMenu.kt` (M8): `loadDevices()` (device enumeration + Bluetooth battery reflection via `BluetoothDevice::class.java.getMethod`) was reached from Main on every trigger — a `LaunchedEffect`, 3 `BroadcastReceiver`s, an `AudioDeviceCallback`, and a 30s `Handler(Looper.getMainLooper())` poll. Both real call sites (`refreshDevices()`, the `bluetoothLauncher` callback) now wrap it in `coroutineScope.launch(NzikDispatchers.DATA)`; `loadDevices` widened to `internal` for testability, body unchanged
  - **M3 (`ArtworkContentProvider.openFile()`) investigated and descoped**: it's a synchronous `ContentProvider` override imposed by the Android framework with no suspend/async variant, and the thread it runs on is caller-determined (a Binder thread for cross-process reads via MediaSession/notifications/Android Auto, or Coil's own background dispatcher for in-app image loads) — no `NzikDispatchers` seam is injectable into it. Documented in the spec's Intent rather than silently dropped, matching the M1 precedent from Lot 2
  - 3 new `*OffMainTest` files (M7a/M7b/M8) plus 8 new `ShufflerTest` cases (M2): completion-callback ordering, fire-and-forget timing proven via `StandardTestDispatcher` (not just an eagerly-executing one), a pinned production-default assertion for `backgroundDispatcher` via `@BeforeAll`, and the DB-failure-still-completes case found in the second review pass
  - Code review (bmad-build, 3 layers, 2 loop iterations): iteration 1 found the fire-and-forget/loading-flag regression above (routed `bad_spec`, code reverted and re-derived) plus 2 small patches (`EditMetadataDialog.kt` missing `CancellationException` rethrow, import order); iteration 2 found and fixed the unguarded DB-filtering exception path above (routed `patch`) plus a doc-comment inaccuracy; 3 items deferred (a narrow double-trigger race on 3 of the touched files, `AudioDeviceMenu`'s concurrent-refresh ordering, `EditMetadataDialog`'s rapid-re-pick ordering) — see deferred-work.md
  - Full unit suite: ComposeN-Zik 270 tests / 63 suites, 0 failures; `assembleDebug` OK

refactor(threading): CPU offloads on M5/M6/M15 hotspots — Goal D, Lot 2 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `extensions/nextvisualizer/NextVisualizer.kt` (M5): `Icon.getCircledBitmap(...)` deported to `NzikDispatchers.MEDIA` in both call sites (artwork `LaunchedEffect` + `onMediaItemTransition` listener, success and `APP_ICON_BITMAP` fallback paths) — only the `bitmapCover`/`circleBitmap` state assignments stay on `NzikDispatchers.UI`; raw `Dispatchers.IO`/`Dispatchers.Main` replaced by `NzikDispatchers.DATA`/`UI` on the same sites
  - `components/player/lyrics/LyricsScreen.kt` (M6): `dynamicColorPaletteOf(...)` (legacy `ColorPalette.kt`, untouched — wraps `Palette.from(bitmap).generate()`, no UI-thread dependency) wrapped in `withContext(NzikDispatchers.MEDIA)`; only `dominantColor`'s assignment stays outside the block
  - `core/youtube/security/potoken/PoTokenWebView.kt` (M15): `makeBotguardServiceRequest`'s `handleResponseBody` changed to `suspend (String) -> Unit`; `parseChallengeData`/`parseIntegrityTokenData` (`JavaScriptUtil.kt`, untouched — 2 JSON parses + a byte-level descramble, non-trivial one-time cost per PoToken init/refresh) now run inside `withContext(NzikDispatchers.MEDIA)` in both callers, `webView.evaluateJavascript(...)` still runs after on Main (WebView's own-thread requirement, unchanged). Added a missing `catch (e: CancellationException) { throw e }` in `onRunBotguardResult` ahead of the existing broad catch, so a cancelled coroutine is no longer mistaken for a parse failure
  - **M1 (`VoiceSearchOverlay.kt`) investigated and descoped**: the audit-cited "200 points" are 3 `sin()` calls each generated inside the `Canvas { }` draw block itself (~600 calls/frame), which must stay on the UI thread by construction (same constraint as `VisualizerView.onDraw`) — negligible cost on target hardware, no measurable gain available without disproportionate over-engineering (precompute+republish via `State` at 60fps) for a purely decorative animation. Documented in the spec's Intent and in deferred-work.md rather than silently dropped
  - 8 new tests across 3 files (`IconCircledBitmapOffMainTest`, `DynamicColorPaletteOffMainTest`, `PoTokenParsingOffMainTest`): each offloaded function verified to produce the same result under `NzikDispatchers.MEDIA` as called directly, and to actually run on an `nzik-media-*` thread rather than the caller's
  - Code review (bmad-build's own 3-layer review): 2 patches applied (stale `NzikDispatchers.MEDIA` KDoc updated with the new consumers; `LyricsScreen.kt`'s fully-qualified `kotlinx.coroutines.withContext` calls switched to the imported form for consistency with the rest of the diff); 4 items deferred (a second, unrelated palette-extraction site in `NextVisualizer.kt` still on raw `Dispatchers.Default`; `PoTokenWebView.kt` still has other unmigrated raw dispatcher usages outside M15's scope; no dedicated test for the new `CancellationException` guard; a pre-existing error-message-quality asymmetry between the two botguard callbacks) — see deferred-work.md
  - Full unit suite: ComposeN-Zik 255 tests / 59 suites, 0 failures; `assembleDebug` OK

refactor(threading): coroutine hygiene pass on M16/M17/M18 hotspots — Goal D, Lot 1 (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `modules/discordrpc/.../GatewayWebSocket.kt` (M16): `close()`'s `runBlocking { session?.close() }` (reachable from Main via `DiscordRpcConnection.closeDirect()`) replaced with `launch {}` on the class's own pre-existing `CoroutineScope` (`SupervisorJob` + `Dispatchers.Default`) — no longer blocks the caller. Decision: no new injection point added to `DiscordRpcConnection`; the existing internal scope was already correctly formed off-Main, only the blocking call itself was the defect
  - `extensions/innertube/.../Invidious.kt` (M17): new `@Volatile var backgroundDispatcher: CoroutineDispatcher` injection seam (defaults to `Dispatchers.IO`, same pattern as `VisualizerHelper.snapshotProvider` — this module can't depend on `NzikDispatchers`, Gradle dependency direction is app -> module only) + `internal val scope = CoroutineScope(SupervisorJob())`, replacing a fire-and-forget `CoroutineScope(Dispatchers.IO).launch` created fresh on every `useUnofficialInstances` toggle; `MainApplication.onCreate()` wires `Invidious.backgroundDispatcher = NzikDispatchers.DATA` next to the existing `VisualizerHelper.snapshotProvider` wiring
  - `extensions/innertube/.../PublicInstances.kt` (M17): `@Volatile` added to `instances` (the field the audit cited). `unreachableInstances` deliberately left non-thread-safe — it's mutated in place (`clear()`/`add()`), not just reassigned, so `@Volatile` alone wouldn't fix it; a real fix needs a thread-safe collection, tracked in deferred-work.md
  - M18 job-leak cleanup: `HomeScreen.kt` (back-handler confirm-tap timer), `ExportCacheDialog.kt` (`onExport`/`batchExport`, one shared scope in the companion object), `Store.kt` (`prefetchCookie`) — each replaced a per-call `CoroutineScope(Dispatchers.IO/Default).launch` with a single shared `CoroutineScope(NzikDispatchers.DATA + SupervisorJob())`, so one failed launch can't take out a later one
  - 4 shared scopes (`Invidious.scope`, `Store.scope`, `ExportCacheDialog.scope`, `HomeScreen`'s `homeScreenScope`) made `internal` (were `private`) solely for JVM test access — same precedent as the pre-existing `StreamResolver.scope`
  - New test infrastructure added to two previously test-less Gradle modules (`extensions/innertube`, `modules/discordrpc`): JUnit5 + kotlinx-coroutines-test, catalog-managed only. 7 new tests total: `InvidiousScopeTest` (oldtube), `GatewayWebSocketCloseTest` (discordrpc), `HomeScreenScopeTest`/`StoreScopeTest`/`ExportCacheDialogScopeTest` (ComposeN-Zik) — each verifies the shared scope survives a failing child (SupervisorJob semantics), matching the existing `StreamResolverScopeTest` template
  - Code review (bmad-code-review, 4 layers): 2 patches applied — `@Volatile` added to `Invidious.backgroundDispatcher` (parity gap vs. the `VisualizerHelper.snapshotProvider` precedent it mirrors); documented in Implementation Notes + deferred-work.md that `GatewayWebSocket.close()` being fire-and-forget means no guarantee the close frame is sent before a process death in the narrow scheduling window (accepted best-effort trade-off per the fix's own intent, not a regression). A claimed close()/connect() session-reassignment race (raised independently by 2 review layers) was verified and refuted: every real call site either builds a brand-new `DiscordRpcConnection`/`GatewayWebSocket` on token change or is terminal teardown, so the same instance is never reconnected after being closed. 6 other low-severity findings rejected (test-coverage depth matching an existing convention, a dispatcher-category cosmetic mismatch, import ordering, and 3 spec-text inconsistencies fixed directly in the spec)
  - Full unit suite: ComposeN-Zik 247 tests / 56 suites (0 failures, baseline was 241/53); `oldtube` (extensions/innertube) 2 tests; `discordrpc` 1 test — all green; `assembleDebug` OK
  - Full spec: `_bmad-output/implementation-artifacts/spec-gh-606-coroutine-hygiene.md`. Goal D split into 4 independently-shippable lots (Lot 1 done here; Lots 2-4 — CPU offloads, DB/IO-on-Main, widget process — tracked in deferred-work.md)

fix(audiobar): false "error updating waveform" toast on the manual Update-waveform button (BMAD problem-solving session, _bmad-output/problem-solution-2026-09-16.md)
  - Root cause: `getOrExtractWaveform(): List<Int>?` conflated "cache transiently incomplete, retry" with "extraction failed for good" under the same `null`. The seekbar's own render path already retried through this transient state silently; the 3 manual-update menus (Song/Player/Video item menus) made a single attempt and reported any null as a fatal error toast
  - New `WaveformResult.kt`: sealed `Success`/`NotReady`/`NoCache`/`Failed` replacing the ambiguous `List<Int>?` contract; `WaveformExtractor.getOrExtractWaveform` now returns it, with a pure/testable `classifyExtractionFailure` distinguishing the transient `PlaceholderDataSource` IOException (`NotReady`) from a real failure (`Failed`)
  - New `WaveformExtractor.updateWaveform`/`retryUntilReady`: single source of truth for the menu's bounded retry (40 attempts x 500ms = 20s, injectable for tests) instead of tripling the retry loop across 3 menus
  - Found and fixed a second, more serious bug during on-device verification: the menu's onClick used to launch the update flow on its own `rememberCoroutineScope`, which is torn down ~300ms after `menuState.hide()`'s exit animation -- once retries/native extraction made the operation take longer than an instant, the coroutine was cancelled before its result (and toast) could ever be delivered, even on full success (confirmed on-device: extraction logged "-> SUCCESS" but no toast shown). Fixed with new `WaveformExtractor.requestUpdateWaveform`, a fire-and-forget entry point running on an object-owned `CoroutineScope(NzikDispatchers.DATA + SupervisorJob())` that outlives the calling composable
  - Retry budget widened from the original spec's 6x500ms (3s) to 40x500ms (20s): a download reaching a terminal state always triggers its own automatic `deleteWaveform` + `refreshSignal` (pre-existing `MyDownloadService.TerminalStateNotificationHelper`, unrelated to this fix), so a manual click racing that churn is the normal case, not a rare edge case, and can legitimately take several seconds to settle -- confirmed by repeated on-device testing sessions with NEVAR
  - 3 menus (SongItemMenu/PlayerItemMenu/VideoItemMenu): added a download-in-progress guard (`QUEUED`/`DOWNLOADING`/`RESTARTING`, queried directly via `MyDownloadHelper.getDownload` since the legacy `DownloadedStateMedia` enum has no IN_PROGRESS value) extending the existing alpha-dimming pattern; switched to `requestUpdateWaveform` with `appContext()` (matches the codebase's existing long-lived-background-op convention) instead of `LocalContext.current`
  - `SeekBarStaticAudioWaves.kt`: adapted to the new `WaveformResult` contract, exiting its own retry loop on `Success` only -- behavior otherwise unchanged, per spec
  - S5: unique per-attempt temp filename (`temp_audio_${mediaId}_${UUID}.tmp`) removing a latent race between two concurrent extractions for the same mediaId
  - Removed dead code: `extractionSuccessSignal`/`extractionErrorSignal` were emitted but never consumed anywhere in the repo (confirmed via full-repo grep)
  - New string `waveform_update_after_download` (values/strings.xml only) for the honest "will update after download finishes" fallback when a retry-exhausted `NotReady` isn't a real error
  - 9 new tests across 6 files: classifier (incl. null-message fallback), cache-hit short-circuit, no-cache, bounded-retry helper, `updateWaveform`'s delete-before-extract wiring, and `requestUpdateWaveform` surviving both a caller-less invocation and an explicitly cancelled caller scope
  - Code review (bmad-code-review, 4 layers): 5 small patches applied (STATE_RESTARTING added to the guard, 3 gap-closing tests, an unrelated CRLF diff-noise cleanup in strings.xml); 4 items deferred (no dedup for concurrent same-mediaId calls, missing size==0 unit test, no cancellation hook on the scope, pre-existing CancellationException-swallowing catch) -- see deferred-work.md
  - Full unit suite: 241 tests / 53 suites, 0 failures; `assembleDebug` OK; manually verified on-device across auto-cache, fresh-download, and manual-button paths

refactor(lyrics): centralize fetch/parse threading on NzikDispatchers.DATA/MEDIA (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - New `LyricsFetchWorker.kt`: extracts LyricsFetcher's ~500-line fetch chain into an instantiable worker (dispatchers as constructor params, default `NzikDispatchers.DATA`/`MEDIA`/`UI` — testable seam, AD-1); network fetch + DAO on DATA, LRC/TTML parsing on MEDIA, all UI callbacks/`Toaster`/`durationProvider` on UI; `LyricsFetcher.kt` is now a thin @Composable wrapper (unchanged signature, `LyricsScreen.kt` untouched)
  - Fixed a pre-existing bug as part of this pass: the Innertube fallback (`tryYouTubeUnsynced`) and the `saveLyricsSafe` retry used to escape the composition scope (fire-and-forget), so a song change could apply a stale callback from the previous song; both are now structured children of the worker's `fetch()`, cancelled together with it
  - `KaraokeLyricsView.kt`/`SyncedLyricsView.kt`: `parseKaraokeLrc`/`parseSyncedSentences`, `gapWindows`/`initialGapWindow` computation moved to `NzikDispatchers.MEDIA`; per-frame scans stay on UI (already lightweight)
  - `SynchronizedLyrics.kt`: dispatcher constructor param (default MEDIA); `update()` now takes the position by value (read on UI) instead of holding an ExoPlayer position-provider lambda
  - `BetterLyrics.kt` (module): `fetchTTML` made public (zero behavior change) so the worker can separate its network fetch (DATA) from TTML parsing (MEDIA); `getLyrics` untouched
  - Found and fixed a genuine MockK limitation while writing tests: MockK cannot reliably mock a suspend function whose own return type is `kotlin.Result<T>` (confirmed with real `ClassCastException`s at runtime, not just theoretical). Since `LrcLib`/`KuGou`/`Innertube` are out of scope to modify, `LyricsFetchWorker` now takes those three calls as constructor-injected lambdas too (same pattern as the dispatchers), so tests supply plain values instead of mocking the objects
  - New tests: `LyricsFetchWorkerTest` (fetch chain, fallback ordering, cancellation, dispatcher placement), `KaraokeLrcParserTest`, `SyncedLrcParserTest`, `SynchronizedLyricsTest`
  - Full architecture decisions in `_bmad-output/planning-artifacts/architecture/architecture-N-Zik-2026-09-16/ARCHITECTURE-SPINE.md` (AD-1)

fix(player): use SupervisorJob for StreamResolver background scope (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `StreamResolver.kt`: module-level `scope` switched from plain `Job()` to `SupervisorJob()` — one unhandled exception in any background task (metadata upsert, album/format cache, FK retry) no longer cancels the shared scope
  - Root cause: with a plain `Job`, the first unhandled child exception cancelled the process-wide scope, silently turning every later `scope.launch` (metadata upserts, format fetches, album/artist caches) into a no-op for the rest of the process lifetime
  - `scope` widened `private` -> `internal` with a KDoc: process-lifetime, must never be cancelled from component lifecycle code (Activity/Service onDestroy), internal for testing only
  - Pre-existing bug found during the #606 NzikDispatchers review (Blind Hunter), tracked in deferred-work.md, fixed here as its own one-line change
  - New `StreamResolverScopeTest.kt` (JUnit5): a failing child does not cancel the shared scope, and a later launch still runs after a sibling failure — a local `CoroutineExceptionHandler` keeps the simulated failure off the thread's uncaught-exception handler
  - Full unit suite: 204 tests / 43 suites, 0 failures; `assembleDebug` OK
  - Follow-up deferred (deferred-work.md): no `CoroutineExceptionHandler` on the scope — an unhandled child exception still reaches the thread's uncaught handler (pre-existing crash path, behavior change beyond this fix)

refactor(visualizer): centralize FFT/waveform capture on NzikDispatchers.VISUALIZER, one loop per session (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `VisualizerHelper.kt` (nextvisualizer module): new `VisualizerSnapshot` (content-based `equals`/`hashCode`) and a `snapshotProvider: (Int) -> VisualizerSnapshot?` functional injection point (`@Volatile`, module can't depend on coroutines) so `getFft()`/`getWave()` return the shared snapshot directly instead of calling `Visualizer.getFft()`/`getWaveForm()` themselves; falls back to the pre-migration direct native call (+ one-time `Timber.w`) if the provider is never wired
  - New `VisualizerCaptureCoordinator.kt` (`app.n_zik.android.extensions.audiobar`): one capture loop per `audioSessionId` on `NzikDispatchers.VISUALIZER` at 60fps, published as a `StateFlow`, started lazily and stopped via `SharingStarted.WhileSubscribed` when idle; a poll-keep-alive proxy collector covers `VisualizerView`'s synchronous `.value`-peek path (which doesn't count as a real Flow collector on its own) so the full-screen visualizer doesn't silently fall back to per-frame native calls when it's the only thing showing
  - `MainApplication.onCreate()`: wires `VisualizerHelper.snapshotProvider` once at startup (not in a composable) since `SeekBarVisualizer` (mini-player) can mount before the full `NextVisualizer` screen ever composes
  - `SeekBarVisualizer.kt`: manual `while(true) { delay(40) }` polling loop replaced with `collectAsStateWithLifecycle()` on the shared flow (cadence now matches `VisualizerView`'s 60fps instead of the old local 25fps); also fixed to read the real captured sampling rate instead of a hardcoded `44100`
  - No painter (`FftBar`, `WfmAnalog`, etc.) or `VisualizerView.kt` changed — their `calc(helper)` contract is untouched; no new Gradle dependency added to the `nextvisualizer` module
  - Review round found and fixed: a buffer-aliasing hazard that could corrupt a shared "immutable" snapshot, an uncaught exception that could permanently freeze a session's capture loop (confirmed happening in this repo's own test run), two non-atomic map races, a `lateinit var` self-cancel race, unbounded per-session map growth, and cross-test mock contamination
  - 9 new/updated unit tests (`VisualizerHelperTest`, `VisualizerCaptureCoordinatorTest`) covering snapshot wiring, fallback, sharing, timeout/keep-alive, session independence, and exception resilience
  - Full architecture decisions in `_bmad-output/planning-artifacts/architecture/architecture-N-Zik-2026-09-16/ARCHITECTURE-SPINE.md` (AD-2)

refactor(threading): introduce NzikDispatchers, a centralized coroutine-dispatcher object (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - New `app.n_zik.android.utils.coroutines.NzikDispatchers`: single source of truth for 5 named dispatchers (UI/PLAYBACK/VISUALIZER/MEDIA/DATA) and 2 named Room executors (ROOM_QUERY_EXECUTOR/ROOM_TX_EXECUTOR); all pool-backed members are `by lazy` so referencing one doesn't eagerly spin up the others (a Kotlin `object` initializes all its properties together on first touch)
  - Absorbed `playback/utils/PlaybackDispatchers.kt` (STREAM_RESOLVER) into `NzikDispatchers.PLAYBACK`; deleted the file and redirected its 8 usages in `StreamResolver.kt`
  - Fixed a latent crash risk found during the migration: `MainActivity.onDestroy()` called `PlaybackDispatchers.STREAM_RESOLVER.close()` on the process-wide singleton executor — since `PlayerServiceModern` (and its `StreamResolver`) can outlive the Activity, this could throw `RejectedExecutionException` on the next stream resolution after any Activity recreation; the close call is removed (NzikDispatchers members are process-lifetime, daemon-threaded, and never explicitly closed)
  - `MyDownloadHelper.kt`: replaced its dedicated `Executors.newCachedThreadPool()` with `NzikDispatchers.DATA`, using `.asExecutor()` for the raw `Executor` media3's `DownloadManager` constructor requires; kept the existing `Semaphore(3)` download-preparation limit; `release()` no longer shuts down a pool it no longer owns
  - `Database.kt`: wired `RoomDatabase.Builder.setQueryExecutor()`/`.setTransactionExecutor()` to the two new named/bounded (4 threads each) executors — same off-main behavior Room's defaults already provided, now visible by name in ANR/profiler traces
  - Hardening from review: exposed the two Room executors through a plain `Executor` SAM wrapper so a caller can't cast to `ExecutorService` and call `.shutdown()` on a process-lifetime pool
  - Tests: `NzikDispatchersTest` (11 cases — aliasing, distinctness, thread naming, PLAYBACK FIFO ordering, executor cast-safety) and `RoomExecutorsTest` (Robolectric, confirms query/transaction executors still run correctly with the new named pools)
  - `VISUALIZER` and `MEDIA` are created but have no consumer yet — wiring the actual hotspots (visualizer FFT capture, lyrics fetch/parsing) onto them is deferred to follow-up work tracked in `deferred-work.md`
  - Full architecture decisions in `_bmad-output/planning-artifacts/architecture/architecture-N-Zik-2026-09-16/ARCHITECTURE-SPINE.md`

fix(ui): migrate slider to Material 3 state API
  - Slider.kt: replace the hidden value-based custom Slider overload with SliderState for Material 3 1.5.0-alpha28 compatibility
  - rememberControlledSliderState synchronizes the animated parent value and recreates the state when the range changes, preserving continuous steps and local magnetism
  - Preserve custom thumb/track rendering, animations, interaction source, value callbacks and drag completion
  - Add SliderStateSynchronizationTest and SliderIntegrationTest for recomposition, range changes, progress semantics, dragging and completion callbacks
  - Verified both tests and assembleDebug; code review completed and functionality confirmed by the user

feat(lastfm): configurable scrobble options — Now Playing toggle, min duration, delay % and cap sliders (issue https://github.com/N-Zik-Group/N-Zik/issues/574)
  - baseline e9acb3210df0bb7a1bdd8bbb25bbe0fa9f24fdef
  - `LastFmPrefs.kt`: 5 new encrypted keys (now-playing toggle, scrobble toggle, min track duration, scrobble delay %, max scrobble delay) + internal `isLastFmSetupKey`/`isLastFmConfigKey` classifiers; defaults preserve the 574 behavior (NP ON, scrobble ON, 30 s / 50 % / 50 s)
  - `LastFmScrobbleManager.kt`: config snapshot read from encrypted prefs, hot-reloaded via `onConfigChanged()` without recreating the manager (pending timer re-armed from now / recomputed while paused, NP never re-sent); Now Playing and scrobble decoupled; job-generation guard aborts stale jobs woken by cancellation; track `duration` forwarded to the API only when > 0 (ExoPlayer `-1` leak, Metrolist parity); an Idle track that becomes eligible while playing is started — NP sent when enabled, even with scrobbling disabled (review round 3)
  - `PlayerServiceModern.kt`: encrypted-prefs listener dispatches the 5 sub-option keys to `onConfigChanged()` and the setup keys (master + session) to (re)setup
  - `LastFmSettingsCard.kt`: Now Playing + Scrobble switches and 3 sliders (min duration 10–60 s step 5, delay 30–95 % step 5, cap 30–360 s step 30) in the existing card; sliders keep local state while dragging and persist once in `onSlideComplete` (no encrypted-write storm); min-duration slider stays visible when scrobbling is off (the gate applies to Now Playing too)
  - `extensions/lastfm/LastFm.kt`: optional `duration`/`duration[0]` on `track.updateNowPlaying`/`track.scrobble` (omitted when ≤ 0, included in the signed map); param assembly extracted into pure `nowPlayingParams`/`scrobbleParams`
  - `ExportSettingsDialog.kt` / `BackupManager.kt` / `ImportSettings.kt`: the 5 new keys added to the export, backup and encrypted-import key lists (9 Last.fm keys now)
  - `values/strings.xml`: 10 new lastfm strings (source of truth only, Crowdin-managed)
  - 13 new unit tests (scrobble options, config hot reload, pause/resume `-1`, generation-guard races, NP-only re-eligibility, param assembly, key classifiers) + `ExportSettingsDialogTest` aligned to the 9-key set — full suite green (174 tests, 0 failures)

feat(lastfm): add Last.fm integration — login, profile picture, now playing, scrobble, love/unlove, backup credentials (issue https://github.com/N-Zik-Group/N-Zik/issues/574)
  - New `extensions/lastfm` Gradle module: Ktor client for `ws.audioscrobbler.com/2.0/` (MD5 `api_sig`) — `auth.getMobileSession`, `track.updateNowPlaying`, `track.scrobble`, `track.love`/`track.unlove`, `user.getinfo`; API key/secret loaded from `local.properties` with `LASTFM_API_KEY`/`LASTFM_API_SECRET` env fallback (gitignored, never committed)
  - `LastFmSettingsCard.kt` (new, `components/settings/`): LAST.FM card in Settings > Account — connect sheet matching the toolbar-overflow/reorder menus exactly (`CustomModalBottomSheet` params + real `ListMenu.Menu` header with white bar), username/password mobile-session login (password masked with show/hide eye toggle, Last.fm error codes 4/6/9/10/13/14/15 mapped to user-friendly strings), profile picture fetched via `user.getinfo` (largest image, `largestImageUrl()`) and shown on the card, disconnect clears session + avatar
  - Playback: `LastFmScrobbleManager` wired in `PlayerServiceModern.kt` — sends now playing on track start and a scrobble after the listened threshold, pause/resume-aware, session-expired (error 9) detection resets the session; enable toggle in the card
  - Menus: love/unlove entries in `SongItemMenu`/`VideoItemMenu`/`PlayerItemMenu` (list + grid variants) through the standard `ListMenuItem`/`GridMenuItem`/`SettingIcon` pipeline, shown only when connected
  - Backup: Last.fm credentials (session key, username, avatar URL, scrobble flag) added to manual export, auto-backup and import behind an opt-in "Include Last.fm identity credentials" option (`ExportSettingsDialog.buildCredentialEntries`, `ExportBackupDialog`, `BackupManager.PREF_INCLUDE_LASTFM`, `AutoBackupSettingsBlock`, `ImportSettings` encryptedKeys)
  - All Last.fm state in encrypted prefs (`lastfmSessionKey`, `lastfmUsernameKey`, `lastfmAvatarUrlKey`, `isLastfmScrobblingEnabledKey`) — unlike RiPlay/Metrolist which store it in plain DataStore
  - 47 unit tests: auth signature, mobile-session error mapping, scrobble delay/pause-resume/session-expired, user-info mapping + largest-image selection, backup credential-entry builder (4 new)

fix(musicbrainz): stop violating MusicBrainz's 1 req/sec limit, add a circuit breaker, stop bulk-fetching on Insights open
  - Root cause bug in `MusicBrainz.kt`: `fetchArtistMetadata`/`fetchAlbumMetadata` each issued TWO sequential HTTP calls (search, then detail lookup) inside a single `makeRateLimitedRequest` block that only delayed once before both — so every single artist/album fetch already fired 2 requests back-to-back, violating MusicBrainz's documented "never more than one request per second" rule by design, independent of any of the cooldown/expiry work above. Fixed by replacing `makeRateLimitedRequest` (wrapped a whole logical operation) with `rateLimitedGet(url)` (wraps every individual HTTP call) and rewriting `fetchArtistMetadata`/`fetchAlbumMetadata`/`fetchArtistRelations` to call it per-request
  - `MusicBrainz.kt`: added a circuit breaker — after 3 consecutive failures the client refuses all further requests for 10 minutes (`circuitOpenUntil`, checked in `rateLimitedGet` before even attempting a request). Applies uniformly to auto on-view sync, manual force-resync, and `MbBackfillWorker` since they all share the one `MBMetadataHelper.Default`/`MusicBrainz` instance — a spammed resync button or a broken worker can no longer keep hammering MusicBrainz once it's clearly down. Exposed via `circuitOpenRemainingMs()`/`MBMetadataHelper.circuitOpenRemainingSeconds()`
  - `InfoAndCommunity.kt`/`AlbumScreen.kt`/`ArtistScreen.kt`: resync now surfaces the circuit-breaker state distinctly instead of the generic failure message — toast "MusicBrainz paused after repeated failures — retry in Xmin" and status line "Paused (too many failures): retry in Xmin" (red, highest priority over Never synced/Sync failed) when the breaker is open, both on auto-sync and manual resync
  - `MusicBrainz.kt`: fixed the User-Agent — per https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting MusicBrainz requires `Application/version ( contact-url )` with a real contact point, not a bare package id; was `n-zik/$appVersion ( com.nevar.nzik )`, now `N-Zik/$appVersion ( https://github.com/N-Zik-Group/N-Zik )`
  - `MbBackfillWorker.kt`: added +/-15min jitter to both the initial 1h delay and the 24h reschedule, per MusicBrainz's explicit guidance to avoid synchronized requests across distributed installations (every install of a new version hitting MB at the same fixed offset)
  - `AlbumInsightsViewModel.kt`: removed the background bulk-backfill loop that MusicBrainz-fetched every "Other albums"/"Singles & EPs" candidate (commonly 10+) just from opening one album's Insights page — that fan-out from a single page view is exactly the bulk-fetch pattern MusicBrainz's rate limiting exists to discourage; MB metadata for those items is now only fetched when the user actually opens that specific album
  - `MusicBrainz.kt`: `fetchArtistMetadata`'s artist-name search query was never URL-encoded (unlike the release-group search, which already used `URLEncoder.encode`) — an artist name containing `&`, `+`, `#`, or similar (e.g. "Guns N' Roses", "Earth, Wind & Fire") would silently break the query and waste a rate-limited request on a lookup that could never match; fixed to encode it the same way
  - `MusicBrainz.kt`/`MainApplication.kt`: the circuit breaker's `circuitOpenUntil` and `consecutiveFailures` are now persisted to SharedPreferences (`mbCircuitOpenUntilKey`, `mbCircuitFailuresKey` in `Preferences.kt`) instead of living only in the in-memory `MusicBrainz` singleton — force-quitting the app during a MusicBrainz outage no longer silently resets the breaker and lets the app start hammering it again; both a fresh failure and a reset-to-zero on success go through the property's persisted setter, so the streak survives restarts either way. The JVM-only `extensions/musicbrainz` module can't read SharedPreferences itself, so this follows the existing `MBNetwork.proxy` pattern: a pluggable `MBCircuitBreakerPersistence` object with `load`/`save` hooks wired from `MainApplication.onCreate()`

improve(insights): album Insights — Singles & EPs section, YTM library enrichment, long-press menus
  - `AlbumInsightsViewModel.kt`: local "Other albums" list now split by MusicBrainz `albumType` (single/EP -> new "Singles & EPs" section, everything else stays "Other albums"); artist resolution falls back to the album's own tracks' `SongArtistMap` relation (`artistTable.findBySongIdDirect`) when name-matching the author text fails (casing, "feat.", collab text)
  - `AlbumInsightsViewModel.kt`: both sections are enriched with albums/singles missing from the local library by fetching the artist's YouTube Music page (`YtMusic.getArtistPage`) and diffing by id/title against what's already known; runs after the local data so the page renders immediately, then a background job MusicBrainz-backfills each "Other albums"/"Singles & EPs" candidate (via `MBMetadataHelper.onAlbumViewed`, same cooldown/rate-limit as the resync feature below) so their rating badge appears once fetched
  - `models.kt`: `AlbumDetailUiState` gained `singlesAndEps: List<Album>`
  - `AlbumInsightsScreen.kt`/`ArtistInsightsScreen.kt`: the "Other albums"/"Local library" grids (manually chunked into `Row`s sized off `LocalConfiguration`) are now a single `LazyRow` — fixes the year/info text getting clipped in narrow grid cells ("fix: year not showing in album insights"); `AlbumInsightsScreen.kt` also gained a "Singles & EPs" card in the same style
  - `AlbumInsightsScreen.kt`/`ArtistInsightsScreen.kt`/`AlbumDetails.kt` (both the legacy `rimusic.ui.screens.album` and current `AlbumScreen.kt` variants): "other albums" items gained a long-press context menu (`OnlineAlbumItemMenu`) with haptic feedback, alongside the existing tap-to-open
  - `strings.xml`: added `mb_insights_singles_eps` ("Singles & EPs")

feat(musicbrainz): resync button, sync status text, and fetch-retry cooldown
  - Root causes of the MusicBrainz 503 log spam, per https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting (server enforces ~1 req/sec per IP): (1) every artist/album view retried the MB fetch immediately whenever the previous attempt had failed, with no backoff; (2) `MusicBrainz.kt`'s `fetchArtistRelations()` (used by the artist Insights "Members & Collabs" section) bypassed the client's own `Mutex`+`delay(1050ms)` rate limiter that already correctly throttles `fetchArtistMetadata`/`fetchAlbumMetadata` — fixed by wrapping it in the same `makeRateLimitedRequest`; (3) `MbBackfillWorker` instantiated its own `MBMetadataHelper()` instead of reusing `MBMetadataHelper.Default`, giving it a separate `MusicBrainz` client with its own independent rate-limit mutex — its daily background backfill could run concurrently with foreground browsing and together exceed the 1 req/sec cap; fixed by pointing it at `MBMetadataHelper.Default`
  - `MBMetadataHelper.kt`: added `RETRY_COOLDOWN_MS` (5min) so a failed fetch isn't retried until the cooldown expires; the genres/tags "already fetched" sentinel also expires after `REFRESH_EXPIRY_MS` (14 days) so a previously-successful sync gets refreshed periodically instead of being fetched once forever; `mbLastFetch` is now persisted after every attempt (success, failure, or a precondition bail-out like a missing/unmatched artist), not just successful ones; `onArtistViewed`/`onAlbumViewed` gained `force: Boolean` (bypasses cooldown/expiry/sentinel) and now return `Boolean` (fetch succeeded/failed) so callers can react
  - `MBMetadataHelper.kt` bugfix: `onAlbumViewed`'s `artistTable.findByNameDirect(albumAuthor)` lookup is fragile (casing, "feat.", collab text — same issue the `AlbumInsightsViewModel.kt` fix below already worked around); when it failed to match, the function returned `false` without ever writing `mbLastFetch`, so the resync button perpetually showed a misleading "MusicBrainz busy" error and the status stayed "Never synced" no matter how many times you retried; fixed with the same fallback (resolve the artist via one of the album's own songs through `SongAlbumMapTable`/`ArtistTable.findBySongIdDirect`) and by recording `mbLastFetch` on every precondition bail-out too
  - `MBMetadataHelper.kt` bugfix (data loss): `onArtistViewed`/`onAlbumViewed` wrote `artist.copy(...)`/`album.copy(...)` on the snapshot read at the *start* of the function, but the MusicBrainz fetch in between can take several seconds under rate-limiting (chained release-group + Wikipedia + inherited-genre lookups can be 2-3 sequential ~1s-throttled requests); `updateReplace`/`update` overwrite the whole row, so any local change made during that window (like/bookmark, dislike, position, etc.) was silently clobbered back to its pre-fetch value; fixed by re-reading the entity immediately before every write and basing `.copy()` on that fresh row instead of the stale one
  - `Artist.kt`/`Album.kt`: new nullable `mbLastFetch: Long?`; `From38To39Migration.kt` extended (still additive-only, not yet released) with `ALTER TABLE Artist/Album ADD COLUMN mbLastFetch INTEGER DEFAULT NULL`
  - `InfoAndCommunity.kt`: new resync icon button before the expand/collapse chevron, swapped for a Material3 `CircularProgressIndicator` while syncing — covers both the automatic on-view sync and a manual resync; status line under the title, always visible (not just when collapsed): "Never synced" / "Last sync: X" / "Sync failed: X" (red), the failed state inferred from `mbLastFetch != null && genres == null`
  - `AlbumScreen.kt`/`ArtistScreen.kt`: wired `lastSyncAt`, `lastSyncFailed`, `isSyncing`, `onResyncClick` (force resync + `Toaster.s`/`Toaster.e` result feedback) into `InfoAndCommunity`; `mbSyncing` state lives in the outer `AlbumScreen`/`ArtistScreen` composables (not `AlbumDetails`/`ArtistOverview`) and is passed down, since the automatic-sync `LaunchedEffect` runs in the outer composable
  - `strings.xml`: added `mb_resync`, `mb_last_sync`, `mb_last_sync_never`, `mb_last_sync_failed`, `mb_resync_success`, `mb_resync_error`

improve(insights): album Insights gains a "Top songs" section (play-time ranked) under the tracklist
  - `SongAlbumMapTable.kt`: new `getTopSongsOfDirect(albumId, limit)` — same songs as `allSongsOfDirect` but ordered by `Song.totalPlayTimeMs DESC` instead of disc position
  - `models.kt`: `AlbumDetailUiState` gained `topTracks` (`tracks` unchanged, still disc-ordered and still backs `AlbumStats.tracksCount`)
  - `AlbumInsightsViewModel.kt`: loads `topTracks` from `getTopSongsOfDirect(albumId)` (no limit — ranks the whole album) filtered to `totalPlayTimeMs >= 1` so never-listened songs are excluded
  - `AlbumInsightsScreen.kt`: section order is now Header → Info → Keywords → Rating → Your listening → Songs (renamed from "Tracklist (%d)", count dropped — was a duplicate of the "Songs" stat already shown in "Your listening") → Top songs (new, `state.topTracks`, play-time order) → Other albums → Links
  - Both the "Songs" and "Top songs" cards reuse the same `SwipeablePlaylistItem` + `SongItem` + rank-overlay pattern as `ArtistInsightsScreen.kt`'s top songs; `likeStatesMap` keyed off `state.tracks` (superset, covers both cards)
  - "Top songs" card hidden via `if (state.topTracks.isNotEmpty())` when the album was never played; "Your listening" / "Songs" gating unchanged (`stats.tracksCount > 0`, i.e. the full tracklist, not just listened songs)
  - Reused existing `mb_stat_songs` ("Songs") and `mb_insights_top_songs` ("Top songs") strings — no new string added

feat(insights): album description fallback + fix main-thread DB crash on album open
  - `Album.kt`: new nullable `description` field, used by the Insights bio card when no MusicBrainz `wikipediaInfo` is available
  - `Database.kt`: DB 38 -> 39; `From38To39Migration.kt` (additive-only, `ALTER TABLE Album ADD COLUMN description TEXT DEFAULT NULL`) + `schemas/.../39.json`
  - `AlbumInsightsScreen.kt`: bio card now reads `album.wikipediaInfo ?: album.description`, moved earlier in the item list (was `wikipediaInfo`-only, appearing after keywords)
  - `AlbumScreen.kt`: fixed crash `IllegalStateException: Cannot access database on the main thread` when opening an album — the description-caching `findByIdDirect`/`updateReplace` calls ran right after, but outside, the `Database.asyncTransaction { }` block, so they executed on the `LaunchedEffect`'s main-thread coroutine; moved inside `asyncTransaction { }` to run on the transaction executor
  - `strings.xml`: removed now-unused `mb_stat_likes`; renamed `mb_stat_tracks` -> `mb_stat_songs` (matches the earlier `mb_insights_top_tracks` -> `mb_insights_top_songs` rename)
  - Root cause: DB write logic added outside its transaction scope instead of inside the existing `asyncTransaction` block

improve(insights): artist insights rework — bio fallback, section reorder, top albums, richer stats
  - `ArtistScreen.kt`: when the artist page is viewed, the YouTube description is now stored in `artist.description` (only when non-blank and different) so the Insights page can reuse it as a bio fallback without refetching the artist page
  - `ArtistInsightsScreen.kt`: biography now reads `artist.wikipediaBio ?: artist.description` and was moved to the second position (right after the header) instead of appearing last; it was previously blank when no Wikipedia bio existed
  - `ArtistInsightsScreen.kt`: sections reordered to Header → Biography → Members & Collabs → Tags & Genres → Popularity → Your listening → Top songs → Top albums → Local library → Links
  - `ArtistInsightsScreen.kt`: new "Top albums" section under "Top songs" — top 5 albums by total play time, rendered as list-mode `AlbumItem` (cover size = `Dimensions.thumbnails.song`, same as songs) with a rank overlay, tap to open the album, long-press for the album menu
  - `ArtistTable.kt`: new `getTopAlbumsByArtist(artistId, limit)` — joins `Album → SongAlbumMap → SongArtistMap → Event`, `GROUP BY A.id`, keeps only listened albums (`HAVING IFNULL(SUM(E.playtime),0) >= 1`), ordered by play time desc
  - `ArtistTable.kt`: `getAlbumsByArtist` now returns only albums that have at least one listened song (added `JOIN Song S` + `S.totalPlayTimeMs >= 1`) — the "Local library" section no longer lists albums whose songs were never played
  - `ArtistTable.kt`: new `getBookmarkedAlbumsCountByArtist(artistId)` — counts distinct albums of the artist with `bookmarkedAt IS NOT NULL`
  - `models.kt`: `ArtistDetailUiState` gained `topAlbums`; `ArtistStats` gained `bookmarkedAlbumsCount`
  - `ArtistInsightsViewModel.kt`: loads `topAlbums` (limit 5) and the bookmarked-albums count, and passes them into the state
  - `ArtistInsightsScreen.kt`: "Your listening" stats row now shows 5 items (Listens, Albums, Liked songs, Bookmarked, Total time); each `StatItem` uses `Modifier.weight(1f)` + `padding(horizontal = 4.dp)` and the label is single-line `basicMarquee` + `fillMaxWidth` + centered, so long labels scroll and never overflow/overlap
  - `strings.xml`: renamed `mb_insights_discography` → `mb_insights_local_library` ("Local library", section icon now `R.drawable.library`), `mb_insights_top_tracks` → `mb_insights_top_songs`; added `mb_insights_top_albums`, `mb_stat_liked_songs` ("Liked songs") and `mb_stat_bookmarked_albums` ("Bookmarked albums"); the album insights keeps the shared `mb_stat_likes` untouched

fix(insights): "Top tracks" no longer lists never-listened songs
  - `ArtistInsightsViewModel.kt`: `getTopSongsByArtist` caps at the limit regardless of play time, so songs at 0ms (never listened) were filling the "Top tracks" list; filtered the result to `totalPlayTimeMs >= 1` so only actually listened songs are shown
  - Empty result now hides the whole section (existing `if (state.topTracks.isNotEmpty())` in `ArtistInsightsScreen.kt`)
  - Did not change the DAO query so the "liked songs" stat (which reuses `getTopSongsByArtist`) is unaffected

improve(ui): align "Info and Community" / "Insights" chevrons with section header icons
  - `InfoAndCommunity.kt`: removed the explicit `Modifier.size(24.dp).padding(end = 12.dp)` on both chevrons (chevron up/down + chevron_forward) so they use the default size and sit flush with the row's content edge, matching the artist/album section header icons (dice/play/chevron_forward in `ArtistScreen.kt`)
  - Whole title rows stay clickable (expand/collapse + navigate to Insights) with the rounded ripple; text placement already identical (`m.semiBold`, `weight(1f)`, 16/24/8 padding = `sectionTextModifier`)
  - Removed the now-unused `androidx.compose.foundation.layout.size` import

feat(musicbrainz): port "Info and Community" + Insights from RiPlay with MusicBrainz metadata
  - `extensions/musicbrainz` (new Gradle module): MusicBrainz API client (artist/album lookup, release year/country, keywords, external links, Wikipedia bio) + `cleanWikipediaText()`; wired via `implementation(projects.musicbrainz)` in `ComposeN-Zik/build.gradle.kts`; `MainApplication.kt`: network + Timber setup
  - DB 37 -> 38: `AlbumTable.kt` / `ArtistTable.kt` additive MusicBrainz columns (year, country, keywords, links, Wikipedia bio) + `Converters.kt` list/string converters; `From37To38Migration.kt` is additive-only (ALTER TABLE ADD COLUMN, no data loss) + `schemas/.../38.json`
  - Approved legacy exception: additive fields only in `models/Album.kt` + `models/Artist.kt`; new `ARTIST_INSIGHTS_ROUTE` / `ALBUM_INSIGHTS_ROUTE` in `AppNavigation.kt`
  - New `ArtistScreen.kt` / `AlbumScreen.kt` (`app.n_zik.android.components.ui.screens.*`): "Info and Community" section between header and first content; titles use the screens' own `sectionTextModifier` (16dp horizontal, 24/8 vertical, `m.semiBold`) so "Info and Community" / "Insights" align exactly with "Top songs" / "Songs" on both sides
  - `InfoAndCommunity.kt` (new): collapsible section — rating bar + year/country `InfoBar`, `KeywordChips`, external links with per-service logos (Apple Music, Deezer, Discogs, Facebook, Instagram, SoundCloud), Wikipedia bio with Crowdin translation; open by default; whole title rows clickable with rounded ripple
  - `insights/` (new): `ArtistInsightsScreen` / `AlbumInsightsScreen` — top artists/albums/songs grids (chunked `Row` + `weight(1f)` => no right gap), position overlay white `s.semiBold` on `overlay` background (same as `StatisticsPage`), star rating badge on album covers, full-screen `Loader()` while loading, `Dimensions.bottomSpacer` bottom padding
  - `strings.xml`: +28 `mb_*` keys
  - Coil unification: `ChangeAlbumBrowseIdDialog` / `ChangeArtistBrowseIdDialog` / `ChangePlaylistBrowseIdDialog` / `ExportCacheDialog` / `MusicAudioSearchOverlay` / `NZikWidgetManager` / `PlaylistWidgetManager` now use the shared `ImageCacheFactory` (single loader, `crossfade(false)`) instead of ad-hoc `AsyncImage` / `SingletonImageLoader`

perf(player): complete palette fade, smooth slide/skip, M3E skip mask and review fixes
  - `MainActivity.kt`: appearance init/fade rework — `computeAppearance()`, `updateAppearance(animateGlobal)`, `setDynamicPalette(animateTheme)`, global fade only on explicit theme/dark switch, root `background0` always fades with `animateColorAsState(350ms, FastOutSlowInEasing)` to avoid app-wide lag on track changes
  - `AnimatedAppearance.kt` (new): global appearance fade helper + `appearanceRetarget(current, fadeFrom, target)` pure model — mid-fade retargets now restart from the currently displayed appearance instead of the previous target, removing visible color jumps on rapid theme changes
  - `PaletteFade.kt` (new): scoped player/mini-player palette + typography fade with `PaletteFadeModel.retarget(...)` — track changes animate only the player subtree while the global palette switches in one step
  - `CustomBottomSheet.kt`: added shared `PLAYER_SHEET_HANDOVER_PROGRESS = 0.45f` and used it in `playerContentFade()` / `miniPlayerFade()` — `Player.kt`, `MiniPlayer.kt`, `DurationIndicator.kt` now use the same constant instead of duplicated magic numbers
  - `PlayerState.kt` + `Player.kt` + `MiniPlayer.kt` + `DurationIndicator.kt`: position polling is gated on the hand-over threshold — the full player polls when visible, the mini-player polls when visible, no dual per-frame polling during the transition
  - `SlideHelpers.kt` (new): shared player pager fling/settle helpers — `SLIDE_SETTLE_DURATION_MS = 700`, `SLIDE_SNAP_POSITIONAL_THRESHOLD = 0.25f`, `flingBehaviorFor(...)` using `PagerDefaults.flingBehavior(snapAnimationSpec = tween(700, FastOutSlowInEasing))`, `itemZIndexFor(...)`, `shouldPlaySettledPage(...)`, `slideSettledPageEffect(...)`, `playerThumbnailCover(...)` with separate `scaleStart` / `alphaStart`
  - `Player.kt`: all 4 player pager layouts use the shared slide helpers; programmatic `LaunchedEffectScrollToPage(...)` now animates 700ms only when foreground + player visible + no skip, and falls back to instant `scrollToPage` when hidden, background or lyrics are shown
  - `Player.kt`: landscape thumbnail `playerThumbnailCover(...)` restored the original visual values — alpha starts at `0.9f`, scale starts at `0.85f`
  - `Player.kt`: pending mini-player action (Lyrics/Visualizer) no longer gets clobbered by the saved-state restore on hand-over — `suppressSavedRestoreOnNextOpen` is set when the pending action consumes the tap, then cleared on the next visible/hidden transition
  - `AnimatedM3EBackground.kt`: skip mask now uses `skipToken = currentMediaItem?.mediaId` — immediate hide on token change, 150ms fade-out, 600ms show, colors read directly from the target palette (no async `displayedColors`), `skipMaskActive` cleared when token becomes null or first token appears, preventing a stuck-invisible M3E background
  - `BlurredCover.kt`: conditional `Modifier.blur(...)` + conditional `graphicsLayer(...)` — no per-frame blur/layer invalidation when idle; removed the old unused `BlurTransformation` dead code path
  - Review cleanup: removed unused `PagerDefaults` import in `Player.kt`, removed duplicate `mutableStateOf` import in `AnimatedM3EBackground.kt`, renamed unused `AnimatedAppearance` content parameter to `_`, tidied slide helper imports
  - Tests added/updated: `AppearanceFadeTest` (+`appearanceRetarget`), `PaletteFadeModelTest`, `SlideHelpersTest` (+`thumbnailCoverTransform`), `AnimatedM3EBackgroundTest`, `CustomBottomSheetThresholdTest` (new), `PauseOnHeadphoneDisconnectTest` (default=true + true/false), `LyricsDecisionMakerTest` (+Synced empty DB), `ShufflerTest` (+max queue cap with MockK)
  - Build + tests: `./gradlew :ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:testDebugUnitTest` green (100 tests, 0 failures)

feat(settings): unify "Max number of items" options with Custom and Unlimited
  - `MaxStatisticsItems.kt` + `TopPlaylistItems.kt`: both enums now share `10,20,30,40,50,70,90,100,150,200` + `Custom` + `Unlimited`; single resolver `toInt(customValue)` (Custom → value clamped to >= 1, Unlimited → Int.MAX_VALUE); `optionLabel()`/`displayName()` via string resources (no hardcoded strings)
  - `strings.xml`: new keys `max_items_custom`, `max_items_custom_value`, `max_items_unlimited`
  - `Preferences.kt`: new Int keys `maxStatisticsItemsCustomValueKey` + `MaxTopPlaylistItemsCustomValueKey` (default 10)
  - `AIRecommendationSettings.kt`: both settings dialogs list the 12 options; selecting `Custom` opens `SettingsInputDialog` (digits only, <= 9 digits, min 1); reset-to-defaults covers the new custom values
  - `StatisticsPage.kt`: parental-control x5 fetch limit computed in Long and capped (no Int overflow with Unlimited/large Custom); `remember` keys include the custom value
  - Migrated all `MaxTopPlaylistItems` consumers: `HomeSongs.kt`, `HomeQuickPicks.kt`, `PeriodSelector.kt` ("Top %s of ..." shows resolved value / "Unlimited"), automotive `AutoSessionCallback.kt`, `SongsBrowseHandler.kt`, `PlaylistDetailHandler.kt`
  - `MaxItemsLimitsTest.kt` (new, JUnit5): 6 tests on `toInt(customValue)` for both enums (numeric mapping, Custom floor, Unlimited = Int.MAX_VALUE)
  - Back-compatible: stored enum names remain valid, existing user settings keep working

fix(statistics): header totals reflect the full listening period instead of the capped list (issue https://github.com/N-Zik-Group/N-Zik/issues/786)
  - `EventTable.kt`: new DAO `countDistinctSongsPlayedBetween(from, to = now): Flow<Int>` — counts distinct `song_id` in `play_events` over the selected period, uncapped
  - `StatisticsPage.kt`: header "songs heard" now reads the new uncapped `distinctSongsPlayedCount` flow instead of the capped `songs.size`; header "time spent" now uses the full-period `getTotalPlayTimeBetween` total (`totalPlayTimes`)
  - `StatisticsPage.kt`: removed the dead `totalPlayTimesSongs`/`songIds` block that recomputed listening time over the capped top-N song list — root cause of the undercount
  - `EventTableTest.kt` (new, Robolectric, in-memory Room): 4 tests covering distinct-song count and total play time with and without out-of-period events
  - Root cause: header aggregates were computed from the top-N list limited by the "Max number of items" setting, so totals were wrong for any user with more than N distinct songs in the period


BETA 8.0.0
fix(quickpicks): load survives page switches — shared scope and shared state instead of composable scope
  - `HomeQuickPicksState.kt`: added `companion object` holding `loadScope = CoroutineScope(SupervisorJob() + Dispatchers.IO)`, `@Volatile sharedLoadJob` and `@Volatile sharedRefreshPending` — the load no longer lives in the composable scope, so it is not cancelled on navigation
  - `HomeQuickPicksState.kt`: removed the `scope: CoroutineScope` constructor param; `load()`, `refresh()` and the phase 2 `dbJob` now run on `loadScope`; `rememberHomeQuickPicksState` no longer calls `rememberCoroutineScope()`
  - `HomeQuickPicksState.kt`: `homePageInit` changed from `rememberPreference` (per-composable SharedPreferences wrapper) to `persist<HomePage>(tag = "home/quickpicks/homePageInit")` (activity-level persistMap) — a background load's writes are visible to the next composable instance, so returning does not trigger a reload
  - `HomeQuickPicksState.kt`: `loadData()` guard-skip path now repairs the `loadedQuickPicks` flag when `trendingList` is non-empty (DB observer cancelled before its first emission) — prevents an endless skeleton
  - `HomeQuickPicksState.kt`: removed unused `rememberPreference` import, added `SupervisorJob` import
  - Behavior: switching pages mid-load no longer cancels it; on return an in-flight load keeps running (honest skeleton via shared `loadedData`/`homePageInit`) and data appears without a reload; a finished load renders instantly
  - Root cause: the load was launched in the composable scope (`rememberCoroutineScope()`), which is cancelled on page change — the job died and the partial persisted state forced a full reload on re-entry

fix(home): back navigation no longer resets Quick Picks — in-app arrow, system back, home-jump option, and PTR indicator
  - `AppHeader.kt` (legacy, targeted edit approved): in-app back arrow now uses a structural check (`currentBackStackEntry != null && previousBackStackEntry != null`) instead of a RESUMED lifecycle gate — the entry can legitimately be non-RESUMED right after a transition, which used to silently swallow the click; Timber.d on every path
  - `HomeScreen.kt`: same structural check in the system `BackHandler`
  - `AppHeader.kt`: home-jump option (`disableNavigationBackStackKey`) now does `popBackStack(NavRoutes.home.name, inclusive = false)` to restore the existing home entry with its saved state; falls back to the old `navigate(home) { popUpTo(home) { inclusive = true } }` only when home is not on the stack
  - Root cause (home jump): the old path destroyed the home entry and rebuilt it, recomposing Quick Picks from scratch and wiping the in-memory persisted state — full reset + reload on every jump-to-home
  - `HomeQuickPicksState.kt`: `loadData()` became `loadData(force: Boolean = false): Boolean` — returns false when the guard skips (loadedData present + homePageInit non-null), true on a real load
  - `HomeQuickPicksState.kt`: `load()` is now the single owner of the pull-to-refresh indicator — sets `refreshing = true` only when the guard will not skip (startup, explicit pull, deferred reload) and clears it in `finally` including after the deferred reload
  - `HomeQuickPicksState.kt`: removed the two `refreshing.value = true` from `refresh()` — the indicator no longer flashes on page change (no-op load); shown on startup, manual pull, and deferred refresh
  - Regression fixed: the earlier single-flight fix set `refreshing = true` unconditionally in `load()` even when the guard skipped, making the PTR spinner appear on every page change

test(quickpicks): extract load guard into a pure function + full truth-table unit test
  - `HomeQuickPicksState.kt`: extracted `shouldSkipQuickPicksLoad(loadedData, homePagePresent)` as a top-level pure function — previously inlined in `loadData()` and the phase 3 merge, now used by both
  - `QuickPicksLoadGuardTest.kt` (new, `ComposeN-Zik/src/test/.../home/quickpicks/`): 4 JUnit5 cases covering the full truth table — skip only when loadedData AND homePagePresent, load on fresh install / missing home data / missing loaded flag
  - 4/4 green; 13 pre-existing test failures (Shuffler, LyricsDecisionMaker, PauseOnHeadphone) proven unrelated via `git stash` on a clean tree

feat(sync): disable YTB sync toggle in non-dev builds
  - `AccountsSettings.kt`: added `import app.n_zik.android.BuildConfig`
  - `AccountsSettings.kt`: `OtherSwitchSettingEntry` for sync toggle now has `enabled = BuildConfig.BUILD_TYPE in listOf("debug", "dev")` — grisé (alpha 0.5f) et non cliquable en build full/minified/beta/foss
  - `AccountsSettings.kt`: `isYouTubeSyncEnabled()` retourne `false` si `BUILD_TYPE` pas dans `["debug", "dev"]` — gate de securite pour tous les consommateurs (SyncYTMusicUtils.kt, Utils.kt)
  - La preference `enableYouTubeSyncKey` reste intacte, pas de suppression de cle
  - La section sync reste visible mais le toggle principal est desactive

feat(player): add in-app queue overlay accessible from mini player long-press
  - `MiniPlayerQueueInterceptor.kt` (nouveau, `app.n_zik.android.core.navigation`): intercepte la navigation vers la route "queue" déclenchée par le long-press du miniplayer via `addOnDestinationChangedListener` + `popBackStack()` synchrone — la route queue n'est jamais composée
  - `MiniPlayerQueueOverlay.kt` (nouveau, `app.n_zik.android.components.player`): copie exacte du panneau de queue inline du player (Player.kt:2315-2482) — panel resizable avec drag-to-resize, scrim animé, handle 48dp avec `background0.copy(alpha)` (Modern=0.5, Essential=1), `QueueToolBar` avec `graphicsLayer` animation (slide+fade), nav bar strip `background1`, fond piloté par la préférence `queueTypeKey` (Modern=Transparent, Essential=background2)
  - `MainActivity.kt`: wiring — `showQueueOverlay` state, `DisposableEffect` pour attach/detach de l'intercepteur, composition de `MiniPlayerQueueOverlay` après le player sheet, fermeture de l'overlay quand `currentMediaId == null`, intégration dans le `nestedScrollConnection` (scroll-hide désactivé quand overlay ouvert)
  - `AppHeader.kt`: la route "queue" est maintenant traitée comme "home" dans le calcul de `isHome` — la flèche de retour in-app ne s'affiche plus quand la queue overlay est ouverte
  - `MiniPlayerQueueInterceptorTest.kt` (nouveau): 4 tests JUnit5 + MockK — interception, idempotence, attach/detach, route non-queue ignorée
  - Le panneau de queue utilise `Queue`, `QueueToolBar`, `QueueArrow`, `Discover`, `Repeat`, `ShuffleQueue` — mêmes composants que le player, même `QueueToolBarState` global

fix(player): miniplayer no longer blocks clicks on the bottom navigation bar
  - `CustomBottomSheet.kt`: moved the animated card clip (`graphicsLayer { shape = CardClipShape; clip = true }`) BEFORE the drag `pointerInput` in the outer Box modifier chain, with an explanatory comment
  - Root cause: commit d3a3bc9f3a ("fix : miniplayer laggy") turned the sheet into a full-screen Box with an unconditional `pointerInput(detectVerticalDragGestures)`; Compose hit-test walks the modifier chain outside-in and records the pointer input before reaching the clip layer, so the clip did not restrict the input area
  - When collapsed, the translated box input bounds (from the miniplayer top down to the screen bottom) covered the entire navbar strip, stealing taps on the bottom navigation tabs
  - With clip→input ordering the hit area becomes the animated card rect itself: only the miniplayer band (65dp, 16dp side insets) when collapsed, full screen when expanded — navbar tabs are clickable again
  - Drag gestures unchanged (expand / collapse / dismiss by velocity, `performFling` untouched); in-progress drags are unaffected because the pointer is acquired by the gesture after touch slop without per-frame re-hit-testing

feat(ui): smooth color palette fade transition on music change
  - `ColorPalette.kt`: Added `lerpTo()` extension function — interpolates all 14 color fields (background0-4, accent, onAccent, red, blue, text, textSecondary, textDisabled, iconButtonPlayer) using Compose `lerp()` with `isDark` from target
  - `ColorPalette.kt`: Added `import androidx.compose.ui.graphics.lerp` for color interpolation
  - `MainActivity.kt`: Modified `setDynamicPalette()` — replaces instant `appearance = appearance.copy(colorPalette = ...)` with stepped fade animation (12 steps × 33ms = ~400ms total)
  - `MainActivity.kt`: All 4 palette code paths (bitmap success, bitmap null, palette null, URL null/violet fallback) now use consistent 12×33ms fade
  - `MainActivity.kt`: Typography set immediately at start of transition (cheap), only colors animated per-step to reduce recomposition scope
  - `MainActivity.kt`: `paletteJob.value?.cancel()` before each new animation — cancels in-flight transition if music changes rapidly (skip scenario)
  - `MainActivity.kt`: Added `import kotlinx.coroutines.delay` for step timing
  - Pattern: Uses `delay(33)` loop instead of `Animatable.animateTo{}` — avoids per-frame `appearance` update that caused player lag in debug builds
  - No lag confirmed: discrete steps reduce recomposition frequency from ~60fps to ~30fps while maintaining visual smoothness

feat(ui): synced toolbar background fade with scroll gesture on home screens
  - `HomeSongsScreen.kt`: Replaced local `nestedScrollConnection` header system with `graphicsLayer` alpha fade — background fades out progressively as toolbar scrolls away, fades back in on return
  - `HomeAlbum.kt`: Same pattern — `nestedScrollConnection` + `headerOffset` + `headerAlpha` via `derivedStateOf` + `graphicsLayer { alpha = headerAlpha }` on header background
  - `HomeArtist.kt`: Replaced `AnimatedVisibility` + `isToolbarVisible` (snapshotFlow-based) with `nestedScrollConnection` + `header` lambda + `graphicsLayer` alpha fade on background
  - `HomeLibrary.kt`: Same as HomeArtist — unified scroll-driven toolbar fade across all home screens
  - All 4 files: Background uses `graphicsLayer { alpha }` (GPU-accelerated, no recomposition) instead of `background(color.copy(alpha = ...))` (caused lag)
  - All 4 files: `derivedStateOf` reads `headerOffset` directly (no state key in `remember` to avoid per-frame recomposition)
  - `TabHeader.kt`: Minor cleanup

fix(innertube): locale-aware artist name parsing — handle "et", "and", etc. as separators
  - `Runs.kt`: Added `ArtistConjunctions` object (default `listOf("and")`) and `splitArtistsByConjunction()` function matching Metrolist's pattern — splits artist runs on locale-aware conjunctions + "&"
  - `Runs.kt`: Added `clean()` extension to drop non-artist first groups from subtitle runs
  - `Runs.kt`: Fixed `splitBySeparator()` to use `trim()` instead of exact match on " • " — handles bullet variants without surrounding spaces
  - `Utils.kt`: `parseArtists()` now splits on conjunctions from `ArtistConjunctions` + filters metadata (years, type labels like "Song"/"Album", "monthly audience", views/plays/likes/subscribers, durations, NBSP)
  - `Utils.kt`: Added `splitArtistNames()` utility for downstream menu splits on stored `artistsText` strings
  - `MainApplication.kt`: Initializes `ArtistConjunctions.conjunctions` from `R.string.and` at startup (locale-dependent: "et" FR, "and" EN, "und" DE, etc.)
  - `values/strings.xml`: Added `<string name="and">and</string>` — Crowdin handles translations for other locales
  - `SongItemMenu.kt`, `PlayerItemMenu.kt`, `VideoItemMenu.kt`, `AlbumItemMenu.kt`, `ImportSongsFromServices.kt`, `VideoOrSongInfoScreen.kt`: All downstream `.split(",", "&")` replaced with `.splitArtistNames()`
  - `Database.kt`: Kept existing "et"/"and" safety-net filter as defense-in-depth
  - Root cause: YouTube/InnerTube returns locale-specific conjunction separators (e.g. "Artist1 et Artist2" in French) — previous code only split on "&" and "," causing "ET" to be treated as an artist name linked to a random artist

feat(ui): unified player/mini-player and improved swipe-to-dismiss
  - `MainActivity.kt`: Replaced separate AnimatedVisibility(MiniPlayer) and CustomModalBottomSheet(Player) with a unified `CustomBottomSheet` component to prevent animation jumps when transitioning
  - `CustomBottomSheet.kt`: Added custom dragging handler to support both the collapsed MiniPlayer and expanded Player in a single gesture, using a continuous `progress` factor for padding and height compensations
  - `MiniPlayer.kt`: Removed internal `pointerInput` vertical drag to let the parent `CustomBottomSheet` handle all drag gestures
  - `PlayerSheetState.kt`: Fixed midpoint calculation bug `dismissedBound + (collapsedBound - dismissedBound) * 0.80f` to make downward dismiss swipes highly sensitive and fluid without rebound logic errors
  - `MainActivity.kt`: Restored old `disableClosingPlayerSwipingDown` preference support in `onDismiss` block. Swipe to dismiss now properly calls `binder.player.stop()`, `binder.player.clearMediaItems()`, and `context.stopService()` like the old `hidePlayer()` implementation did.

fix(download): restore upsertSongInfo for album/artist metadata during downloads
  - `StreamResolver.kt`: Reordered `upsertSongInfo` before `upsertSongFormat` in `processForDownload()` — `upsertSongFormat` was setting `justInserted = videoId` which caused `upsertSongInfo` to skip via guard
  - `StreamResolver.kt`: Removed duplicate `upsertSongInfo` call after `upsertSongFormat`
  - Root cause: `justInserted` guard in `upsertSongInfo` was preventing metadata fetch during downloads
  - Result: Album songs, artist pages, and song metadata now properly saved during download

fix(download): separate streaming and downloading resolvers + improve download speed
  - `StreamResolver.kt`: Created `createDownloadDataSourceFactory()` dedicated to downloads, separate from streaming resolver
  - `StreamResolver.kt`: Created `processForDownload()` that calls `InnerTubeXPlayer.playerResponseForPlayback()` directly — no session change retry, no fetchFormatIfMissing, no upsertSongInfo
  - `MyDownloadHelper.kt`: Uses `createDownloadDataSourceFactory()` instead of `createDataSourceFactory()`
  - Root cause: Streaming resolver's session change retries were canceling download requests, adding ~200ms+ per download
  - Pattern: Same separation as Metrolist's `DownloadUtil.kt` and Cubic Music's `MyDownloadHelper.createDataSourceFactory()`

fix(download): stop/delete download on re-click + fix notification progress
  - `MyDownloadHelper.kt`: `handleDownload()` now checks `STATE_DOWNLOADING/QUEUED/RESTARTING` — stops and removes download if re-clicked
  - `MyDownloadService.kt`: Notification now shows actual download progress percentage (`bytesDownloaded/contentLength`) instead of indeterminate spinner for individual downloads

fix(session): restore ghost cookie fetch for session handling
  - `Store.kt`: Restored `fetchIfNeeded()`, `getCookie()`, and `DEFAULT_COOKIE` for YouTube watch page cookie fetch
  - `Store.kt`: Restored `ghostResponseHeaders`, `ghostResponseBody`, `cookie` fields
  - `Innertube.kt`: Removed `YOUTUBE_COUNTRIES` locale validation — simplified `applyLocale()` to use device country directly

perf(like): centralized like state cache — batch SQL queries instead of N per-item queries
  - `LikeStateManager.kt` (new): Centralized manager for song like states with `getLikeStates(songIds)` returning `Flow<Map<String, Boolean?>>` — 1 SQL query for N songs instead of N queries
  - `SongTable.kt`: Added `getLikeStatesForSongs(songIds)` query returning `Flow<List<SongLikeState>>` for batch loading
  - `SongLikeState.kt` (new): Data class for Room query results (songId, likeState)
  - `SongItem.kt`: Added `isLiked: Boolean? = null` parameter — when provided, skips individual DB query; uses `getLikedIcon()`/`getDislikedIcon()` instead of `getLikeState()` to never show unlike icon
  - `StatisticsPage.kt`: Combined songs + like states loading via `flatMapLatest` — icons appear immediately with list, no delay
  - `HomeSongs.kt`, `OnDeviceSongs.kt`, `HomeQuickPicksSections.kt`, `HomeQuickPicksComponents.kt`, `HistoryList.kt`, `LocalPlaylistSongs.kt`, `PlaylistSongList.kt`, `AlbumDetails.kt`, `ArtistDetails.kt`, `ArtistLocalSongs.kt`, `Queue.kt`, `MoodList.kt`: Batch like state loading via `LikeStateManager.getLikeStates()`
  - `OnlineSearch.kt`, `Podcast.kt`, `LocalSongSearch.kt`, `HomePage.kt`: Migrated from legacy `SongItem` to new `SongItem` with `isLiked` parameter
  - `SwipeablePlaylistItem.kt`: Restored in StatisticsPage after temporary removal
  - Performance: 50 visible items = 1 SQL query instead of 50, scroll lag eliminated

fix(statistics): show bookmark/follow icons for artists, albums, and playlists
  - `StatisticsPage.kt`: Changed `ArtistItem` to use overload with `artist = artists[it]` — automatically queries `likeState` from DB for follow icon
  - `StatisticsPage.kt`: Changed `AlbumItem` to use overload with `album = albums[it]` — automatically queries `likeState` from DB for bookmark icon
  - `StatisticsPage.kt`: Added `isYoutubePlaylist = playlists[it].playlist.isYoutubePlaylist` to `PlaylistItem` — shows bookmark icon for YouTube playlists
  - `StatisticsPage.kt`: Added `showAuthors = true` to AlbumItem for consistent display with HomeAlbum

fix(statistics): fix crash when scrolling with large numbers in statistics screen
  - `StatisticsPage.kt`: Replaced `Int.MAX_VALUE` limit in `totalPlayTimesFlow` with new optimized SQL query `getTotalPlayTimeBetween()` — was loading ALL songs from database into memory
  - `StatisticsPage.kt`: Replaced N+1 query in `LaunchedEffect` (one DB call per song) with single SQL query `getSongsTotalPlayTimeBetween()` for batch playtime calculation
  - `EventTable.kt`: Added `getTotalPlayTimeBetween(from, to)` query — calculates total playtime directly in SQL without loading all Song objects
  - `EventTable.kt`: Added `getSongsTotalPlayTimeBetween(songIds, from, to)` query — calculates playtime for specific songs in one query instead of N individual queries
  - `SongItem.kt`: Fixed missing `LaunchedEffect` import that caused compilation error
  - `SongItem.kt`: Fixed Compose state delegate issue — replaced `by remember(key) { mutableStateOf() }` with `remember { mutableStateOf() }` to resolve setValue delegate error
  - Root cause: `Int.MAX_VALUE` limit caused OOM crash, N+1 queries caused lag during scroll
  - Build verified: `./gradlew :ComposeN-Zik:assembleDebug` BUILD SUCCESSFUL

feat(settings): add disliked tab to albums/artists settings dialogs + fix theme
  - `Preferences.kt`: Added keys `homeAlbumsDislikedToolbarOrderKey`, `homeArtistsDislikedToolbarOrderKey`, `showDislikedAlbumKey`, `showDislikedArtistKey`
  - `HomeAlbumsSortSettingsDialog.kt`: Added Disliked tab with sort options, dynamic theme (accent color for selected tab, textSecondary for unselected)
  - `HomeArtistsSortSettingsDialog.kt`: Added Disliked tab with sort options, dynamic theme
  - `HomeAlbumsToolbarSettingsDialog.kt`: Added Disliked tab with toolbar buttons, dynamic theme
  - `HomeArtistsToolbarSettingsDialog.kt`: Added Disliked tab with toolbar buttons, dynamic theme
  - `HomeAlbumsSettingsDialog.kt`: Added Disliked option with heart_outline icon, dynamic toggle
  - `HomeArtistsSettingsDialog.kt`: Added Disliked option with heart_outline icon, dynamic toggle
  - `HomeSongsSortSettingsDialog.kt`: Fixed dynamic theme (accent color for selected tab)
  - `HomeSongsToolbarSettingsDialog.kt`: Fixed dynamic theme
  - `HomeLibrarySortSettingsDialog.kt`: Fixed dynamic theme
  - `HomeLibraryToolbarSettingsDialog.kt`: Fixed dynamic theme
  - All dialogs: Added HorizontalDivider with dynamic color between tabs and content
  - All dialogs: Added TabRowDefaults.SecondaryIndicator with accent color for tab indicator
  - `HomeAlbum.kt`: Connected showDislikedAlbumKey and homeAlbumsDislikedToolbarOrderKey
  - `HomeArtist.kt`: Connected showDislikedArtistKey and homeArtistsDislikedToolbarOrderKey

feat(quickpicks): avoid showing wrong like icon during database load
  - `SongItem.kt`: Added likeStateLoaded flag to track when database has returned value, only show dislike icon after database has loaded

feat(ui+db): enhance item components, menus, and add DB migration v36→v37
  - `StatisticsPage.kt`: Enhanced statistics page with new UI components
  - `EventTable.kt`: Added getTotalPlayTimeBetween and getSongsTotalPlayTimeBetween queries

feat(ui): add bookmark/dislike indicator icons on artist, album, and playlist items
  - `ArtistItem.kt`: Real-time like state via `Database.artistTable.likeState()` Flow, shows bookmark (favoritesIcon) or bookmark_slash (red) on thumbnail BottomStart with absoluteOffset, clip moved to inner Box to prevent icon clipping
  - `AlbumItem.kt`: Same pattern via `Database.albumTable.likeState()` Flow, bookmark/bookmark_slash icon on thumbnail
  - `PlaylistItem.kt`: Bookmark icon shown when `isYoutubePlaylist == true` (playlist saved locally), clip moved to inner Box, uses `Database.playlistTable.findByBrowseId()` for online playlists to check bookmark status
  - `LocalArtistItemMenu.kt`: Added bookmark icon on artist thumbnail in menu display, moved likeState declaration before thumbnail to fix scope
  - `AlbumItemMenu.kt`: Added bookmark icon on album thumbnail in menu display with reactive likeState Flow
  - `OnlineAlbumItemMenu.kt`: Added bookmark icon on album thumbnail with reactive likeState Flow
  - `OnlineArtistItemMenu.kt`: Added bookmark icon on artist thumbnail with reactive likeState Flow
  - `LocalPlaylistItemMenu.kt`: Added bookmark icon on playlist thumbnail when bookmarked, clip moved to inner Box
  - `OnlinePlaylistItemMenu.kt`: Added bookmark icon on playlist thumbnail when bookmarked, clip moved to inner Box
  - `PlaylistSongList.kt`: Bookmark icon color changed to favoritesIcon when playlist is bookmarked
  - All icons use same pattern as SongItem's like indicator (HeaderIconButton, 12dp, BottomStart alignment with absoluteOffset)

feat(dislike): add complete dislike system for songs, artists, and albums
  - `SongTable.kt`: Rotation neutre→liké→disliké→neutre, added `getAllDislikedIds()`, `getSongsByDislikedArtists()`, `getSongsByDislikedAlbums()` queries
  - `ArtistTable.kt`: Added `dislikedAt` field, `rotateLikeState()`, `likeState()`, `toggleDislike()`, `isDisliked()`, `allDisliked()`
  - `AlbumTable.kt`: Added `dislikedAt` field, `rotateLikeState()`, `likeState()`, `toggleDislike()`, `isDisliked()`, `allDisliked()`
  - `From36To37Migration.kt`: Database migration adding `dislikedAt` column to Artist and Album tables
  - `Database.kt`: Version bumped to 37
  - `YouTubeSync.kt`: State determined BEFORE rotation (null→true, true→false, false→null), YouTube sync blocked when disliked
  - `LocalArtistItemMenu.kt`, `OnlineArtistItemMenu.kt`: 3-state icon (bookmark/bookmark_slash/bookmark_outline) with red color
  - `AlbumItemMenu.kt`, `OnlineAlbumItemMenu.kt`: 3-state icon with red color
  - `SongItemMenu.kt`, `VideoItemMenu.kt`, `PlayerItemMenu.kt`: heart_dislike icon with red color
  - `MiniPlayer.kt`, `Essential.kt`, `Modern.kt`: Red color for dislike icon in player controls
  - `FollowButton.kt`: 3-state rotation (followed→neutral→disliked→neutral), red background when disliked
  - `AlbumDetails.kt`: AlbumBookmark uses rotateLikeState(), red color when disliked
  - `SongItem.kt` (components): Real-time like state via Flow from database, shows heart/heart_dislike on thumbnail
  - `HomeArtist.kt`, `HomeAlbum.kt`: Added Disliked tab with sort preferences
  - `ArtistsType.kt`, `AlbumsType.kt`: Added Disliked enum value
  - `GeneralSettings.kt`: 3 toggles for exclude disliked songs/artists/albums (default ON)
  - `Preferences.kt`: Added `excludeDislikedSongsKey`, `excludeDislikedArtistsKey`, `excludeDislikedAlbumsKey`
  - `Player.kt`: Exclusion in `excludeMediaItems()` for disliked songs, artists, albums
  - `Shuffler.kt`: Exclusion in `play()` for disliked songs, artists, albums
  - `NZikRadio.kt`: Exclusion in `discoverFilter()` for disliked songs, artists, albums
  - `EventTable.kt`: Excluded disliked songs/artists/albums from stats and monthly playlists
  - `LikeState.kt`: Fixed initial value from `false` to `null` to prevent dislike icon flashing at load
  - `strings.xml`: Added `exclude_disliked_songs`, `exclude_disliked_artists`, `exclude_disliked_albums`
  - Rotation order: songs (liked→neutral→disliked→neutral), artists (followed→neutral→disliked→neutral), albums (bookmarked→neutral→disliked→neutral)
  - Dislike is local only - NOT synced to YouTube

fix(queue): enable long press context menu on queue items
  - `Queue.kt`: Removed `onLongClick = {}` from `SongItem` call — empty lambda suppressed `SongItemMenu` creation (condition in `SongItem.kt:210`: menu only created when `onLongClick == null`)
  - Changed to `onLongClick = null` (explicit) with comment to prevent future regression
  - `SongItemMenu` content is rendered lazily (only on open) — no performance impact on queue scrolling

fix(innertube): fix HTTP 400 on browse/search for devices with unsupported region locale (issue https://github.com/N-Zik-Group/N-Zik/issues/6)
  - Root cause: Devices with `Locale.getDefault().country` returning unsupported regions (e.g. MM/Myanmar) or `Locale.getDefault().toLanguageTag()` returning full tags (e.g. `fr-MM`) caused YouTube's WEB_REMIX API to return 400 INVALID_ARGUMENT
  - `Innertube.kt`: Added retry-on-400 logic for `browse()`, `search()`, and `getSearchSuggestions()` — falls back to language-based region (e.g. `fr`→`FR`) when ISO region is rejected
  - `Innertube.kt`: `applyLocale()` now validates `gl` against `Locale.getISOCountries()` (matches Metrolist's `CountryCodeToName` approach)
  - `Innertube.kt`: `regionOverrideActive` no longer propagated to innertubex — was stripping visitor data from browse requests
  - `Innertube.kt`: `useLoginForBrowse` now delegates directly to innertubex (fixed race condition with field initializer)
  - `Innertube.kt`: `dataSyncId` setter sends raw value instead of parsing `||` separator
  - `Innertube.kt`: `ensureVisitorData()` no longer checks against hardcoded `DEFAULT_VISITOR_DATA`
  - `Innertube.kt`: `defaultRequest` URL changed to full path `https://music.youtube.com/youtubei/v1/` (matches Metrolist)
  - `MainApplication.kt`: Disables `useLoginForBrowse` and writes to SharedPreferences when cookie is invalid/missing/expired
  - `AccountsSettings.kt`: `useLoginForBrowse` toggle visible when YouTube enabled (grised if not logged in); re-enabled after login
  - `SettingsScreen.kt`: Added `enabled` parameter to `OtherSwitchSettingEntry` with visual feedback (alpha 0.5f when disabled)
  - `strings.xml`: Added `youtube_connect_first` string for disabled toggle description
  - Reference: Metrolist validates `gl` in `App.kt:280-284` via `CountryCodeToName`; innertubex `systemYouTubeLocale()` has no validation (potential upstream fix)

improve(debug): add centralized network logging via InnertubeLogger/InvidiousLogger with Timber routing
  - Created `InnertubeLogger.kt` (JVM module) — centralized logging abstraction with listener pattern, fallback to println when no listener registered
  - Created `InvidiousLogger.kt` (invidious module) — same pattern for Invidious HTTP client
  - `Innertube.kt`: Added OkHttp interceptor in `createClient()` logging all InnerTubeX API requests (method, path, status code, timing)
  - `SongInfo.kt`: Added OkHttp interceptor to `youtubeWebClient` logging YouTube WEB client requests
  - `YtMusic.kt`: Replaced 32 `println()` calls with `InnertubeLogger.d/e/w()`
  - `HttpFetcher.kt`: Replaced 2 `println()` calls with `InnertubeLogger.d()`
  - `Invidious.kt` (me.knighthat): Replaced `println()` with `InnertubeLogger.e()`
  - `Invidious.kt` (it.fast4x): Replaced 4 `println()` calls with `InvidiousLogger.d/e()`
  - `AlbumPage.kt`, `ChartsPageComplete.kt`, `HistoryPage.kt`, `LibraryPage.kt`, `NewReleaseAlbumPage.kt`: Replaced `println()` with `InnertubeLogger.d/e()`
  - `MainApplication.kt`: Registered `InnertubeLogger` and `InvidiousLogger` listeners routing to Timber
  - `ShazamRepository.kt`: Replaced 7 `Log.e/w()` calls with `Timber.tag()`
  - Removed 12 lines of dead `//Log.d` code across 6 files
  - `Innertube.kt`: Removed dead `init` block (`YoutubePreferences.preference` always null)
  - `YoutubePreferences.kt`: Added `@Deprecated` on unused `preference` property
  - Root cause: JVM modules had no proper logging — println went to stdout invisible in Android debug log; now all logs route through Timber

fix(player): remove brotli compression from InnerTube HTTP client — YouTube doesn't accept brotli-compressed POST bodies
  - `Innertube.kt`: Removed `brotli(1.0F)` from `ContentEncoding` plugin — YouTube's InnerTube API servers accept `gzip` and `deflate` but not `br` for incoming request bodies; brotli compression caused ALL player clients to return `streamingPresent=false, formatCount=0` and the `next` endpoint to return HTTP 400
  - `MainApplication.kt`: Changed `visitorData` and `dataSyncId` fallback from empty string `""` to `null` — empty string passes null-checks and sends `X-Goog-Visitor-Id: ` (empty header), which YouTube rejects; Metrolist uses `takeIf { it != "null" }` pattern
  - Root cause: Ktor's `ContentEncoding` plugin compressed POST bodies with brotli (highest quality 1.0), but YouTube's servers cannot decompress brotli request bodies — gzip/deflate work correctly

fix(player): align InnerTube network layer with Metrolist to fix YouTube stream blocking
  - `Innertube.kt`: Removed suspicious `sec-ch-ua`, `sec-ch-ua-mobile`, `sec-ch-ua-platform`, `Content-Type`, and `prettyPrint=false` headers from defaultRequest — these Client Hints headers allowed YouTube to fingerprint the app as a bot, causing all player clients to return `streamingPresent=false, formatCount=0`
  - `Innertube.kt`: Added defensive `dataSyncId` parsing with `||` split logic (matches Metrolist) — prevents invalid DataSyncId values from breaking authenticated sessions
  - `MainApplication.kt`: Added `isMainProcess()` guard to prevent crash when CrashActivity runs in a separate process — matches Metrolist's process isolation pattern
  - `MainApplication.kt`: Prewarm now waits up to 12s for `visitorData` before launching InnerTubeX prewarm — matches Metrolist's timing pattern, ensures visitor data is available for extraction
  - `Store.kt`: Removed ghost cookie fetch and default cookie to match Metrolist's simpler session model
  - `InnerTubeXPlayer.kt`: Changed failed client tracking from WEB_REMIX-only to all clients (`ConcurrentHashMap<String, Long>`) — matches Metrolist's generic failure tracking
  - `InnerTubeXPlayer.kt`: Cipher refresh now clears ALL client failures, not just WEB_REMIX — allows all clients to retry after cipher update
  - `InnerTubeXPlayer.kt`: Added `withEmbeddedConfigFallback()` extension on `YtConfigParser` — if watch-page config fetch fails, falls back to anonymous embedded player endpoint (matches Metrolist's resilience pattern)
  - `StreamResolver.kt`: Unified `markClientFailed()` and `clearAllFailures()` to replace legacy `markWebRemixFailed()` / `clearWebRemixFailures()` — consistent with InnerTubeXPlayer's generic tracking
  - `PlayerServiceModern.kt`: Updated error handlers to use new `markClientFailed()` / `clearAllFailures()` API — 403/expired and remote playback errors now track the actual failing client, not just WEB_REMIX
  - Root cause: YouTube was rejecting ALL player clients (VISIONOS, WEB_EMBEDDED_PLAYER, WEB_REMIX, WEB_CREATOR, TVHTML5_SIMPLY, WEB_KIDS) because the `sec-ch-ua` headers exposed non-browser fingerprint, and the `next` endpoint returned HTTP 400

improve(debug): replace clipboard copy with file export for debug logs
  - `CopyLogsDialog.kt`: Replaced `textCopyToClipboard()` with `ActivityResultContracts.CreateDocument("text/plain")` — logs are now saved to a user-selected file instead of the clipboard
  - `CopyLogsDialog.kt`: Removed `ClipboardManager` and `ClipData` imports, added `rememberLauncherForActivityResult` with `Dispatchers.IO` coroutine scope for writing to `contentResolver.openOutputStream(uri)`
  - `CopyLogsDialog.kt`: Button now launches file picker (default names: `N-Zik_debug_log.txt`, `N-Zik_crash_log.txt`, `N-Zik_logs.txt`) instead of copying text
  - `OtherSettings.kt`: Updated debug settings entry to use new `export_logs` / `export_debug_log_description` strings
  - `strings.xml`: Added 7 new strings (`export_logs`, `export_debug_log`, `export_crash_log`, `export_both_logs`, `export_debug_log_description`, `export_crash_log_description`, `export_both_logs_description`) — old clipboard strings kept for Crowdin

fix(home): force English locale on related page and isolated sessions for YTM home page data
  - `Innertube.kt`: Added `hl` and `setLogin` parameters to `next()` with isolated session pattern — mirrors existing `browse()` implementation; creates isolated session via `createIsolatedSession(includeAccount = setLogin)`, sets locale override, executes request, closes in `finally` block
  - `RelatedPage.kt`: `relatedPage()` now forces `hl = "en"` on both `next()` and `browse()` calls — previously used global session locale which returned localized section titles (e.g. "Lié" instead of "Related" on French devices), causing `findSectionByTitle()` exact-match to fail and return null
  - `RelatedPage.kt`: `relatedPage()` now accepts `setLogin` parameter and passes it to both API calls — personalized related songs when user is logged in
  - `RelatedPage.kt`: Changed tab finding from `equals("Related")` to `contains("Related")` for robustness against minor title variations
  - `HomeQuickPicksState.kt`: All 3 `relatedPage()` callers (MostPlayed, LastPlayed, CasualPlayed branches) now pass `setLogin = isYouTubeLoggedIn() && Innertube.useLoginForBrowse`
  - `QuickPicksBrowseHandler.kt`: `relatedPage()` call now passes `setLogin` for personalized Android Auto content
  - `AutoSessionCallback.kt`: `relatedPage()` call now passes `setLogin` for personalized Android Auto content
  - `QuickPicksRepository.kt`: `relatedPage()` call now passes `setLogin` for personalized legacy path
  - Root cause: On non-English devices, `relatedPage()` returned null because section titles were localized and didn't match hardcoded English predicates, resulting in empty quick picks (0, 0, 0) on fresh install

improve(ui): Quick Picks section animations and empty-state handling — complete overhaul
  - `HomeQuickPicks.kt`: Wrapped every section (charts, related albums, similar artists, new albums, playlists, moods, generic YTM sections, etc.) in `AnimatedVisibility` with `expandVertically()` + `fadeIn()` enter and `shrinkVertically()` + `fadeOut()` exit — sections now smoothly slide in instead of popping
  - `HomeQuickPicks.kt`: Each section now checks `hasXxxContent` (e.g. `hasChartsContent`, `hasRelatedAlbums`, `hasMoods`) before rendering — empty sections are hidden entirely instead of showing blank space
  - `HomeQuickPicks.kt`: All `item()` blocks use `key` for stable LazyColumn identity and `Modifier.animateItem()` for smooth reorder animations
  - `HomeQuickPicks.kt`: `GenericYtmSections` moved outside the `sectionOrder.forEach` loop into its own `item(key = "generic_ytm_sections")` — prevents recomposition issues when section list changes
  - `HomeQuickPicks.kt`: Error/login/cookie messages moved into `item()` blocks with keys for stable positioning
  - `HomeQuickPicksComponents.kt`: Added `AnimatedSection` and `AnimatedSectionStaggered` composables — reusable animation wrappers with staggered title/content entry (title slides from top, content slides from bottom with 80ms delay)
  - `HomeQuickPicksComponents.kt`: `YtmSectionByTitle` now uses `AnimatedVisibility` for shimmer loading state — shimmers slide in with staggered `slideInVertically` + `fadeIn` (each placeholder delayed by `it * 50ms`) and fade out when items arrive
  - `HomeQuickPicksComponents.kt`: `YtmSectionItems` now wraps content in `Column` with `showTitle` parameter — allows hiding title when section is the primary content
  - `HomeQuickPicksComponents.kt`: Added `contentType` to all `items()` / `itemsIndexed()` calls in `YtmSectionItems`, `GenericYtmSections` for LazyRow/LazyHorizontalGrid performance
  - `HomeQuickPicksSections.kt`: All section composables (`NewAlbumsOfYourArtistsSection`, `NewAlbumsSection`, `RelatedAlbumsSection`, `SimilarArtistsSection`, `MonthlyPlaylistsSection`, `MyTopSection`, `MoodsSection`, `MoodsAndGenresSection`, `ChartsSection`, `GenericYtmSections`) wrapped in `Column` with `showTitle` parameter for conditional title rendering
  - `HomeQuickPicksSections.kt`: All `LazyRow` / `LazyHorizontalGrid` items now use `.animateItem()` for smooth insertion/removal animations
  - `HomeQuickPicksSections.kt`: `ChartsSection` songs and artists grids changed from `GridCells.Fixed(2)` to `GridCells.Fixed(3)` with dynamic height (`Dimensions.itemsVerticalPadding * 3 * 9`) for better density
  - `HomeQuickPicksSections.kt`: Removed redundant `?: {}` empty-block fallbacks after `?.let {}` — cleaner null handling
  - `HomeQuickPicksSections.kt`: `GenericYtmSections` song items now render as `AlbumItem` (thumbnail + title + authors) instead of `SongItem` — consistent visual style with other YTM sections
  - `HomeQuickPicksSections.kt`: `GenericYtmSections` now shows full-page shimmer (`TextPlaceholder` + 4 `LazyRow` rows) when `isLoading && sections.isNullOrEmpty()` — previously showed nothing during initial load
  - `QuickPicksGrid`: Added `scrollToStartTrigger` param — `LaunchedEffect` calls `animateScrollToItem(0)` on refresh so grid resets to top
  - `QuickPicksGrid`: Added `.animateItem()` to `SongItem` for smooth reorder on recommendation changes

improve(state): Quick Picks data loading resilience and refresh behavior
  - `HomeQuickPicksState.kt`: Replaced `coroutineScope` with `supervisorScope` in Phase 1 network calls — one failed API (charts, discover, quick picks) no longer cancels the others; partial data still loads
  - `HomeQuickPicksState.kt`: Added `dbJob: Job?` tracking — `dbJob?.cancel()` before launching new DB observation prevents duplicate Flow collectors accumulating on refresh
  - `HomeQuickPicksState.kt`: Added `refreshKey: MutableState<Int>` — incremented on `refresh()` to trigger `QuickPicksGrid` scroll-to-start
  - `HomeQuickPicksState.kt`: `refresh()` now clears `recommendations.value = emptyList()` in addition to `trendingList` and `ytmQuickPicks` — stale recommendations no longer flash before new ones load
  - `HomeQuickPicksState.kt`: `homePageInit` changed from `persist<HomePage?>` to `rememberPreference` — proper reactive state instead of manual persistence for YTM section data

improve(perf): EncryptedSharedPreferences caching — eliminate repeated create calls
  - `EncryptedPreferences.kt`: Added `cachedEncryptedPreferences: SharedPreferences?` with `@Volatile` + `@Synchronized` singleton pattern — `EncryptedSharedPreferences.create()` is expensive (~50-100ms), now called once per process lifetime instead of on every preference read
  - `EncryptedPreferences.kt`: Added `clearEncryptedPreferencesCache()` — allows invalidation when master key changes (e.g. after `deleteSharedPreferences` on API 24+)
  - `EncryptedPreferences.kt`: Error recovery unchanged — `deleteSharedPreferences("secure_preferences")` on API 24+ when master key mismatch, but now also clears the cache to force re-creation

fix(sync): YouTube login state read on IO thread — prevent ANR on slow storage
  - `ActionBar.kt`: Moved `cookie`, `isLoginEnabled`, `accountThumbnail` reads from synchronous `remember(trigger)` to `LaunchedEffect(trigger)` + `withContext(Dispatchers.IO)` — `EncryptedSharedPreferences` can block on first access
  - `HistoryList.kt`: Moved `isCoreSyncEnabled` read from direct `appContext().encryptedPreferences.getBoolean()` to `LaunchedEffect(Unit)` + `withContext(Dispatchers.IO)` — prevents main-thread freeze on history screen entry
  - `WelcomeMessage.kt`: Moved `isYouTubeLoggedIn()` and `ytAccountName()` calls to `LaunchedEffect(baseMessage)` + `withContext(Dispatchers.IO)` — welcome greeting no longer blocks composition
  - `SettingsScreen.kt`: Added `ytLoggedIn` state read via `LaunchedEffect(trigger)` + `withContext(Dispatchers.IO)` — settings tab icon/title updates without blocking; passed as param to `AIRecommendationSettings`
  - `AIRecommendationSettings.kt`: Added `ytLoggedIn: Boolean` parameter — replaces direct `isYouTubeLoggedIn()` call for consistent state from parent

fix(sync): Logout cleanup — disable all sync sub-toggles and useLoginForBrowse
  - `AccountsSettings.kt`: On YouTube logout: now disables `enableYouTubeSync`, `useLoginForBrowse`, and all 14 sync sub-toggles (`autosyncArtists`, `autosyncAlbums`, `autosyncPlaylists`, `autosyncLikes`, `syncImportHistory`, `syncImportLibrarySongs`, `syncImportUploadedSongs`, `syncImportUploadedAlbums`, `syncImportEpisodes`, `syncPushHistory`, `syncPushSongLike`, `syncPushAlbumBookmark`, `syncPushArtistFollow`, `syncPushPlaylist`, `syncPushEpisode`) — previously sync toggles persisted after logout causing silent failures
  - `AccountsSettings.kt`: On YouTube logout: resets `Innertube.useLoginForBrowse = false` — prevents stale login state for browse requests
  - `AccountsSettings.kt`: On YouTube login restore: auto-enables `useLoginForBrowse` only if real credentials exist (`"SAPISID" in parseCookieString(savedCookie)`) — prevents enabling browse with empty/expired cookies
  - `AccountsSettings.kt`: Added `LaunchedEffect(isYouTubeLoginEnabled)` — auto-disables `isYouTubeSyncEnabled` when login is turned off, prevents sync running without credentials
  - `AccountsSettings.kt`: "Login for Browse" toggle now only visible when `isLoggedIn` — hides irrelevant option when not logged in
  - `AccountsSettings.kt`: Sync section visibility changed from `useLoginForBrowseSync` to `useLoginForBrowseSync && isYouTubeLoginActive` — sync settings hidden when YouTube login is disabled

fix(ui): Home tabs animation timing — consistent snappy transitions
  - `HomeAlbum.kt`: All `AnimatedVisibility` animations now use explicit `tween(200)` animationSpec — toolbar show/hide and sync button visibility transitions are consistent 200ms instead of default duration
  - `HomeArtist.kt`: Same `tween(200)` fix across all `AnimatedVisibility` blocks
  - `HomeLibrary.kt`: Same `tween(200)` fix across all `AnimatedVisibility` blocks
  - `HomeSongsScreen.kt`: Same `tween(200)` fix across all `AnimatedVisibility` blocks

fix(coroutine): Replace unstructured CoroutineScope with structured scopes
  - `HomeAlbum.kt`: Replaced `CoroutineScope(Dispatchers.IO).launch { importYTMLikedAlbums(force = true) }` with `scope.launch { ... }` — uses the composable's lifecycle-aware scope, prevents leak on navigation
  - `HomeArtist.kt`: Replaced `CoroutineScope(Dispatchers.IO).launch { importYTMSubscribedChannels(force = true) }` with `coroutineScope.launch { ... }` — structured concurrency
  - `HomeLibrary.kt`: Replaced `CoroutineScope(Dispatchers.IO).launch { ... }` with `coroutineScope.launch { ... }` — structured concurrency for import operations

fix(parsing): skip duration patterns in artist name parsing
  - `Utils.kt`: `parseArtists()` now skips strings matching duration patterns (`45:38`, `1:23:45`) via regex `^\d{1,2}:\d{2}(:\d{2})?$` — prevents duration strings from being treated as artist names in album/playlist metadata

improve(ui): global smart hide/show of App Header and NavBar on scroll
  - `MainActivity.kt`: Replaced linear pixel tracking in `NestedScrollConnection` with intelligent threshold-based hiding (-150px down to hide, +15px up to show).
  - `MainActivity.kt`: Removed restrictions to enable scroll-hide behavior on Quick Picks, Search Results, and Settings screens.
  - `MainActivity.kt`: Animated transitions using `Animatable` for smooth sliding in/out regardless of list size.

feat(ui): smooth hide/show of TabToolBar on scroll up/down
  - `HomeSongsScreen.kt`, `HomeArtist.kt`, `HomeAlbum.kt`, `HomeLibrary.kt`: Added `snapshotFlow` to track scroll direction and `firstVisibleItemIndex`.
  - Hides the toolbar when scrolling down past index 10, and instantly reappears when scrolling up.

feat(sync): add detailed status messages and expandable UI for sync operations
  - `SyncYTMusicUtils.kt`: Added `SyncItemStatus` holding state and `detailMessage`.
  - `SyncYTMusicUtils.kt`: Track exact counts (e.g., "15 playlists synced") or errors for 13 operations using `latestSyncDetail`.
  - `SyncStatusDialog.kt`: Transformed rows into expandable columns; click to reveal details below.
  - `SyncStatusDialog.kt`: Added chevron arrow with 180deg animated rotation and `AnimatedVisibility` for the detail text.
  - `SyncStatusDialog.kt`: Used `uiRoundnessShape()` clipping and ripple effect for the clickable expandable area.
  - `AccountsSettings.kt`, `HomeSongsScreen.kt`, `HomeAlbum.kt`, `HomeArtist.kt`, `LocalPlaylistItemMenu.kt`, `OnlinePlaylistItemMenu.kt`: Handled updated `SyncStatus` properties and minor UI tweaks.
  - `PlayerServiceModern.kt`: Added `isYouTubeSyncEnabled()` gate and explicit `pushHistoryEnabled` preference toggle to YTM history pushing.
  - `strings.xml`: Added string resources for "Auto-Sync: ON/OFF".

improve(ui): search bar icon sizing and text alignment refinements
  - `SearchScreen.kt`: Resized search, microphone, sound_effect, and close icons to 25dp for consistent sizing
  - `SearchScreen.kt`: Adjusted text field padding (`start = 30.dp, end = 100.dp`) to prevent text overlapping with right-side icons
  - `SearchScreen.kt`: Set `contentAlignment = Alignment.CenterStart` on text box so typed text aligns left
  - `MusicAudioSearchOverlay.kt`: Added `verticalAlignment = Alignment.CenterVertically` to the play/more-details button Row in `RecognizedTrackCard`

Removed:
- remove(audio-search): remove system audio (MediaProjection) mode from audio search
  - `MusicAudioSearchOverlay.kt`: Removed `useSystemAudio`, `mediaProjection` state variables and `projectionLauncher` — eliminates Android 14+ FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION crash
  - `MusicAudioSearchOverlay.kt`: Removed Microphone/System toggle UI (was gated behind `Build.VERSION.SDK_INT >= Q`)
  - `MusicAudioSearchOverlay.kt`: Simplified `startListening()` to always use microphone — removed system audio branch and `MediaProjectionManager` dependency
  - `AudioRecorder.kt`: Removed `useSystemAudio` and `mediaProjection` parameters from `start()` — always creates mic recorder
  - `AudioRecorder.kt`: Removed `createSystemAudioRecorder()` method and unused imports (`AudioPlaybackCaptureConfiguration`, `MediaProjection`, `Build`)
  - `strings.xml`: Removed unused string resources `find_source_mic`, `find_source_system`, `find_error_system_audio`

feat(audio-search): add direct play button on recognition results
  - `MusicAudioSearchOverlay.kt`: Added `playRecognizedTrack()` — searches Innertube for the recognized song via `Innertube.searchPage` with `SearchFilter.Song` and plays the first result via `binder?.startRadio()`
  - `MusicAudioSearchOverlay.kt`: Added `isPlaying` state and `LocalPlayerServiceBinder` reference
  - `RecognizedTrackCard`: Added circular play button (accent background, `play.xml` icon) with `CircularProgressIndicator` loading state
  - `RecognizedTrackCard`: Play button sits alongside the existing "More details" button — play for direct playback, details for search results screen
  - Flow: recognition → card → [Play] starts radio or [More details] → search results

feat(innertubex): add proxy auth, region override, and login-for-browse settings
  - `Innertube.kt`: Added `proxyAuth` property — enables OkHttp `proxyAuthenticator` for authenticated proxies
  - `Innertube.kt`: Added `regionOverrideActive` + `regionOverride` properties — allows overriding country code for API requests
  - `Innertube.kt`: Added `useLoginForBrowse` property — enables login state for browse requests (disabled by default)
  - `Innertube.kt`: Added `applyLocale()` — checks `regionOverrideActive` before falling back to `Locale.getDefault().country`
  - `Innertube.kt`: Updated `recreateTransport()` to preserve `regionOverrideActive` across proxy changes
  - `Preferences.kt`: Added `proxyUsernameKey`, `proxyPasswordKey`, `regionOverrideKey`, `useLoginForBrowseKey`
  - `OtherSettings.kt`: Added Proxy Username, Proxy Password, and Region Override UI in the Proxy section
  - `AccountsSettings.kt`: Added "Login for Browse" toggle switch in the YouTube section (disabled by default)
  - `MainApplication.kt`: Added startup wiring for proxy auth, region override, and login-for-browse preferences
  - Build verified: `./gradlew :ComposeN-Zik:assembleDebug` BUILD SUCCESSFUL

feat(innertubex): complete InnerTubeX migration verification — Metrolist comparison
  - Full comparison of InnerTubeXPlayer, StreamResolver, StreamUrlCache, DownloadUtil, Innertube facade
  - InnerTubeXPlayer: 85% identical, core extraction logic correct
  - StreamUrlCache: Identical + N-Zik adds `clear()` method
  - StreamResolver: N-Zik-only, InnerTubeX integration correct
  - Download pipeline: N-Zik more feature-rich, core logic equivalent
  - Innertube facade: Different architecture (hybrid vs pure facade) but InnerTubeX integration points correct
  - All cross-component references verified correct
  - Streaming confirmed working via device logs (VISIONOS client, no 403 errors)

fix(player-ui): fix StatsForNerds layout and remove unused fields
  - `StatsForNerds.kt`: Removed Artist + Title row (Row 1.5) — data from InnerTubeX videoDetails, not useful for debug
  - `StatsForNerds.kt`: Removed audio channels display — always shows "Unknown" since InnerTubeX doesn't provide this field
  - `StatsForNerds.kt`: Fixed row nesting — Row 4 (Stream Client) and Row 5 (Volume) were incorrectly nested inside Row 3 (Container/Codec/Sample Rate), causing layout offset
  - Build verified: `./gradlew :ComposeN-Zik:assembleDebug` BUILD SUCCESSFUL

fix(innertubex): fix session sync, proxy handling, and expired stream detection
  - `Innertube.kt`: Session properties (`locale`, `visitorData`, `dataSyncId`, `cookie`) now delegate to `innerTubeX` instead of being stored locally — ensures InnerTubeX always has current session state
  - `Innertube.kt`: Added `recreateTransport()` with `sessionSnapshot()`/`replaceSession()` — preserves full session (cookie, visitorData, dataSyncId, authUser, useLoginForBrowse) across proxy changes
  - `Innertube.kt`: Added `init` block that initializes session from `YoutubePreferences` in one `replaceSession()` call — avoids multiple session change events that cause `CancellationException`
  - `Innertube.kt`: `ensureVisitorData()` now handles nullable `visitorData` and uses local variable to avoid smart cast issues
  - `MyDownloadHelper.kt`: Replaced fragile string matching in `isExpiredStreamError()` with type-safe `HttpDataSource.InvalidResponseCodeException` cause-chain walk — checks for HTTP 403/410/416 response codes
  - `PlayerServiceModern.kt`: Fixed `MyDownloadHelper.songUrlCache.remove()` → `.invalidate()` — `songUrlCache` type changed from `LinkedHashMap` to `StreamUrlCache` which uses `invalidate()` instead of `remove()`
  - Build verified: `./gradlew :ComposeN-Zik:assembleDebug` BUILD SUCCESSFUL

Refactor:
- refactor(code-quality): comply with CODE.md rules — anti-patterns, null-safety, imports, LazyColumn (71 files, ~324 violations)
  - Phase 1 — Anti-patterns critiques: replaced `runBlocking(Dispatchers.IO)` with `LaunchedEffect` + state async in HomeAlbum.kt, HomeArtist.kt, HomeLibrary.kt; replaced `collectAsState()` with `collectAsStateWithLifecycle()` in ImageCacheFactory.kt, SeekBarStaticAudioWaves.kt, HomeSongs.kt; replaced `e.printStackTrace()` with Timber in UpdateScreen.kt
  - Phase 2 — CoroutineScope structuré: replaced `CoroutineScope(Dispatchers.X).launch` with `rememberCoroutineScope()` in 14 menus/dialogs (OnlineAlbumItemMenu, PlayerMenu, VideoItemMenu, PlayerItemMenu, SongItemMenu, etc.); structured scope in StreamResolver.kt (12 occurrences) and PlayerServiceModern.kt (6 occurrences)
  - Phase 3 — Imports wildcard: resolved ~100 wildcard imports in 21 files (LyricsScreen.kt, PlayerServiceModern.kt, StreamResolver.kt, MyDownloadHelper.kt, PlaybackDispatchers.kt, BitmapProvider.kt, AutoSessionConstants.kt, AutoSessionCallback.kt, NetworkQualityHelper.kt, NetworkExtensions.kt, OkHttpRequest.kt, GlobalNetworkLogger.kt, NetworkClientFactory.kt, Store.kt, UpdateScreen.kt, Updater.kt, UpdateDownloadManager.kt, UpdaterConstants.kt, MajorUpdateConfig.kt, GithubRelease.kt, PlaybackExceptions.kt)
  - Phase 4 — Null-safety: replaced ~90+ `!!` operator with safe calls (`?.`, `?: return`, `?: emptyList()`) in settings dialogs (8 files), menus (OnlineAlbumItemMenu, LocalPlaylistItemMenu, OnlinePlaylistItemMenu), ImageCacheFactory.kt, PoTokenGenerator.kt, GoToArtist.kt, GoToAlbum.kt, SleepTimer.kt, AudioDeviceMenu.kt, PlaybackSettingsMenu.kt, MyDownloadHelper.kt, PlaylistWidgetManager.kt, EditMetadataDialog.kt, ExportCacheDialog.kt, SeekBarVisualizer.kt, MainActivity.kt, MainApplication.kt; added Timber.w logging for null cases
  - Phase 5 — LazyColumn key + contentType: added `key` + `contentType` to ~25+ `items()`/`itemsIndexed()` calls in HomeQuickPicksComponents.kt, HomeQuickPicksSections.kt, HomeDiscovery.kt, KaraokeLyricsView.kt, SyncedLyricsView.kt, ChangeAlbumBrowseIdDialog.kt, ChangeArtistBrowseIdDialog.kt, ChangePlaylistBrowseIdDialog.kt
  - Review patches: added error handling with runCatching + Timber.e in HomeAlbum/Artist/Library LaunchedEffect; replaced remaining runBlocking in ExportCacheDialog.kt; added Timber.w for null cases in settings dialogs, ImageCacheFactory, MainActivity/MainApplication
  - Build verified: `./gradlew :ComposeN-Zik:assembleDebug` BUILD SUCCESSFUL

- refactor(code-quality): fix remaining 12 CODE.md violations (9f75661dd1)
  - Replaced 9 `!!` operator with safe calls in OnlinePlaylistItemMenu.kt, HomeQuickPicksSections.kt, HomeQuickPicks.kt, HomeSongs.kt, Sort.kt, EditMetadataDialog.kt, KaraokeLyricsView.kt, LyricsTrackSelector.kt
  - Fixed 3 `runBlocking`: added justification comments in Store.kt (@Blocking), SessionMediaItemMapper.kt (private non-suspend); replaced with `rememberCoroutineScope()` in Locator.kt
  - Build verified: `./gradlew :ComposeN-Zik:assembleDebug` BUILD SUCCESSFUL

Removed:
- remove(home-songs): remove OnDevice loading placeholders
  - `OnDeviceSongs.kt`: Removed `isLoading` variable and placeholder items block (20 `SongItemPlaceholder` rendered during load)
  - `HomeSongs.kt`: Removed unused `SongItemPlaceholder` import
  - Note: Placeholders no longer used by any home songs screen

fix(download): resolve notification, pause state, and progress bar bugs during active/paused downloads
  - MyDownloadService.kt: Reordered onIdle to always show the 'Paused' notification when DownloadManager is paused, preventing it from being swallowed if other downloads had just completed.
  - MyDownloadService.kt: Removed 
esetBatch() from the 'completed' block in onIdle so batch progress (total/completed count) is preserved when pausing, fixing the missing progress bar and the 'infinite loading' bug upon resume.
  - MyDownloadService.kt: Intercepted ACTION_PAUSE_DOWNLOADS and ACTION_RESUME_DOWNLOADS in onStartCommand to persist downloads_paused_state to SharedPreferences.
  - MyDownloadService.kt: Removed the redundant 'Download cancelled' notification in onIdle when the queue becomes empty to prevent the illusion of a new download starting when clicking 'Delete All'.
  - MyDownloadService.kt: Reset downloads_paused_state to False in onIdle when the queue becomes empty so that new downloads don't silently fail to start.
  - MyDownloadHelper.kt: Restored downloads_paused_state upon DownloadManager initialization and applied pauseDownloads() if true, ensuring the pause state survives app restarts.
  - MyDownloadHelper.kt: Restored BatchTotal to downloads.size upon initialization if there are active downloads so the progress bar works after a restart.
perf(player): optimize M3E Morphing theme performance — grid collision + path cache + disabled shake default
  - `AnimatedM3EBackground.kt`: Replace O(n²) pairwise collision with 8×8 spatial grid — only adjacent cells checked, ~90% fewer distance computations per frame
  - `AnimatedM3EBackground.kt`: Add ComposePath pool (18 objects) to reuse paths instead of allocating new ones each frame via `asComposePath()`
  - `AnimatedM3EBackground.kt`: Add delta clamp (`coerceAtMost(0.05f)`) to prevent shape teleportation on frame spikes
  - `AnimatedM3EBackground.kt`: Add index guard in grid collision to process each pair only once (fixes double force application)
  - `AppearanceSettings.kt`: Change default shake sensitivity from `High` to `Disabled` — sensor no longer activated by default
  - `AnimatedM3EBackground.kt`: Change sensor preference fallback default to `Disabled`

fix(player): add Disabled shake option and lazy sensor registration (c43a63791c)
  - `ShakeSensitivityTheme.kt`: Add `Disabled(thresholdG = 0f)` enum value to allow users to disable shake detection entirely
  - `AnimatedM3EBackground.kt`: Replace `DisposableEffect(Unit)` with `DisposableEffect(animating)` — sensor only registers when player is animating
  - `AnimatedM3EBackground.kt`: Skip sensor registration when `sensitivity == Disabled` or `accelerometer == null`
  - `AnimatedM3EBackground.kt`: Clean up listener in `onDispose` to prevent memory leaks
  - `strings.xml`: Add `shake_sensitivity_theme_disabled` = "Disabled"
  - `values-fr/strings.xml`: Add `shake_sensitivity_theme_disabled` = "Désactivé"
  - Note: Full Path cache optimization for morph shapes deferred to follow-up (requires deeper refactor of `asComposePath()` allocation pattern)

fix(player-ui): fix queue lag and stretching, fix miniplayer like icon, update submodules
  - `Player.kt`: Replace `fillMaxHeight` + `translationY` with `Modifier.layout` to fix queue panel stretching and lag. Restore `QueueToolBar` slide down animation.
  - `MiniPlayer.kt`: Fix "like" icon overlay position on the 48dp cover art.
  - `SwipeableContent.kt`, `Queue.kt`, `SongItem.kt`: Related UI tweaks for queue performance.
  - `.gitmodules`: Update `betterlyrics` and `nextvisualizer` to track `main` branch.

perf(home-songs): optimize Home lists performance — eliminate lag on Downloads/Cached tabs
  - `HomeSongs.kt`: Pre-compute `downloadedIds` as `Set<String>` via `derivedStateOf` — O(1) lookup instead of O(n) per item with `List<String>`
  - `HomeSongs.kt`: Pre-compute `cachedSongIds` in `LaunchedEffect` — `binder.cache.isCached()` called once at load instead of N times per item
  - `HomeSongs.kt`: Pre-compute `downloadStatesMap` (`Map<String, DownloadedStateMedia>`) — eliminates per-item Flow subscriptions for cache state
  - `HomeSongs.kt`: Replace `collectAsState()` with `collectAsStateWithLifecycle()` across all tabs (All, Favorites, Top, Downloaded, Cached)
  - `HomeSongs.kt`: Wrap `itemsOnDisplay.clear() + addAll()` in `Snapshot.withMutableSnapshot` for atomic diffing — fixes LazyColumn recomposition dance
  - `HomeSongs.kt`: Remove `distinctBy { it.id }` from `itemsIndexed` — now computed once in `LaunchedEffect` instead of every recomposition
  - `OnDeviceSongs.kt`: Same `Snapshot.withMutableSnapshot` + `distinctBy` removal as HomeSongs
  - `SongItem.kt`: Add optional `precomputedCacheState` and `precomputedDownloadState` params — bypasses 3-4 Flow subscriptions per item when provided
  - `DownloadUtils.kt`: Replace 5 `collectAsState()` calls with `collectAsStateWithLifecycle()` — Flows suspend when app is in background
  - `MediaItemUtils.kt`: Replace 4 `collectAsState()` calls with `collectAsStateWithLifecycle()`
  - Impact: ~300-400 Flow subscriptions eliminated for 100+ visible items, scroll FPS target ≥55

Improved:
- improve(ui): stabilize Home tab headers and cache indicator animations
  - `HomeSongsScreen.kt`: Hoisted `Header()` out of `LazyColumn` into the parent `Column` to prevent full subtree destruction and layout jumps when switching tabs
  - `HomeSongsScreen.kt`: Wrapped `CacheSpaceIndicator` in `AnimatedVisibility` to prevent it from instantly popping when switching to Downloaded/Offline tabs
  - `HomeSongs.kt` & `OnDeviceSongs.kt`: Added `animateItem()` to `SongItemPlaceholder` and `isLoading` states for smooth shimmer-to-content transitions
  - `CacheSpaceIndicator.kt`: Wrapped progress bar in `animateFloatAsState` so the bar fills smoothly from zero
  - `CacheSpaceIndicator.kt`: Replaced `Formatter.formatShortFileSize` for max size with enum `.text` property to correctly display "8GB" instead of "8.2 GB" (bypassing base-1000 formatting)
  - `DataSettings.kt`: Passed `showCacheInfo = false` to `CacheSpaceIndicator` to remove duplicate sizing text that was already present in Settings
  - `HomeSongsScreen.kt`: Hoisted `maxCacheSize` preferences to prevent `AnimatedVisibility` from executing a flash animation when the cache size is set to `Unlimited`

Removed:
- remove(home-songs): hide download buttons from On Device tab (issue on-device-downloads)
  - `HomeSongsScreen.kt`: Prevented `download_all` and `delete_downloads` buttons from rendering in the toolbar for `BuiltInPlaylist.OnDevice`
  - `HomeSongsToolbarSettingsDialog.kt`: Removed `download_all` and `delete_downloads` from `tabAvailableIds` for `OnDevice`, so they no longer appear in the settings modal for this tab
- remove(home-songs): remove loading placeholders to enable reorder animation
  - `HomeSongs.kt`: Removed `SongItemPlaceholder` rendering during `isLoading` so that loaded items slide in with a fluid insertion animation, matching `HomeAlbum` and `HomeArtist`.
- fix(home): prevent grid items from animating over headers on load
  - `HomeAlbum.kt`: Hoisted TabHeader and ButtonsRow out of `LazyVerticalGrid` so that insertion animations start below the chips.
  - `HomeArtist.kt`: Hoisted TabHeader and ButtonsRow out of `LazyVerticalGrid` for the same reason.
  - `HomeLibrary.kt`: Hoisted TabHeader and ButtonsRow out of `LazyVerticalGrid` for consistency.

- feat(artist-ui): apply artist ui roundness setting globally across the app
  - `AppearanceSettings.kt`: Added a new slider for "Artist roundness"
  - `ArtistItem.kt`, `Dialog.kt` (SelectorArtistsDialog): Applied `artistThumbnailShape()`
  - `LocalArtistItemMenu.kt`, `OnlineArtistItemMenu.kt`: Applied `artistThumbnailShape()` to the header thumbnails
  - `OnlineSearchList.kt`: Applied `artistThumbnailShape()` for ArtistItems in search results
  - `StatisticsPage.kt`: Applied `artistThumbnailShape()` to the artist top ranking overlay
  - `ChangeArtistBrowseIdDialog.kt`: Applied `artistThumbnailShape()` to the artist search results
  - `Dialog.kt` (`SelectorArtistsDialog`): Fixed clipping issue where inner image lacked `artistThumbnailShape()` and dialog was incorrectly shaped
  - `ArtistDetails.kt` & `LayoutWithAdaptiveThumbnail.kt`: Passed `artistThumbnailShape()` into `DynamicOrientationLayout` for landscape artist layout

feat(sync): new YouTube sync engine — 3-state like rotation, sync direction, auto-sync options and status UI
  - ⚠️ KNOWN ISSUE: the new sync is very buggy — expect failures/inconsistencies until follow-up fixes land; treat as experimental for now
  - `YouTubeSync.kt`: `toggleSongLike` → `rotateSongLikeState` — cycles likes neutral → liked → disliked; pushes to YouTube only when `isYouTubeSyncEnabled` + `syncPushSongLike` + direction ≠ `YT_TO_APP` + network connected; disliked = local blacklist only (never pushed to YT)
  - `SyncDirection.kt` (new): `TWO_WAY` / `APP_TO_YT` / `YT_TO_APP` enum, consumed by `YouTubeSync`, `FollowButton`, `AlbumDetails`, album/playlist menus and `NewPlaylistDialog`
  - `Preferences.kt`: new keys — `syncDirectionKey`, `syncPushSongLikeKey`, `syncPushAlbumBookmarkKey`, `syncPushArtistFollowKey`, `syncPushPlaylistKey`, `syncImportHistoryKey`, `syncPushHistoryKey`, import/clear keys, `syncCooldownKey`, `syncShowDetailsKey`, `syncBackgroundGuardKey`
  - `AccountsSettings.kt`: new sync section — direction picker, per-target push toggles, auto-sync options (channels/albums/playlists/likes), cooldown slider (default 30 min), background guard (default on), per-playlist auto-sync, "Show sync details", "Sync Now", "Clear synced data"
  - `SyncStatusDialog.kt` (new): per-operation status (running / last sync Xmin ago / never) opened from settings
  - `SyncResultDialog.kt` (new): per-operation ✓/✗ result list with elapsed time after "Sync Now"
  - `SongTable.kt`: added `toggleDislike()`, `likeState(songId, likedAt)`, `getLikedAt()`; rewrote `rotateLikeState()` CASE logic (neutral → now-ms → -1); `isYoutubeSong` added to song queries
  - `Song.kt` (model + Room entity): new `isYoutubeSong` field — `From34To35Migration` (ALTER TABLE Song ADD COLUMN)
  - `Playlist.kt` / `PlaylistTable.kt`: new `isAutoSync` field + `toggleAutoSync()` / `isAutoSync()` Flow — `From35To36Migration` (ALTER TABLE Playlist ADD COLUMN)
  - `Database.kt`: Room version 34 → 35, both migrations registered, schema `35.json` exported
  - `HomeSyncService.kt`: fixed boolean classification (`isYoutubeX && id.startsWith(...)` → `||`) for artist/album sync — local items no longer misclassified as YouTube; migrated calls to new Innertube API
  - `LikeState.kt`: added `setDisLikeState()`; `Preference.kt`: new Disliked tab sort keys
  - `strings.xml`: ~45 new sync strings (direction, push toggles, auto-sync, cooldown, background guard, status, clear synced data)

Refactor:
- refactor(innertube): remove request body objects — direct named parameters everywhere
  - Deleted `models/bodies/*.kt` (13 body classes: `SearchBody`, `BrowseBody`, `PlayerBody`, `QueueBody`, ...) + `YouTubeClient.kt` + `YouTubeContext.kt`
  - All `it.fast4x.innertube.requests.*` functions now take named args (`query =`, `browseId =`, ...) instead of body objects
  - Call sites updated across `extensions/innertube` + `ComposeN-Zik` (`Database.kt`, `HomeSyncService.kt`, settings, menus, ...)

Other:
- test(playback): add stream cache + session change tests
  - `StreamUrlCacheTest.kt` (new): cache put / invalidate / expiry behavior
  - `StreamResolverSessionChangeTest.kt` (new): resolver behavior across Innertube session changes

perf(bookmark): centralized bookmark state cache — batch SQL queries for artists, albums, playlists
  - `BookmarkStateManager.kt` (new): Centralized manager for artist/album/playlist bookmark states with `getArtistBookmarkStates()`, `getAlbumBookmarkStates()`, `getPlaylistBookmarkStates()` returning `Flow<Map<String, Boolean?>>` — 1 SQL query for N items instead of N queries
  - `ArtistTable.kt`: Added `ArtistBookmarkState` data class + `getBookmarkStatesForArtists()` batch query
  - `AlbumTable.kt`: Added `AlbumBookmarkState` data class + `getBookmarkStatesForAlbums()` batch query
  - `PlaylistTable.kt`: Added `PlaylistBookmarkState` data class + `getBookmarkStatesForPlaylists()` batch query
  - `ArtistItem.kt`: Added optional `bookmarkState: Boolean?` parameter — when provided, skips individual DB query
  - `AlbumItem.kt`: Added optional `bookmarkState: Boolean?` parameter — when provided, skips individual DB query
  - `PlaylistItem.kt`: Added optional `isBookmarked: Boolean?` parameter for Innertube.PlaylistItem overload
  - Applied to 12+ screens: HomeArtist, HomeAlbum, HomeDiscovery, HomeQuickPicksSections, HomeQuickPicksComponents, OnlineSearchList, MoodList, StatisticsPage, NewAlbumsFromArtists, NewAlbums, AlbumDetails, ArtistDetails
  - Performance: N visible items = 1 SQL query instead of N

feat(video): add like icon on VideoItem
  - `VideoItem.kt`: Added `likeState: Boolean? = null` parameter + `HeaderIconButton` with heart icons (getLikedIcon/getDislikedIcon) on thumbnail BottomStart
  - `OnlineSearchList.kt`: Passes `likeState` to VideoItem via `Database.songTable.likeState()` query
  - `HomeQuickPicksSections.kt`: Passes `likeState` via `LikeStateManager.getLikeStates()` batch query
  - `HomeQuickPicksComponents.kt`: Passes `likeState` via `LikeStateManager.getLikeStates()` batch query
  - `ArtistDetails.kt`: Passes `likeState` via `LikeStateManager.getLikeStates()` batch query for video sections

fix(search): add bookmark/like icons to OnlineSearchList
  - `OnlineSearchList.kt`: Added bookmark icons for Album, Artist, Playlist items in list view (else branch)
  - `OnlineSearchList.kt`: Added like icon for VideoItem in list view
  - Pattern: queries `Database.*Table.likeState()` for each item type, displays `HeaderIconButton` on thumbnail

fix(rotation): fix rotateLikeState SQL — correct cycle order
  - `ArtistTable.kt`: Fixed SQL rotation to use original values (dislikedAt IS NULL AND bookmarkedAt IS NULL for neutral→bookmarked)
  - `AlbumTable.kt`: Same SQL fix for consistent rotation
  - Cycle: neutral → bookmarked → disliked → neutral (was: neutral → disliked → bookmarked)

fix(toaster): fix inverted toaster messages for bookmark/dislike
  - `OnlineArtistItemMenu.kt`: Fixed newState mapping (null→true for neutral→bookmarked)
  - `OnlineAlbumItemMenu.kt`: Same fix
  - `LocalArtistItemMenu.kt`: Same fix
  - `AlbumItemMenu.kt`: Same fix
  - `AlbumDetails.kt`: Same fix
  - `FollowButton.kt`: Same fix

fix(insert): insert artist/album before rotateLikeState
  - `OnlineArtistItemMenu.kt`: Added `insertIgnore()` before `rotateLikeState()` for online artists
  - `OnlineAlbumItemMenu.kt`: Added `insertIgnore()` before `rotateLikeState()` for online albums
  - Root cause: `rotateLikeState` does UPDATE — if item doesn't exist in DB, affects 0 rows

fix(dislike): preserve dislikedAt across YouTube fetch
  - `AlbumScreen.kt`: Replaced `upsert()` with `insertMetadata()` + `updateMetadata()` — doesn't touch bookmarkedAt/dislikedAt
  - `ArtistScreenModern.kt`: Same pattern — `insertMetadata()` + `updateMetadata()`
  - `HomeSyncService.kt`: Added `dislikedAt` to all 6 album/artist upsert calls
  - `Database.kt`: Added `dislikedAt` to all 4 album upsert calls
  - `UpdateYoutubeEntity.kt`: Added `dislikedAt` to album/artist upsert calls
  - `AlbumDetailHandler.kt`: Added `dislikedAt` to album upsert call
  - Root cause: `@Upsert` replaces ALL columns — omitting `dislikedAt` reset it to null

feat(dislike): DislikeMode enum — centralized dislike toggle
  - `DislikeMode.kt` (new): Enum `Enabled`/`Disabled` with `isEnabled` property, implements `TextView`
  - `GeneralSettings.kt`: `ValueSelectorDialog` for songs/artists/albums dislike toggle
  - `SongTable.kt`: Added `clearAllDisliked()` query (UPDATE Song SET likedAt = NULL WHERE likedAt = -1)
  - All menus: `showDisliked.isEnabled` check — uses `rotateLikeState` when enabled, `toggleBookmark`/`toggleLike` when disabled
  - `Shuffler.kt`, `NZikRadio.kt`, `Player.kt`: `getString` + `DislikeMode.valueOf` pattern for non-Compose contexts
  - `YouTubeSync.kt`: Same pattern for song like rotation

fix(settings): settings dialogs write to correct DislikeMode keys
  - `HomeSongsSettingsDialog.kt`: Writes to `excludeDislikedSongsKey` (DislikeMode enum) instead of `showDislikedPlaylistKey` (Boolean)
  - `HomeArtistsSettingsDialog.kt`: Writes to `excludeDislikedArtistsKey` (DislikeMode enum) instead of `showDislikedArtistKey` (Boolean)
  - `HomeAlbumsSettingsDialog.kt`: Writes to `excludeDislikedAlbumsKey` (DislikeMode enum) instead of `showDislikedAlbumKey` (Boolean)
  - Root cause: settings dialogs were writing to old Boolean keys while Home screens read from new DislikeMode enum keys

fix(home): filter disliked items from All tab when dislike disabled
  - `HomeArtist.kt`: Filters `dislikedAt == null` when `!showDislikedArtist.isEnabled`
  - `HomeAlbum.kt`: Filters `dislikedAt == null` when `!showDislikedAlbum.isEnabled`
  - `HomeSongs.kt`: Filters `likedAt != -1L` when `!showDislikedPlaylist.isEnabled`
  - `HomeSongsScreen.kt`: Tab visibility uses `showDislikedChip.isEnabled` in toggleMap
  - `HomeArtist.kt`: Tab visibility uses `showDislikedArtist.isEnabled` in toggleMap
  - `HomeAlbum.kt`: Tab visibility uses `showDislikedAlbum.isEnabled` in toggleMap

fix(settings): use bookmark_slash icon for dislike tabs
  - `HomeArtistsSettingsDialog.kt`: Changed `R.drawable.heart_outline` to `R.drawable.bookmark_slash` for disliked icon
  - `HomeAlbumsSettingsDialog.kt`: Changed `R.drawable.heart_outline` to `R.drawable.bookmark_slash` for disliked icon

fix(history): default sort order descending
  - `Preference.kt`: Changed `HISTORY_SORT_ORDER` default from `Ascending` to `Descending` — shows newest events first

perf(swipe): optimize SwipeableContent — eliminate N+1 DB queries and SharedPrefs listeners per item
  - `SwipeableContent.kt`: Added optional pre-computed params to `SwipeablePlaylistItem` and `SwipeableQueueItem` — `likeState`, `downloadStateParam`, `downloadedStateMediaParam`, `swipeLeftActionParam`, `swipeRightActionParam`
  - `SwipeableContent.kt`: When params provided, skips individual DB queries and SharedPrefs reads — uses `mutableStateOf` fallback for backward compat
  - `SwipeableContent.kt`: Fixed `CoroutineScope` leak — replaced `CoroutineScope(Dispatchers.IO).launch` with `rememberCoroutineScope()` in both `SwipeablePlaylistItem` and `SwipeableQueueItem`
  - `SwipeableContent.kt`: Added `onDownloadToggle` in `SwipeablePlaylistItem` — handles download/cancel/delete toggle (not downloaded→download, downloading→cancel, downloaded→delete)
  - `PlaylistSwipeAction.kt`: `getStateIcon()` already handles download states correctly (downloading/queued/restart show progress icon)
  - 10 parent screens updated to hoist preferences and download states: `OnlineSearchList.kt`, `HomeSongs.kt`, `Queue.kt`, `PlaylistSongList.kt`, `HistoryList.kt`, `OnDeviceSongs.kt`, `MoodList.kt`, `AlbumDetails.kt`, `ArtistLocalSongs.kt`, `LocalPlaylistSongs.kt`
  - Pattern: `MyDownloadHelper.downloads.collectAsStateWithLifecycle()` hoisted at composable level, `rememberPreference` for swipe actions hoisted before LazyColumn/LazyGrid
  - Impact: 50 visible items → 0 flows DB + 2 SharedPrefs listeners + 0 scope leaks (was: ~250 flows + 100 listeners + 50 leaks)

improve(ui): finger-linked header/navbar scroll animation
  - `MainActivity.kt`: Header and navbar now move directly with scroll delta (no more threshold-based jump)
  - `MainActivity.kt`: `onPreScroll` directly updates offset with scroll delta, clamped to bar height
  - `MainActivity.kt`: `onPostScroll` forces bars visible when scroll exhausted at top (`available.y > 0`)
  - `MainActivity.kt`: `onPostFling` snaps to fully visible or fully hidden with linear 150ms animation
  - `MainActivity.kt`: Removed accumulated scroll tracking (`accumulatedDownScroll`/`accumulatedUpScroll`) — no longer needed
  - Behavior: scroll down → header slides up with finger, scroll up → header slides down with finger, release → snaps to nearest position, scroll to top → bars reappear

fix(rescue): synchronize rescue center colors with main app theme and fix app shortcuts dialog (issue https://github.com/N-Zik-Group/N-Zik/issues/811)
  - `RescueActivity.kt`: Reverted hardcoded background color and initialized MonetCompat to fetch dynamic palettes to perfectly match the main app's theme
  - `AppShortcutsSettingsDialog.kt`: Fixed shortcut dialog to properly lock and gray out disabled entries

fix(rescue): use dynamic theme color for rescue shortcut icon (issue https://github.com/N-Zik-Group/N-Zik/issues/811)
  - `shortcut_rescue.xml`: Replaced hardcoded black tint with `@color/shortcut_icon` so the icon adapts correctly to the launcher's dynamic theme like other shortcuts.



feat(lyrics): 30-day refetch TTL with per-row timestamp and protected stored data
  - Lyrics.kt: new nullable lastFetchedAt: Long? column stamped on every fetch write
  - LyricsDecisionMaker.kt: unified per-row rule (absent/empty -> dedup-gated fetch, non-empty edited -> never, word-timed -> 30-day TTL with null stamp = expired, no word timings -> Karaoke/Synced dedup hunt or Unsynced TTL), injected 
ow: Long clock with clock-skew guard
  - LyricsFetchWorker.kt: upsertUnlessEdited stamps lastFetchedAt and refuses to clear an existing non-empty row with an empty fetch result
  - From40To41Migration.kt: adds lastFetchedAt to Lyrics and rebuilds Song/Playlist/Album/Artist/Format/SongPlaylistMap to apply the entity-declared column defaults that the deployed v40 schema never had (Room post-migration verification compares defaults; SQLite cannot set a default via ALTER TABLE) - parents rebuilt before children, SongPlaylistMap indexes recreated
  - Root cause of the 2026-09-18 crash: commit 86ba201acd added ~48 @ColumnInfo(defaultValue=...) to legacy entities after v40 shipped, so the 40 -> 41 upgrade would have failed Room verification for every user; fixed by the rebuild migration (annotations kept per maintainer decision, dev-DB and production-DB upgrade paths both covered)
  - Database.kt: version 41 + From40To41Migration registered in the inline migration list
  - Tests: LyricsDecisionMakerTest (19: full mode matrix, clock skew, per-mode active sets on empty DB), LyricsFetchWorkerTest (16: stamp on write, empty-result guard, all-sources-fail keeps data), LyricsTableTest (5: round-trip), From40To41MigrationTest (2: full v40 fixture, rows survive the rebuild), DatabaseUpgrade40To41Test (2: real Room builder upgrade from production v40 without defaults and from dev v40 with defaults - the state behind the 2026-09-18 crash log)

fix(threading): move download-manager init and update-APK purge off the main thread (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - `DownloadUtils.kt` (legacy, `InitDownloader()` body only): the download-manager init no longer runs synchronously during composition; it now runs in a `LaunchedEffect` on `NzikDispatchers.DATA` via the new top-level `initDownloadManagerOffMain(context)` in `MyDownloadHelper.kt`, wrapped in `runCatching` + Timber so a failed one-time init can no longer crash the app
  - `MyDownloadHelper.kt`: dropped the redundant explicit `getDownloads()` call (first-time init already loads the index at the end of `ensureDownloadManagerInitialized`; the `syncDownloads` listener keeps the `downloads` map fresh afterwards)
  - `SeekBarStaticAudioWaves.kt`: the `remember { MyDownloadHelper.getDownloadCache(context) }` blocking fallback is now a `mutableStateOf` filled from a `LaunchedEffect` on `NzikDispatchers.DATA`, guarded by `runCatching` + Timber (freeze-at-first-composition semantics kept)
  - `UpdateScreen.kt`: the update-cache trash click no longer deletes the APK in `onClick`; the purge runs in `NzikDispatchers.fireAndForget(NzikDispatchers.DATA)` via the new top-level `clearUpdateCache(context)`, with the toast shown afterwards on `NzikDispatchers.UI`
  - `UpdateDownloadManager.kt`: `cleanupUpdateDir` now logs each undeletable file with Timber instead of silently discarding it
  - Tests (11 across 3 classes): `MyDownloadHelperPreInitTest` (6 - pre-init accessor safety + init wiring: thread assert + swallowed init failure), `UpdateDownloadManagerCleanupTest` (4 - purge core + click wiring: thread assert + exactly-one `clearCache`), `UpdateDownloadManagerClearCacheTest` (1 - Robolectric: `clearCache` purges the context-derived external-files `Download/nzik_updates` dir and resets the state to Idle)
  - Verification: `:ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:test` green - 533 tests, 0 failures

fix(threading): move the 5 remaining Main-thread blocks found by the re-audit off the main thread (issue https://github.com/N-Zik-Group/N-Zik/issues/606)
  - H1 `PlaylistSongList.kt`: the in-composition mutation of `playlistPage?.songs` (`asMediaItem` x3 per song on every recomposition) is replaced by `filteredPageSongs = remember(filter, playlistSongs) { playlistSongs.applyFilter(filter) }`; new pure top-level `applyFilter` (same title/artist/album predicate, null-safe) also fixes the cumulative-shrink bug (a new search used to filter an already-filtered list). Derived from `playlistSongs` (parental-control filtered + deduplicated) so batch actions keep the previous behavior with no filter; the header song count follows the same list; `hasNonDislikedSongs` is hoisted into one `remember`; the unreachable `disliked_this_collection` toast branches are removed
  - H2 `PlaylistSongList.kt` / `Podcast.kt` / `ArtistLocalSongs.kt`: the 8 download-all / delete-all `onConfirm` dialogs run their per-song loop in a single `NzikDispatchers.fireAndForget(DATA).launch` (dialog state stays on Main); new top-level `processPlaylistBatch`; `ArtistLocalSongs` snapshots the list, merges its per-song `deleteBySongId` launches into one `asyncTransaction` run before the download loop (keeps the previous delete-then-download order)
  - H3 `PreferredStreamClientDialog.kt` / `StreamClientsSettingsDialog.kt` / `AccountsSettings.kt` (legacy, purge block only): the full audio-cache purge (`cache.keys.forEach { removeResource }`) moves to the new `clearAudioCache(cache)` launched on DATA; the toasts and `clearStreamCaches()` stay immediate on Main
  - H4 `SmartTrash.kt`: `deleteDownloads` runs the new `deleteDownloadsOffMain` in one DATA launch; `Toaster.done()` now appears before the batch finishes (accepted)
  - H5 `MediaDownloadDialog.kt` / `DeleteAllDownloadedSongsDialog.kt`: the per-song loop runs in one DATA launch via the new `runDownloadBatch`; `onDismiss()` is called on Main before the launch (`ConfirmationDialog` already closes right after `onConfirm`); the binder check stays before the launch
  - Tests (19 across 5 classes): `PlaylistFilterTest` (8), `PlaylistDownloadBatchOffMainTest` (4), `ClearAudioCacheOffMainTest` (2), `SmartTrashDeleteDownloadsTest` (2), `MediaDownloadBatchOffMainTest` (3, teardown now `@AfterEach` so `unmockkAll()` really runs under JUnit 5)
  - Code review (bmad-code-review): 1 decision + 3 patches applied (H1 base list, ArtistLocalSongs ordering, JUnit teardown, redundant late `onDismiss`); known gap: the dialog-level wiring (call sites) has no automated test
  - Verification: `:ComposeN-Zik:assembleDebug` + `:ComposeN-Zik:test` green. NOT run on a device: playlist with parental control (batch actions vs displayed list), download-all on a large playlist, SmartTrash "Downloaded" clear, stream client change with a large audio cache
